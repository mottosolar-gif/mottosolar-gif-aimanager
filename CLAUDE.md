# aim — AI Manager (กติกาถาวร · เปิดโฟลเดอร์นี้ต้องอ่านก่อนแตะอะไร)

เขียน 2026-08-28 · ทุกตัวเลข/ข้อความในไฟล์นี้ตรวจจากของจริงแล้ว ไม่ได้อ้างจากความจำ
รีโปนี้คือ **บ้านใหม่ของ AI Manager** (Cloudflare Worker + D1) ที่แยกออกมาจาก `C:\WebApp\vendor-ksk`

> **กติกาแม่ของ estate อยู่ที่ `C:\WebApp\vendor-ksk\CLAUDE.md` — ไฟล์นี้ไม่คัดลอกมา ตั้งใจ**
> บ้านนี้จ่ายบทเรียนเรื่อง "สำเนา = ความจริงสองชุด" มาแล้วหลายรอบ · ไฟล์นี้เก็บเฉพาะกติกาที่ผูกกับรีโปนี้
> และ **ชี้** ไปหาของจริงเสมอ ถ้าเจอที่ไหนเขียนกติกาเดียวกันซ้ำ = อันนั้นผิด ให้ลบแล้วชี้มาที่ต้นทาง

---

## ⛔ สองท่าที่ทำแล้วกู้ไม่ได้ — อ่านก่อนพิมพ์คำสั่ง git ใด ๆ

1. **`git clean -fd` ในรีโปนี้ = ลบงาน P2 ทั้งเฟสถาวร**
   งาน WP-P2-A2/A3 (~2,090 บรรทัด: `worker/src/queryEngine.ts` · `worker/src/db/queries.ts` ·
   `worker/test/queryEngine.test.ts` · `worker/test/helpers/sqliteD1.ts`) ยัง **untracked**
   และ **`git remote -v` ว่างเปล่า · `git stash list` ว่างเปล่า** ⇒ มีอยู่ที่เดียวบนดิสก์เดียว ไม่มีสำเนาที่ไหนเลย
2. **`git checkout -- api/line_webhook.php` ที่ `C:\WebApp\vendor-ksk` = ถอดปลั๊กสะพานที่รันอยู่จริง เงียบ ๆ**
   `git show HEAD:api/line_webhook.php` **ไม่มี** `aim_forward` เลย — ตัวเรียกยังไม่ถูก commit
   HEAD มีแต่ไลบรารีที่ไม่มีใครเรียก ⇒ revert แล้วโค้ด **ยังดูเหมือนต่ออยู่** แต่ log เงียบไปเฉย ๆ ไม่มีใครรู้

**ผลพลอยได้ที่อ่านผิดได้ง่าย:** `npm test` = 146 ผ่าน แต่ **94 ข้อในนั้นอยู่ในไฟล์ที่ยัง untracked**
เช็คเอาต์ HEAD สะอาด ๆ จะเหลือ 52 ⇒ ใครรายงาน "146 เขียว" โดยยังไม่ commit = รายงานตัวเลขที่ไม่มีอยู่ในรีโป

---

## 1. บทบาท — เปิดโฟลเดอร์นี้แล้ว "ห้ามลงมือเขียนโค้ดเอง"

| ใคร | ทำอะไร |
|---|---|
| **Claude (ตัวที่กำลังอ่านอยู่นี้)** | สถาปัตยกรรม · เขียนใบสั่งงาน · **ตรวจงาน** · ตัดสินเมื่อชนกติกาบ้าน |
| **Codex** (`codex exec --sandbox workspace-write`) | เขียนโค้ด Worker/เทส/migration ทั้งหมด · **ห้ามตรวจงานตัวเอง** |
| **code-reviewer อิสระ** | ด่านสุดท้ายก่อนถือว่าปิดงาน |

ที่มา: `docs/IMPLEMENTATION_PLAN.md` §7 และ `docs/WORK_PACKAGES_P0.md:3`
⚠ สัญชาตญาณแรกของเซสชันใหม่คือเปิด `worker/src/*.ts` แล้วแก้เลย — **นั่นคือการผิดกฎก่อนพิมพ์เทสบรรทัดแรก**
**ใบสั่งงานทุกใบต้องครบ 6 ช่อง:** เป้าหมาย · ไฟล์ที่แตะได้/ห้ามแตะ · สัญญา · เกณฑ์ผ่าน · ยามที่ต้องยังเขียว · เจออะไรให้หยุดถาม

**Codex จะหยุดขออนุมัติแผนก่อนลงมือเสมอ** — ถ้าใบสั่งงานผ่านการตัดสินมาแล้ว ให้สั่งซ้ำโดยเขียนว่า
"แผนนี้อนุมัติแล้ว ลงมือได้เลย ไม่ต้องถามซ้ำ" ไม่งั้นจะได้แผนกลับมาแทนโค้ด

---

## 2. กติกาที่ห้ามพัง (ทุกข้อจ่ายบทเรียนมาแล้ว)

1. **ไม่มีอะไรถึงผู้ใช้จริงโดยไม่มีคำอนุมัติของตัวเอง** — "อนุมัติดีไซน์ ≠ อนุมัติปล่อยจริง"
   พี่เต้พูดว่า "ไล่ไปเลย P2-5" **ไม่ใช่** ใบเบิกทางให้แตะ LIVE · ทุกใบที่แตะ LIVE (P0-5 · P1-B2 · P1-B3)
   ขออนุมัติแยกใบของมันเองทุกครั้ง · ทดสอบส่ง LINE = ส่งหาพี่เต้คนเดียว
2. **ข้อมูลคนห้ามออกจากบ้าน** (`docs/IMPLEMENTATION_PLAN.md` §4 · D-P0-01)
   คลาวด์เก็บได้แค่ **รหัสแทนตัว (`W07`) + แผนก + บทบาท** · ชื่อจริงอยู่ที่ cim-server เท่านั้น ·
   เหตุผลการลาไม่ออกจากบ้าน · `body_ref` ต้องเป็น NULL เสมอ · แปลงรหัสกลับเป็นชื่อได้ที่ชั้น ②/③ เท่านั้น
   ⚠ **ตัวกันอัตโนมัติถูกถอดไปแล้ว** (D-P0-06 พี่เต้ตัดสินเอง) ⇒ **migration ใหม่ทุกใบต้องไล่ดูคอลัมน์ด้วยตา ทุกครั้ง**
   ⚠ `lib/aim_forward.php` ส่ง **event ดิบทั้งก้อน** (ข้อความ · `userId` · `replyToken`) ออกจากเครื่อง —
   สิ่งเดียวที่ปอกทิ้งคือ `extractMetadata()` ใน `worker/src/payload.ts` ⇒ **แก้ไฟล์นั้นหลวมเมื่อไร ข้อมูลส่วนบุคคลลง D1 ทันทีโดยไม่มีเสียงเตือน**
3. **`canView()` ใน `worker/src/visibility.ts` เป็นจุดตัดสินสิทธิ์จุดเดียว**
   ห้ามเขียนเงื่อนไข role/department ซ้ำใน SQL หรือใน engine (กฎเดียวกับ `next_action.php`/`staff_session.php` ฝั่งโน้น)
   `worker/src/taskMachine.ts` เป็นจุดเดียวที่ตัดสิน ownership ของการเปลี่ยนสถานะงาน
4. **น้ำเสียง "น้องกุ้ง" อยู่ในซอร์สแล้ว ไม่ใช่แค่ในเอกสาร** — `queryEngine.ts` มีข้อความไทยที่ผู้ใช้อ่านจริง
   แทนตัวเองว่า **น้องกุ้ง** · ลงท้าย **ค่ะ/คะ** · เพิ่ม intent ที่ 21 แล้วเผลอเขียนอังกฤษ/"ผมครับ" = เสียงแตกเป็นสองคน
   **ขอบเขตที่ห้ามข้าม: ความน่ารักห้ามทำให้ข่าวร้ายอ่อนลง** — เทสตกต้องบอกว่าตก ตัวเลขต้องเป๊ะ
5. **ห้ามเขียนลงฐานของ cim-server** — ฝั่งนั้นอ่านอย่างเดียวตามคำสั่งพี่เต้
6. **ห้ามคุยในกลุ่ม LINE จริง** — ช่วงทดสอบใช้กลุ่มทดสอบเท่านั้น ไม่งั้นบอทสองตัวพูดทับกัน
   (`IMPLEMENTATION_PLAN.md` §8 ความเสี่ยง #4)
7. **ห้ามผูก vendor เข้ากับ LINE เด็ดขาด** — vendor 41 บริษัทยืนยันตัวตนด้วยอีเมล+bcrypt เท่านั้น
   งาน AI Manager ทั้งหมดเป็นพื้นที่ของ **พนักงานภายใน** (กติกาแม่ฝั่ง vendor-ksk)
8. **กฎกันติดกับดักของฟรี** (`IMPLEMENTATION_PLAN.md` §5) — SQL มาตรฐานเท่านั้น (ห้ามให้ Durable Objects/Queues
   เป็นกระดูกสันหลังจนย้ายบ้านไม่ได้) · ตรรกะธุรกิจห้ามอยู่ใน SaaS · **ห้ามผูกบัตรกับบริการคิดเงินตามการใช้ที่ไม่มีเพดาน**
9. **ห้ามแตะ Worker `mottosolar-line-relay`** (D-P0-02) — เป็นของ homelab รัน 22,370 ครั้ง/24 ชม.
   deploy ทับ = ระบบนั้นตายทันที · Worker ของเราชื่อ **`aim-ingest`** เท่านั้น
   โควตาบัญชี Cloudflare ฟรี = 100,000 invocation/วัน **ต่อทั้งบัญชี** (relay ใช้ไปแล้ว ~22%) ⇒ เพิ่ม cron/polling = ใช้โควตาของคนอื่น
10. **โควตา LINE push จริง = 15,000/เดือน ใช้ไป 610** (วัดจาก LINE API ตรง 2026-08-27 · พี่เต้ทักแก้ตัวเลขเก่าเอง)
    ⚠ เอกสารเก่าที่อื่นในเครือยังเขียนเลขผิด (`vendor-ksk/CLAUDE.md` "~27/เดือนจาก 241" · `TEAM_TOOLS_INVENTORY.md` §A9 "300/เดือน")

---

## 3. สถานะเฟส P0 → P2 (ณ 2026-08-28 · ของจริงบนดิสก์ ไม่ใช่ของในเอกสารเก่า)

| เฟส | สถานะ | ค้างอะไร |
|---|---|---|
| **P0** ท่อ+กระดูก | เดินจริงแล้ว **แต่ยังไม่มีใบปิด** | WP-P0-4 (cron+consumer) โค้ดมีแล้วแต่ไม่มีบันทึกว่าปิด · WP-P0-5 ยังขาด `bin/smoke_aim_forward.php` (D-P0-07 M2) · **WP-P0-6 ยังไม่ปิด** |
| **P1** ประสาทสัมผัส | **ปิดจบ 100%** (D-P0-12) | พิสูจน์วงจรเต็มด้วยของจริงแล้ว: ปุ่มบนมือถือ → webhook ลายเซ็นจริง → คลาวด์ → เปลี่ยนสถานะ → การ์ดกลับ LINE |
| **P2 ชุด A** สายตา | A1 ปิดจบ (D-P0-13) · **A2/A3 อยู่ระหว่างตรวจรอบ 3** | ยัง untracked · ยังไม่มีใบตัดสิน · ยังไม่ผ่าน code-reviewer |
| **P2 ชุด B** ปาก (แตะ LIVE) | **ยังไม่เริ่ม — ต้องรออนุมัติแยก** | B1 ดักคำถามฝั่ง vendor-ksk · B2 endpoint `/ask` · B3 สรุปเช้า-เย็น |
| **P3–P5** | ยังไม่เขียนใบสั่งงาน | ตั้งใจไม่ออกแบบล่วงหน้า — รอผลจริงจาก P2 ก่อน |

**ของจริงที่รันอยู่ตอนนี้:** Worker `aim-ingest` (`https://aim-ingest.mottosolar.workers.dev`) ·
ฐาน D1 `aim-db` · Cron ทุก 1 นาที · endpoint `POST /ingest/line` + `GET /healthz` ·
migration 3 ใบ (`0001_initial` · `0002_task` · `0003_postback`)
**สะพานฝั่ง LIVE ทำงานจริงแล้ว ไม่ใช่ต้นแบบ** — `vendor-ksk/line_webhook.log` มี `aim_forward: HTTP 200` 6 ครั้ง
(ล่าสุด 2026-08-27 16:05:02) และ `HTTP 400` 1 ครั้ง (บั๊ก ULID ใน D-P0-08 ซึ่งแก้แล้ว)

**สิ่งที่ห้ามรายงานว่าเสร็จ (ตรวจแล้วว่ายังไม่จริง):**
- `bin/smoke_aim_forward.php` **ไม่มีอยู่** — เป็นเงื่อนไขปิด WP-P0-5 ที่สเปกตัวเองตั้งไว้
- WP-P0-6 ข้อ 6 สั่งให้ตรวจชื่อจริง **"ด้วยสคริปต์ ไม่ใช่ด้วยตา"** แต่ D-P0-06 ลบสคริปต์นั้นทิ้งแล้ว ⇒ **ข้อนี้ทำตามที่เขียนไม่ได้อีก** ต้องให้พี่เต้เคาะถ้อยคำใหม่
- WP-P0-6 ข้อ 4 (ปิด cim-server แล้วคลาวด์ยังเดิน) ยังไม่เคยทดสอบ
- ประตู P0 ใน `IMPLEMENTATION_PLAN.md` §6 **ผ่านไม่ได้ตามที่เขียน** (มันขอ homelab + ตอบกลับกลุ่ม ซึ่ง P0 ไม่ทำ) — ประตูที่ใช้จริงคือลิสต์ 7 ข้อใน WP-P0-6
- D-P0-13 ค้าง 2 ข้อ: `visibility.ts` ยังเช็ค `=== ""` แทน falsy · `eslint.config.mjs` ยังไม่รวม `vitest.config.ts`

---

## 4. ประตูที่ห้ามข้าม (รันเองทุกครั้ง ห้ามเชื่อรายงานของผู้เขียนโค้ด)

```
npm run typecheck          # tsc --noEmit
npm test                   # vitest — ต้องเขียวทั้งหมด
npm run lint               # eslint (no-floating-promises)
npm run db:migration:check # migration รันซ้ำได้ + body_ref ยัง NULL-only
```
ณ 2026-08-28: typecheck 0 · lint 0 · **146 ผ่าน** (queryEngine 94 · worker 19 · taskMachine 17 ·
visibility 10 · queueTaskAction 6) · migration check ผ่าน 3 ไฟล์
**ตัวเลขนี้เป็นภาพนิ่ง ห้ามอ้างต่อ — รันเองทุกครั้ง**

**CI เป็นความอุ่นใจปลอม 2 ชั้น (ตรวจแล้ว):**
- `.github/workflows/deploy.yml` ยิงเมื่อ push เข้า **`main`** แต่ branch จริงชื่อ **`master`** และ **ไม่มี remote**
  ⇒ **CI ไม่เคยรันเลยสักครั้ง**
- job `test` รันแค่ `npm ci` · `typecheck` · `test` · `db:migration:check` — **ไม่มี `npm run lint`**
  ⇒ กฎ `no-floating-promises` (ที่ตั้งไว้กัน `canView()` ลืม `await` ซึ่งลืมครั้งเดียว = สิทธิ์รั่วทั้งระบบ) **ไม่มีใครบังคับใช้**

**เทสต้องยิงของจริง:** เทส query engine รันบน `node:sqlite` จริงทับ migration จริง (`worker/test/helpers/sqliteD1.ts`)
— mock ที่จับคู่สตริง SQL พิสูจน์ได้แค่ว่า mock ทำงาน ห้ามถอยกลับไปใช้

---

## 5. คำสั่งที่ยิงโดนของจริงทันที (หน้าตาเหมือน npm script ธรรมดา)

| คำสั่ง | ผลจริง |
|---|---|
| `npm run deploy` | deploy ทับ Worker production |
| `npm run db:migrate:remote` | แก้ฐาน D1 production |
| `npm run db:dump` | ⚠ **default = `local`** (`AIM_D1_MODE ?? "local"`) — ใครรันแล้วนึกว่าสำรอง production อยู่ จะได้ไฟล์ local เกือบว่าง |

วันนี้ทั้งสองตัวแรก **ล้มเอง** เพราะเครื่องนี้ไม่มี wrangler auth และไม่มี `CLOUDFLARE_API_TOKEN` ในเชลล์ —
แต่พอไหนมี token เมื่อไร มันรันทันทีโดยไม่มีขั้นยืนยัน · **token ให้อยู่ใน env ของเซสชันเท่านั้น ห้ามเขียนลงไฟล์**

**ความลับที่เผลอ commit ได้:** `worker/wrangler.toml` (gitignore อยู่ แต่มี `account_id`/`database_id` จริง —
`git add -f` หรือย้ายชื่อไฟล์ = หลุด) · `config/*.credentials.php` ฝั่ง LIVE ถือ `ingest_key` 64 ตัวและ `notify_key` 64 hex

---

## 6. กับดักที่จ่ายค่าเรียนไปแล้ว (มองไม่เห็นจากตัวโค้ด)

- **ULID (D-P0-08):** regex เดิมบังคับ `webhookEventId` ขึ้นต้นด้วยตัวอักษร แต่ ID จริงของ LINE เป็น ULID
  ที่ **มักขึ้นต้นด้วยตัวเลข** ⇒ event จริงถูกปฏิเสธทุกตัวตั้งแต่วันแรกที่ deploy โดยไม่มีใครรู้ เพราะทั้งผู้เขียนและผู้ตรวจ
  ต่างตั้งค่าจำลองของตัวเองที่ขึ้นต้นด้วยตัวอักษรพอดี
  **บทเรียนถาวร: ค่าจำลองที่ผู้เขียนเทสคิดขึ้นเอง ย่อมผ่านกฎที่ผู้เขียนคนเดียวกันตั้ง — ต้องสุ่มให้ครอบคลุมรูปแบบจริงของโลกภายนอก**
- **ต้อง `is_scalar()` ก่อน cast `(string)` ทุกค่าที่มาจาก JSON** — เจอมาแล้ว 2 รอบ (`api/aim_notify.php` แล้วก็ `groupId`
  ใน `lib/aim_forward.php`) · เครื่องนี้ `display_errors=1` ⇒ warning หลุด path เซิร์ฟเวอร์ออกไปให้คนนอกเห็น
- **ตำแหน่ง early-return:** `if ($testGroupId === '') return;` วางก่อน loop = ฆ่า postback forwarding ทั้งหมดเงียบ ๆ
- **ข้อความไทยที่ส่งเป็น inline argument ผ่าน Bash บนเครื่อง Windows นี้จะเพี้ยน** — เขียน JSON ลงไฟล์แล้วส่งด้วย
  `curl --data-binary @file`
- **ไฟล์ `.ps1` ต้องเป็น ASCII ล้วน** (PS 5.1 อ่านไฟล์ไม่มี BOM เป็น ANSI)
- **สไตล์ PHP ฝั่ง LIVE ถูกบังคับด้วยเทส ไม่ใช่ด้วย runtime** — `bridge/test_aim_forward.ps1` ตกงานถ้าเจอ arrow fn,
  `??=`, `declare(strict_types)`, typed property, return type (PHP จริงเป็น 8.1.9 รันได้ แต่เทสไม่ให้ผ่าน — ตั้งใจให้เข้ากับ vendor-ksk)

---

## 7. ของที่ซ้ำซ้อนอยู่ตอนนี้ (ต้องระวัง — ยังไม่ได้แก้)

1. **`bridge/api_aim_notify.php` เก่ากว่าของจริงและขาดแพตช์ความปลอดภัย**
   ตรวจแล้ว: ของจริง `vendor-ksk/api/aim_notify.php` มี `is_scalar` 3 จุด · สำเนาใน `bridge/` มี **0 จุด**
   ⇒ **ก๊อป bridge → live เมื่อไร = ย้อนแพตช์ที่ code-reviewer สั่งแก้ทิ้งเงียบ ๆ**
   ซ้ำร้าย `test_aim_forward.ps1` เทสต์ **สำเนาใน `bridge/`** ไม่ใช่ไฟล์ที่รันจริง ⇒ ยามไม่ได้เฝ้าของจริง
   **ความจริงอยู่ที่ `C:\WebApp\vendor-ksk\` เสมอ · `bridge/` เป็นแค่ staging**
2. **`C:\WebApp\vendor-ksk\docs\ai-manager\` เป็นสำเนาแช่แข็งของเอกสาร 4 ฉบับ** (byte-identical) และ **ไม่มี**
   `DECISIONS_P0.md` · `WORK_PACKAGES_P1/P2.md` ⇒ ใครอ่านจากตรงนั้นจะเห็นโลกแค่ยุค P0
   **เอกสารตัวจริงอยู่ที่ `C:\WebApp\aim\docs\` เท่านั้น**
3. **เอกสารในรีโปนี้ที่ล้าสมัยจนทำให้รายงานผิดได้:**
   `README.md` เขียนว่า "ไม่มี Cron, consumer, bridge, การ deploy จริง" — **ผิดทั้ง 4 ข้อ** ·
   `HANDOFF.md` ยังบอกขอบเขต P0-1..3 / "7 tests" / อ้าง `npm run privacy:check` ที่ถูกลบไปแล้ว ·
   `bridge/README.md` ข้อ 4 เขียนว่าแชท 1:1 ไม่ถูกส่งออก — **ผิดตั้งแต่ WP-P1-B3**

---

## 8. ระบบนี้อยู่นอกสายตายามทั้งหมดของ estate

ตรวจแล้ว: ไม่มี scheduled task `ksk-*` ของ aim สักตัว · ไม่มีช่องใน `nt_registry()` · ไม่มีใน `bin/check_send_channels.php`
· `bin/selfcheck.php` ไล่รายการจาก schtasks + `nt_registry()` ⇒ **ระบบนี้มองไม่เห็นโดยโครงสร้าง ไม่ใช่เพราะลืม**
`lib/aim_notify.php` ยิง `ll_line_push_to()` ตรง ไม่ผ่านทะเบียนแจ้งเตือน ⇒ ไม่มี holiday gate ไม่มีสวิตช์ปิด

ผลตอนนี้: `aim_forward` สำเร็จครั้งสุดท้าย 2026-08-27 16:05 — **ไม่มีใครบอกได้ว่านั่นแปลว่า "กลุ่มทดสอบเงียบ"
หรือ "ท่อขาด"** เพราะไม่มีอะไรวัด · นี่คือบทเรียน "สัญญาที่ไม่มีตัวบังคับใช้ = ไม่มีสัญญา" (D-004) กำลังเล่นซ้ำในระบบใหม่
เช่นเดียวกับ `IMPLEMENTATION_PLAN.md` §5 ข้อ 2 ที่สั่งให้ **dump กลับบ้านทุกวัน** — ยังไม่มี task ไหนทำเลย
และ dump ที่มีอยู่ใบเดียว (`dumps/aim-db-2026-08-27T01-50-04.743Z.sql`) เก่ากว่า migration `0002`

---

## 9. เอกสารตัวจริงในรีโปนี้

| ไฟล์ | ใช้ตอนไหน |
|---|---|
| `docs/DECISIONS_P0.md` | **อ่านก่อนเสมอ** — 13 คำตัดสิน (D-P0-01..13) ที่ห้ามรื้อซ้ำ |
| `docs/IMPLEMENTATION_PLAN.md` | สถาปัตยกรรม 4 ชั้น · เฟส P0–P5 · กฎกันติดกับดักของฟรี |
| `docs/WORK_PACKAGES_P0/P1/P2.md` | ใบสั่งงานรายเฟส + เกณฑ์ผ่านของแต่ละประตู |
| `docs/ARCHITECTURE.md` · `DATABASE_SCHEMA.md` | โครงระบบ + สคีมา |

**เจอข้อความที่ขัดกันระหว่างเอกสารเก่ากับของจริงบนดิสก์ — ของจริงชนะเสมอ แล้วแก้เอกสารให้ตรง อย่าปล่อยไว้**
