# aim — AI Manager (กติกาถาวร · เปิดโฟลเดอร์นี้ต้องอ่านก่อนแตะอะไร)

อัปเดต **2026-09-04** ที่ HEAD `252be4f` · รีโปนี้คือ **บ้านใหม่ของ AI Manager** (Cloudflare Worker + D1)
ที่แยกออกมาจาก `C:\WebApp\vendor-ksk`

> ## 📍 ฝั่ง LIVE มีบ้านบน GitHub แล้ว — `mottosolar-gif/KSK-Decepticons`
> `C:\WebApp\vendor-ksk` **ไม่ได้อยู่แค่ในเครื่องที่บ้าน** — มันคือ repo `KSK-Decepticons` บน GitHub
> (`bin/aim_reconcile.php` · `lib/aim_forward.php` · `api/line_webhook.php` อยู่ในนั้นครบ)
> ⇒ **เซสชันไหนก็อ่านโค้ดฝั่ง LIVE ได้ ไม่ต้องรอเปิดเครื่องที่บ้าน**
> ⚠ **ข้อนี้เขียนไว้ตั้งแต่ 2026-08-27 แล้ว** ที่ `IMPLEMENTATION_PLAN.md` **§1** ตาราง "ของฟรีที่ estate
> เป็นเจ้าของอยู่แล้ว (VERIFIED)" — บรรทัด `GitHub | repo จริง mottosolar-gif/KSK-Decepticons`
> 2026-09-04 ยังมีเซสชันเขียนใบสั่งงานให้ของที่ทำเสร็จแล้ว เพราะ **เดาว่าแตะฝั่งโน้นไม่ได้ โดยไม่เคยอ่าน §1**
> **สาเหตุเชิงโครงสร้าง:** §9 ของไฟล์นี้ชี้ `IMPLEMENTATION_PLAN.md` ไว้แค่ **"§6"** ⇒ ทุกคนอ่านแต่ลำดับเฟส
> **§1–§5 (คำสั่งที่ยึด · สถาปัตยกรรม · ท่ออ่าน · กฎข้อมูลคน · กฎกันติดกับดักของฟรี) เลยไม่มีใครเปิด**
> ⇒ แก้ที่ §9 แล้ว · และ **ก่อนบอกว่า repo ไหนเข้าไม่ถึง ให้เรียก `list_repos` ดูก่อนเสมอ**
> ⚠ แต่ **GitHub ไม่ใช่ความจริงล่าสุดเสมอ** — ของที่ commit ในเครื่องแล้วยังไม่ push จะมองไม่เห็นจากที่นี่
> (ตัวอย่างจริง: `docs/DATA_POLICY.md` ถูกอ้างถึงจากฝั่งโน้น แต่ยังไม่มีในรีโปนี้)

> **กติกาแม่ของ estate อยู่ที่ `C:\WebApp\vendor-ksk\CLAUDE.md` — ไฟล์นี้ไม่คัดลอกมา ตั้งใจ**
> บ้านนี้จ่ายบทเรียนเรื่อง "สำเนา = ความจริงสองชุด" มาแล้วหลายรอบ · ไฟล์นี้เก็บเฉพาะกติกาที่ผูกกับรีโปนี้
> และ **ชี้** ไปหาของจริงเสมอ ถ้าเจอที่ไหนเขียนกติกาเดียวกันซ้ำ = อันนั้นผิด ให้ลบแล้วชี้มาที่ต้นทาง
>
> **บทบาทของไฟล์นี้** (พี่เต้เคาะ 2026-08-29 · commit `1dfaa07`): `README.md` ตอบว่า *ระบบคืออะไร* ·
> **ไฟล์นี้ตอบว่า *ตอนนี้สถานะเป็นยังไง*** ⇒ ตัวเลขในนี้เก่าเมื่อไร ไฟล์นี้ก็ทำหน้าที่ตัวเองไม่ได้ทันที

## 🔍 ป้ายบอกที่มาของทุกข้อเท็จจริงในไฟล์นี้ (อ่านก่อน แล้วจะไม่หลงเชื่อผิดตัว)

| ป้าย | แปลว่า |
|---|---|
| ✅ | **เซสชันที่เขียนบรรทัดนี้รันเอง/เปิดไฟล์ดูเอง** บนรีโปนี้ที่ HEAD ที่ระบุ |
| 📄 | เชื่อตามใบตัดสิน/เอกสาร — **คนอื่นวัด เซสชันนี้ไม่ได้เห็นของจริงเอง** |
| ❓ | **ยังไม่มีใครยืนยัน** หรืออยู่นอกมือของเซสชันที่เขียน |

⚠ รอบอัปเดต 2026-09-04 นี้ทำจาก **เซสชันคลาวด์ (Linux)** ⇒ ทำอะไรได้/ไม่ได้:
- ✅ **อ่านโค้ดฝั่ง LIVE ได้** ผ่าน `KSK-Decepticons` (ดูกล่องข้างบน) — ข้อที่ตรวจจากซอร์สจริงติดป้าย ✅ ได้
- ❌ **รันเทสฝั่ง LIVE ไม่ได้** — ไม่มี MySQL ในเครื่อง ⇒ เลข `smoke_*.php` ทุกตัวเป็น 📄 (ลอกจาก
  `KSK-Decepticons/CLAUDE.md`) **ไม่ใช่เลขที่รันเอง**
- ❌ **ยิง production ไม่ได้** — `curl https://aim-ingest.mottosolar.workers.dev/healthz` ตอบ
  `CONNECT tunnel failed 403` (network policy บล็อก ไม่ใช่ Worker ล่ม) ✅ยืนยันแล้วว่าบล็อกจริง
  · `cursor.com` / `docs.cursor.com` ก็บล็อกเหมือนกัน
- ⇒ **ห้ามสรุปจากไฟล์นี้ว่า production ปกติดี** ใครเปิดเครื่องที่บ้านได้ ให้ยิง `/healthz` เองเสมอ

---

## ⛔ ท่าที่ทำแล้วกู้ไม่ได้ — อ่านก่อนพิมพ์คำสั่ง git ใด ๆ

1. **✅ รีโปนี้มี remote แล้ว** (`origin` = `github.com/mottosolar-gif/mottosolar-gif-aimanager` · branch `main`)
   ⇒ อันตรายเดิม ("ประวัติอยู่บนดิสก์เดียว") **หมดไปแล้ว** แต่มีอันตรายใหม่มาแทน 2 ข้อ:
   - **commit แล้ว ≠ push แล้ว** — ของที่ commit ค้างในเครื่องยังหายไปกับเครื่องได้ เช็ค `git status` ว่า ahead กี่ commit
   - **ของที่ยัง untracked ยังโดน `git clean -fd` กินได้เสมอ** — เช็ค `git status` ก่อนทุกครั้ง
   **บทเรียนเดิมที่ยังต้องจำ:** ก่อน `44fa7cd` เคยมีเทส 146 ข้อเขียวอยู่ แต่ 94 ข้ออยู่ในไฟล์ที่ยัง untracked
   — เช็คเอาต์ HEAD สะอาด ๆ จะเหลือ 52 · **เลขเทสที่ยังไม่ commit คือเลขที่ไม่มีอยู่ในรีโป ห้ามเอาไปรายงานว่าผ่านประตู**
2. **📄 `git checkout -- api/line_webhook.php` ที่ `C:\WebApp\vendor-ksk` = ถอดปลั๊กสะพานที่รันอยู่จริง เงียบ ๆ**
   ตัวเรียก `aim_forward` **ยังไม่ถูก commit** ฝั่งโน้น (D-P0-14 หนี้ข้อ 2 · ปนกับงานค้างของอีกเซสชัน)
   ⇒ revert เมื่อไร โค้ด **ยังดูเหมือนต่ออยู่** แต่ log เงียบไปเฉย ๆ ไม่มีใครรู้
   ❓ **ยังไม่มีใครยืนยันว่าเรื่องนี้ถูกแก้แล้วหรือยัง** — เป็นหนี้ที่ค้างมาตั้งแต่ 2026-08-28

---

## 1. บทบาท — เปิดโฟลเดอร์นี้แล้ว "ห้ามลงมือเขียนโค้ดเอง"

| ใคร | ทำอะไร |
|---|---|
| **Claude (ตัวที่กำลังอ่านอยู่นี้)** | สถาปัตยกรรม · เขียนใบสั่งงาน · **สั่ง Codex ลงมือ** · **ตรวจงาน** · ตัดสินเมื่อชนกติกาบ้าน |
| **Codex** (`codex exec --sandbox workspace-write`) | เขียนโค้ด Worker/เทส/migration ทั้งหมด · **ห้ามตรวจงานตัวเอง** · หยิบงานจาก `docs/QUEUE.md` |
| **code-reviewer อิสระ** | ด่านสุดท้ายก่อนถือว่าปิดงาน |

ที่มา: `docs/IMPLEMENTATION_PLAN.md` §7 · ทางเข้าของ Codex คือ `AGENTS.md` → `docs/QUEUE.md`
**ปกติ Claude เป็นคนสั่ง Codex เอง** ไม่ใช่ส่งใบสั่งงานกลับไปให้เจ้าของระบบเดินเรื่องต่อ

⚠ **เซสชันคลาวด์ (Claude Code on the web) สั่ง Codex CLI ไม่ได้** ✅ตรวจแล้ว: ไม่มี `codex` ใน PATH
⇒ ทำได้แค่ **เขียนใบสั่งงาน** คนที่เปิดเครื่องที่บ้านเป็นคนสั่ง Codex
**แต่ "อ่านโค้ดฝั่ง LIVE ไม่ได้" นั้นไม่จริง** — อ่านได้ผ่าน `KSK-Decepticons` (ดูกล่องบนสุด)
⇒ **เขียนใบสั่งงานที่แตะฝั่ง LIVE ต้องเปิดไฟล์จริงดูก่อนเสมอ ห้ามเขียนจากที่เดา**
บทเรียนจริง 2026-09-04: เขียนใบสั่ง WP-P4-B ทั้งใบโดยไม่เปิดดู แล้วพบว่าของทำเสร็จไปแล้ว
⚠ สัญชาตญาณแรกของเซสชันใหม่คือเปิด `worker/src/*.ts` แล้วแก้เลย — **นั่นคือการผิดกฎก่อนพิมพ์เทสบรรทัดแรก**
**ใบสั่งงานทุกใบต้องครบ 6 ช่อง:** เป้าหมาย · ไฟล์ที่แตะได้/ห้ามแตะ · สัญญา · เกณฑ์ผ่าน · ยามที่ต้องยังเขียว · เจออะไรให้หยุดถาม

**Codex จะหยุดขออนุมัติแผนก่อนลงมือเสมอ** — ถ้าใบสั่งงานผ่านการตัดสินมาแล้ว ให้สั่งซ้ำโดยเขียนว่า
"แผนนี้อนุมัติแล้ว ลงมือได้เลย ไม่ต้องถามซ้ำ" ไม่งั้นจะได้แผนกลับมาแทนโค้ด

---

## 2. กติกาที่ห้ามพัง (ทุกข้อจ่ายบทเรียนมาแล้ว)

1. **ไม่มีอะไรถึงผู้ใช้จริงโดยไม่มีคำอนุมัติของตัวเอง** — "อนุมัติดีไซน์ ≠ อนุมัติปล่อยจริง"
   พี่เต้พูดว่า "ไล่ไปเลย P2-5" **ไม่ใช่** ใบเบิกทางให้แตะ LIVE · ทุกใบที่แตะ LIVE ขออนุมัติแยกใบของมันเองทุกครั้ง
   ✅ กติกานี้ถูกฝังเข้า CI แล้ว: deploy ต้องสั่งด้วยมือ (`workflow_dispatch` + พิมพ์คำว่า `deploy` ยืนยัน) ดู §4
2. **ข้อมูลคนห้ามออกจากบ้าน** (`docs/IMPLEMENTATION_PLAN.md` §4 · D-P0-01)
   คลาวด์เก็บได้แค่ **รหัสแทนตัว + แผนก + บทบาท** · ชื่อจริงอยู่ที่ cim-server เท่านั้น ·
   เหตุผลการลาไม่ออกจากบ้าน · `body_ref` ต้องเป็น NULL เสมอ · แปลงรหัสกลับเป็นชื่อได้ที่ชั้น ②/③ เท่านั้น
   ⚠ **ตัวกันอัตโนมัติถูกถอดไปแล้ว** (D-P0-06 พี่เต้ตัดสินเอง) ⇒ **migration ใหม่ทุกใบต้องไล่ดูคอลัมน์ด้วยตา ทุกครั้ง**
   ⚠ `lib/aim_forward.php` ส่ง **event ดิบทั้งก้อน** (ข้อความ · `userId` · `replyToken`) ออกจากเครื่อง —
   สิ่งเดียวที่ปอกทิ้งคือ `extractMetadata()` ใน `worker/src/payload.ts` ⇒ **แก้ไฟล์นั้นหลวมเมื่อไร ข้อมูลส่วนบุคคลลง D1 ทันทีโดยไม่มีเสียงเตือน**
3. **`canView()` ใน `worker/src/visibility.ts` เป็นจุดตัดสินสิทธิ์จุดเดียว**
   ห้ามเขียนเงื่อนไข role/department ซ้ำใน SQL หรือใน engine · `worker/src/taskMachine.ts` เป็นจุดเดียว
   ที่ตัดสิน ownership ของการเปลี่ยนสถานะงาน
4. **น้ำเสียง "น้องกุ้ง" อยู่ในซอร์สแล้ว ไม่ใช่แค่ในเอกสาร** — `queryEngine.ts` มีข้อความไทยที่ผู้ใช้อ่านจริง
   แทนตัวเองว่า **น้องกุ้ง** · ลงท้าย **ค่ะ/คะ** · เพิ่ม intent ที่ 21 แล้วเผลอเขียนอังกฤษ/"ผมครับ" = เสียงแตกเป็นสองคน
   **ขอบเขตที่ห้ามข้าม: ความน่ารักห้ามทำให้ข่าวร้ายอ่อนลง** — เทสตกต้องบอกว่าตก ตัวเลขต้องเป๊ะ
5. **ห้ามเขียนลงฐานของ cim-server** — ฝั่งนั้นอ่านอย่างเดียวตามคำสั่งพี่เต้
   ⇒ ต่อยอดเป็น D-P0-17: **`tbl_task` ฝั่ง LIVE คือเจ้าของความจริงของ "งาน" · คลาวด์เป็นสำเนาอ่านอย่างเดียว**
   `POST /sync/tasks` เป็น **ทางเดียว** LIVE → คลาวด์ · ห้ามสร้าง endpoint เขียนกลับเด็ดขาด
6. **ห้ามยิง LINE หาคนจริงระหว่างทดสอบ** — ใช้ `--dry`/`--test` เสมอ (`--test` = ถึงพี่เต้คนเดียว ไม่จดลงฐาน)
   🟠 **ข้อ "ในกลุ่มบอทไม่ตอบ" ไม่จริงแล้ว** — ฝั่ง LIVE มี `aim_ask_handle_group()` เป็น **ข้อยกเว้นเดียว**
   ที่ให้บอทตอบในกลุ่มได้ (📄 `KSK-Decepticons/CLAUDE.md:319`) ⇒ ถ้อยคำเดิมของกติกาข้อนี้ต้องให้พี่เต้เคาะใหม่
   📄 เคยพลาดมาแล้ว 2 ครั้งในวันเดียว (`QUEUE.md` ข้อห้าม 4): `task_escalate` ไม่ใส่ `--test` ⇒ ถึงพนักงานจริง 2 คน
   · `smoke_line_register` ยิงแจ้งเตือนความปลอดภัยปลอมถึงพี่เต้
7. **ห้ามผูก vendor เข้ากับ LINE เด็ดขาด** — vendor 41 บริษัทยืนยันตัวตนด้วยอีเมล+bcrypt เท่านั้น
   งาน AI Manager ทั้งหมดเป็นพื้นที่ของ **พนักงานภายใน** (กติกาแม่ฝั่ง vendor-ksk)
8. **กฎกันติดกับดักของฟรี** (`IMPLEMENTATION_PLAN.md` §5) — SQL มาตรฐานเท่านั้น (ห้ามให้ Durable Objects/Queues
   เป็นกระดูกสันหลังจนย้ายบ้านไม่ได้) · ตรรกะธุรกิจห้ามอยู่ใน SaaS · **ห้ามผูกบัตรกับบริการคิดเงินตามการใช้ที่ไม่มีเพดาน**
9. **ห้ามแตะ Worker `mottosolar-line-relay`** (D-P0-02) — เป็นของ homelab รัน 22,370 ครั้ง/24 ชม.
   deploy ทับ = ระบบนั้นตายทันที · Worker ของเราชื่อ **`aim-ingest`** เท่านั้น
   โควตาบัญชี Cloudflare ฟรี = 100,000 invocation/วัน **ต่อทั้งบัญชี** (relay ใช้ไปแล้ว ~22%) ⇒ เพิ่ม cron/polling = ใช้โควตาของคนอื่น
10. **โควตา LINE push = 15,000/เดือน** · 📄 วัดจริง 2026-08-29 = **36.8 push/วัน ⇒ ~1,103/เดือน**
    (`WORK_PACKAGES_P4.md` · จาก `bin/selfcheck.php`) — ใช้ไป ~7% ของเพดาน **แต่นี่คือตอนที่ยังมีคนใช้จริง 2 คน**
    ⚠ ตัวเลขนี้เป็นส่วนหนึ่งของ **ประตู P4** ("โควตา push ต่อเดือนไม่เกินที่มี") ⇒ ขยายคนเมื่อไรต้องวัดใหม่ทันที
    ⚠ เอกสารเก่าที่อื่นในเครือยังเขียนเลขผิด (`vendor-ksk/CLAUDE.md` · `TEAM_TOOLS_INVENTORY.md` §A9)

---

## 3. go live คืออะไร แล้วตอนนี้อยู่ตรงไหน

**"go live" ในแผนนี้ = P5** (`ย้ายของจริง แล้วค่อยตัดสาย CIM`) ไม่ใช่ "deploy ขึ้น Cloudflare"
— คลาวด์ขึ้น production ไปตั้งแต่ 2026-08-27 แล้ว และมีคนใช้จริงอยู่ทุกวัน **ระหว่างที่ของเก่ายังเดินคู่กันอยู่**

```
P2 ปิดแล้ว ──► P3 ? ──► P4 กำลังนับเวลา ──► P5 = go live (ยังเริ่มไม่ได้)
                          ▲ ประตู: ตรงกัน 100% ติดกัน 7 วัน + รันคู่ ≥ 2 สัปดาห์ + โควตา push ไม่เกิน
```

| เฟส | สถานะ | หลักฐาน / ค้างอะไร |
|---|---|---|
| **P0** ท่อ+กระดูก | เดินจริง · **ไม่มีใบปิดอย่างเป็นทางการ** | 📄 D-P0-04 · เกณฑ์ WP-P0-6 ข้อ 4 (ปิด cim-server แล้วคลาวด์ยังเดิน) ❓ยังไม่เคยทดสอบ |
| **P1** ประสาทสัมผัส | **ปิดจบ 100%** | 📄 D-P0-12 — พิสูจน์วงจรเต็มด้วยปุ่มจริงบนมือถือ ไม่ใช่ synthetic event |
| **P2** สายตา+ปาก | **ปิดจบ (2026-08-29)** | 📄 D-P0-18 — 20/20 คำถามยิงกับ production จริง + สิทธิ์การมองเห็นทดสอบด้วยตัวตนคนที่สอง |
| **P3** ความคิดริเริ่ม | 🟠 **ไม่มีใบปิดในรีโปนี้ แต่ของมันเดินอยู่จริง** | ✅ ตรวจแล้วว่ามีจริงใน `KSK-Decepticons`: `lib/escalation.php` · `lib/ai_policy.php` (`aip_decide()`) · `bin/task_escalate.php` · `bin/smoke_escalation.php` — **แต่ `DECISIONS_P0.md` ยังไม่มี D-P0-xx ไหนบันทึกว่าประตู P3 ผ่าน** ❓ |
| **P4** ทดสอบเต็มระบบ | **กำลังนับเวลา — เริ่ม 2026-08-29** · ✅ **ใบงานเสร็จหมดแล้ว** | WP-P4-A ✅ (`sql/053_task_photo_meta.sql`) · WP-P4-B ✅ (`--streak` + ตัวแจ้งประตูอัตโนมัติ) · WP-P4-C 🟠 (มีในเครื่อง ยังไม่ push) ⇒ **เหลือแค่รอเวลา** กับบั๊ก `--streak` ที่ยังไม่แปะแพตช์ |
| **P5 = go live** | **ยังเริ่มไม่ได้** | 📄 `WORK_PACKAGES_P5.md` เขียนใบไว้ล่วงหน้าแล้ว ห้ามลงมือก่อน P4 ผ่านประตู |

### ⛔ 3 อย่างที่ขวาง go live อยู่ตอนนี้ (ไม่ใช่เรื่องโค้ดสักข้อ)

1. **ตัวนับสตรีคมีแล้ว แต่ตอบผิดถ้ากดกลางวัน** ✅ตรวจซอร์สจริง — `rec_streak_days()` เดินถอยหลัง**จากวันนี้**
   แต่ `ksk-aim-reconcile` รัน **23:30** ⇒ กด `--streak` ก่อนสี่ทุ่มครึ่ง **ได้ 0 เสมอ** ทั้งที่สตรีคจริงอาจเดินมาหลายวัน
   ⇒ **ยังไม่มีใครตอบได้ว่าสตรีคถึงกี่วันแล้ว ถ้าไม่รอถึงสี่ทุ่มครึ่ง หรือเปิด `tbl_notify_log` ดูเอง**
   · เส้นทางแจ้งอัตโนมัติไม่กระทบ (บันทึกก่อนนับ) ⇒ **P5 ไม่มีทางถูกพลาด**
   · แพตช์พร้อมแล้ว ยังไม่แปะ — ดู `WORK_PACKAGES_P4.md` §WP-P4-B
   ⚠ บทเรียน: ยามเดิม 6 เคส **seed "วันนี้" ทุกเคส** ⇒ มองไม่เห็นสภาพที่คนเจอบ่อยที่สุด
   — **ตัววัดที่ตอบผิด น่ากลัวกว่าไม่มีตัววัด** (D-P0-16 เล่นซ้ำอีกรอบ)
2. **📄 บอทเข้ากลุ่มงานจริงไม่ได้** — 2026-08-29 06:02 เชิญ OA เข้ากลุ่มจริง (16 คน) แล้ว log ขึ้น `join` + `leave`
   ในวินาทีเดียวกัน · ไม่ใช่บั๊กโค้ด แต่เป็นสวิตช์ *"อนุญาตให้เพิ่มบัญชีเข้ากลุ่ม"* ใน LINE OA Manager
   ที่ **อ่าน/แก้ผ่าน API ไม่ได้ ต้องพี่เต้กดเอง** (D-P0-18) ❓ยังไม่มีใครยืนยันว่ากดแล้วหรือยัง
3. **❓ P3 ไม่มีใบปิด** — แผนสั่งไว้ว่า *"ไล่ตามลำดับจริง ไม่ข้ามประตู"* (D-P0-18 เพิ่งจ่ายบทเรียนข้อนี้มาเอง)
   ⇒ ก่อนประกาศ P4 ผ่าน ต้องเคลียร์ก่อนว่า P3 ผ่านประตูของมันจริงหรือแค่ "ของมันเดินอยู่"

### ✅ ของจริงที่รันอยู่ตอนนี้ (ตรวจจากซอร์สที่ HEAD `252be4f`)

Worker `aim-ingest` (`https://aim-ingest.mottosolar.workers.dev`) · ฐาน D1 `aim-db` · Cron ทุก 1 นาที ·
migration **3 ใบ** (`0001_initial` · `0002_task` · `0003_postback`) · **6 endpoint** ใน `worker/src/index.ts`:

| Path | กุญแจ | หมายเหตุ |
|---|---|---|
| `GET /healthz` | ไม่ต้อง | คิวค้าง · งานที่ตายถาวร · event ค้างเกิน 15 นาที |
| `POST /ingest/line` | `X-AIM-Key` | รับ webhook envelope แบบ idempotent |
| `POST /ask` | `X-AIM-Ask-Key` | ถาม-ตอบ rule-based ผ่าน `canView()` เสมอ · **20 intent** ✅นับจากทะเบียนจริง |
| `POST /link` | `X-AIM-Link-Key` | ผูก LINE id (แฮชแล้ว) เข้ากับ `person_code` — ตัวปลดล็อกให้เกิน 2 คน |
| `POST /sync/tasks` | `X-AIM-Sync-Key` | สำเนางานจาก LIVE **ทางเดียว** (D-P0-17) |
| `POST /admin/summary` | `X-AIM-Admin-Key` | สั่งยิงสรุป 1 รอบ หรือ `preview` ดูก่อนส่ง |

**กุญแจแยกดวงต่อหน้าที่โดยเจตนา** — ไม่ตั้ง secret = เส้นทางนั้นปิด (`503`) · กุญแจผิดตอบ `403` เหมือนกันทุกตัวอักษร
📄 Version ที่ deploy ล่าสุดที่มีบันทึก: `4fd4f31e-f98c-456f-9954-4fd873e547a5` (D-P0-15, 2026-08-28)
❓ **เซสชันคลาวด์ยืนยันไม่ได้ว่า production วันนี้ยังเป็นตัวนี้อยู่** — proxy บล็อก (ดูหัวข้อป้ายบอกที่มาด้านบน)

### หนี้เก่าที่ **ปิดแล้ว** (เลิกรายงานว่าค้างได้แล้ว)

| เคยค้าง | ปิดด้วยอะไร |
|---|---|
| `visibility.ts` เช็ค `=== ""` แทน falsy (D-P0-13) | ✅ ไม่เหลือ `=== ""` ในไฟล์แล้ว |
| `eslint.config.mjs` ไม่รวม `vitest.config.ts` (D-P0-13) | ✅ อยู่ใน `files` แล้ว |
| ไม่มีโค้ดเขียน `person_link` ⇒ คนที่ 3 ติด 403 ตลอดกาล (D-P0-14 หนี้ 1) | ✅ `POST /link` (commit `2a04df1`) |
| ไม่มี dump กลับบ้านรายวัน (กฎ §5 ข้อ 2) | 📄 commit `4c9378d` |
| `README.md` เขียนสถานะผิด 4 ข้อ · `bridge/README.md` ข้อ 4 ผิด | ✅ แก้แล้วทั้งคู่ (commit `32ad837`) |
| ปุ่มกดแล้วสถานะถูกซิงก์ทับหายภายใน 10 นาที (D-P0-17) | 📄 commit `d283388` + D-P0-18 ยืนยัน "วงจรการ์ดครบ" |
| เกณฑ์ WP-P0-6 ข้อ 6 อ้างสคริปต์ที่ถูกลบไปแล้ว | 📄 D-P0-16 แก้ถ้อยคำแล้ว |

### ✅ ข้อที่เคยขัดกันเอง — ปิดแล้ว (2026-09-04)

`bin/smoke_aim_forward.php` **มีอยู่จริง** ✅ (เห็นกับตาใน `KSK-Decepticons/bin/`) ⇒ เงื่อนไขปิด WP-P0-5
ตาม D-P0-07 M2 **ครบแล้ว** · ฉบับก่อนเขียนขัดกันเองเพราะไม่มีใครเปิดฝั่งโน้นดู ตอบไม่ได้อยู่หลายรอบ

### 🟠 ของฝั่ง LIVE ที่โตไปแล้วแต่เอกสารฝั่งนี้ยังไม่รู้จัก (ตรวจ 2026-09-04)

`KSK-Decepticons/CLAUDE.md` มีของที่ไฟล์นี้ไม่เคยพูดถึงเลย — **ไม่ใช่งานค้าง แต่เป็นจุดบอดของเอกสารฝั่งนี้**:
- `lib/aim_mail_cmd.php` + `bin/aim_mail_cmd_ingest.php` — สั่งงาน AI Manager **ทางอีเมล** (ยาม 40 ข้อ)
- `lib/kb_match.php` — กฎจับคู่ความรู้/คลังคำตอบ (ยาม 49 ข้อ) · `lib/fim_qa.php` (43 ข้อ)
- `aim_ask_handle_group()` — **บอทตอบในกลุ่มได้แล้ว** ในฐานะ *ข้อยกเว้นเดียว* ของกฎ "ในกลุ่มบอทไม่ตอบ"
  ⚠ ข้อนี้กระทบกติกาข้อ 6 ของไฟล์นี้โดยตรง — **ต้องให้พี่เต้เคาะว่าถ้อยคำใหม่คืออะไร**
- `smoke_aim_ask` โตจาก **41 → 59 ข้อ** ⇒ เลขในไฟล์นี้/`QUEUE.md` เคยล้าหลังไป 18 ข้อ

---

## 4. ประตูที่ห้ามข้าม (รันเองทุกครั้ง ห้ามเชื่อรายงานของผู้เขียนโค้ด)

```
npm ci                     # clone ใหม่ต้องรันก่อน ไม่งั้นทุกคำสั่งข้างล่างล้ม
npm run typecheck          # tsc --noEmit
npm test                   # vitest — ต้องเขียวทั้งหมด
npm run lint               # eslint (no-floating-promises / no-misused-promises)
npm run db:migration:check # migration รันซ้ำได้ + body_ref ยัง NULL-only
```

✅ **รันจริงเมื่อ 2026-09-03 บน HEAD `252be4f` (Linux · Node 22) — ผ่านครบ 4/4:**

| ด่าน | ผล |
|---|---|
| typecheck | สะอาด (exit 0) |
| test | **284 ผ่าน / 7 ไฟล์** — queryEngine 181 · worker 41 · summary 25 · taskMachine 18 · visibility 11 · queueTaskAction 6 · outbound 2 |
| lint | สะอาด (exit 0) |
| migration check | ผ่าน 2 รอบใน memory · 3 ไฟล์ · `body_ref` ยัง NULL-only |

**ตัวเลขนี้เป็นภาพนิ่ง ห้ามอ้างต่อ — รันเองทุกครั้ง** · **จำนวนเทสห้ามลดลง** (ฝั่ง LIVE มีเลขของมันใน `docs/QUEUE.md`)

### ✅ CI ไม่ใช่ความอุ่นใจปลอมอีกแล้ว — แต่ต้องรู้ว่ามันทำอะไร

เดิมไฟล์นี้เขียนว่า CI "ไม่เคยรันเลยสักครั้ง" (ยิงที่ `main` แต่ branch จริงชื่อ `master` และไม่มี remote)
**ทั้งสองข้อไม่จริงแล้ว** ✅ ตรวจแล้วที่ `.github/workflows/deploy.yml`:

- **push เข้า `main` = รันเทสอย่างเดียว** — `npm ci` → `typecheck` → `test` → **`lint`** → `db:migration:check`
  (กฎ `no-floating-promises` ที่กัน `canView()` ลืม `await` **มีคนบังคับใช้แล้ว** ต่างจากเดิม)
- **deploy ต้องสั่งด้วยมือเท่านั้น** — `workflow_dispatch` + ต้องพิมพ์คำว่า `deploy` ยืนยัน + ต้องอยู่บน `main`
  (พี่เต้เคาะ 2026-08-28 เพราะของเดิม push แล้วขึ้น production เอง = ขัดกติกาข้อ 1)

**เทสต้องยิงของจริง:** `visibility`/`queryEngine`/`summary` รันบน `node:sqlite` จริงทับ migration จริง
(`worker/test/helpers/`) — mock ที่จับคู่สตริง SQL พิสูจน์ได้แค่ว่า mock ทำงาน **ห้ามถอยกลับไปใช้**

---

## 5. คำสั่งที่ยิงโดนของจริงทันที (หน้าตาเหมือน npm script ธรรมดา)

| คำสั่ง | ผลจริง |
|---|---|
| `npm run deploy` | deploy ทับ Worker production |
| `npm run db:migrate:remote` | แก้ฐาน D1 production |
| `npm run cf:config` | สร้าง `worker/wrangler.toml` จาก env (ไฟล์นี้ถือ `account_id`/`database_id` จริง) |
| `npm run db:dump` | ⚠ **default = `local`** (`AIM_D1_MODE ?? "local"`) — ใครรันแล้วนึกว่าสำรอง production อยู่ จะได้ไฟล์ local เกือบว่าง · ของจริงต้อง `-- --remote` |

สองตัวแรกจะ **ล้มเองถ้าไม่มี `CLOUDFLARE_API_TOKEN` ในเชลล์** — แต่พอมี token เมื่อไร
**มันรันทันทีโดยไม่มีขั้นยืนยัน** · token ให้อยู่ใน env ของเซสชันเท่านั้น **ห้ามเขียนลงไฟล์**

**ความลับที่เผลอ commit ได้:** `worker/wrangler.toml` (gitignore อยู่ แต่ `git add -f`/เปลี่ยนชื่อไฟล์ = หลุด) ·
`config/*.credentials.php` ฝั่ง LIVE ถือ `ingest_key` 64 ตัวและ `notify_key` 64 hex

---

## 6. กับดักที่จ่ายค่าเรียนไปแล้ว (มองไม่เห็นจากตัวโค้ด)

- **ยามที่ตกไม่ได้ ไม่ใช่ยาม** — บทเรียนที่แพงที่สุดของโปรเจกต์นี้ จ่ายซ้ำ **3 ครั้ง** (D-P0-14 · D-P0-15)
  รอบหนึ่งผู้ตรวจ **ลบ scope ออกจากคิวรีตัวจริง** แล้ว 227 เทสยังเขียวหมด เพราะ fixture ให้คนที่ต้องมองไม่เห็น
  ถือคนละ 1 งาน ส่วนผู้ถามถือ 7 ⇒ คนที่ซ่อนอยู่ชนะอันดับไม่ได้เลยโดยโครงสร้าง
  ⇒ **ทุกครั้งที่เขียนยาม ต้องทำให้มันตกจริงก่อน แล้วค่อยคืนค่า** · รายงานต้องบอกว่า "sabotage อะไร → ตกกี่ข้อ → คืน → ผ่านเท่าไร"
  ⚠ **"จำนวนเทสเพิ่มขึ้น" ไม่ใช่หลักฐานว่าเทสมีฟัน** — assertion ที่เพิ่มในเคสเดิมไม่ทำให้ตัวเลขขยับเลย
  ⚠ ตัว sabotage เองก็ต้องยืนยันว่าแทนที่สำเร็จก่อน — เคยพลาด: `sed` ไม่ตรงช่องว่าง แล้วรายงานว่าเขียว ทั้งที่ไม่ได้แก้อะไรเลย
- **แก้ที่ "ชนิดของบั๊ก" ไม่ใช่ที่จุดที่ถูกชี้** — เคยแก้ `stats_*` 4 ตัวที่ถูกชี้ แล้วปล่อย `team_*` 4 ตัวที่ผิดแบบเดียวกันเป๊ะ
- **ULID (D-P0-08):** regex เดิมบังคับ `webhookEventId` ขึ้นต้นด้วยตัวอักษร แต่ ID จริงของ LINE เป็น ULID ที่
  **มักขึ้นต้นด้วยตัวเลข** ⇒ event จริงถูกปฏิเสธทุกตัวตั้งแต่วันแรกที่ deploy โดยไม่มีใครรู้
  **ค่าจำลองที่ผู้เขียนเทสคิดขึ้นเอง ย่อมผ่านกฎที่ผู้เขียนคนเดียวกันตั้ง — ต้องสุ่มให้ครอบคลุมรูปแบบจริงของโลกภายนอก**
- **ตัววัดที่ตอบผิด น่ากลัวกว่าไม่มีตัววัด** (D-P0-16) — จ่ายบทเรียนนี้ 2 ครั้งในวันเดียว:
  `LIKE 'C%'` ใน SQLite ไม่แยกตัวพิมพ์ ⇒ จับ hash ที่ขึ้นต้น `c` มาเป็นของดิบ · และเทียบ `datetime('now','-20 minutes')`
  กับ `occurred_at` ที่เป็น ISO มี `T`/`Z` ⇒ นับ cron ได้ 819 นาทีใน 20 นาที (ต้องใช้ `substr(occurred_at,1,16)`)
- **ค่าที่อ่านไม่ได้ให้เป็น NULL ห้ามเดา** · **ไม่รู้จัก = ปฏิเสธ ไม่ใช่ปล่อยผ่าน** (fail-closed ทุกด่านใหม่)
- **ต้อง `is_scalar()` ก่อน cast `(string)` ทุกค่าที่มาจาก JSON** — เจอมาแล้ว 2 รอบ · ฝั่ง LIVE `display_errors=1`
  ⇒ warning หลุด path เซิร์ฟเวอร์ออกไปให้คนนอกเห็น
- **ตำแหน่ง early-return:** `if ($testGroupId === '') return;` วางก่อน loop = ฆ่า postback forwarding ทั้งหมดเงียบ ๆ
- **สไตล์ PHP ฝั่ง LIVE ต้องเข้ากับ vendor-ksk** — ห้าม arrow fn, `??=`, `declare(strict_types)`, typed property, return type
  ⚠ **ไม่มีเทสบังคับข้อนี้แล้ว** (ตัวที่เคยบังคับถูกลบไปพร้อมสำเนาโค้ด — ดู §7) ⇒ เป็นวินัยของคนเขียน ไม่ใช่ของเครื่อง

**เฉพาะเครื่อง Windows ที่บ้าน** (เซสชันคลาวด์ Linux ไม่เจอสองข้อนี้ แต่ห้ามลืมเวลากลับไปทำที่นั่น):
- ข้อความไทยที่ส่งเป็น inline argument ผ่าน Bash **จะเพี้ยน** — เขียน JSON ลงไฟล์แล้วส่งด้วย `curl --data-binary @file`
- ไฟล์ `.ps1` ต้องเป็น ASCII ล้วน (PS 5.1 อ่านไฟล์ไม่มี BOM เป็น ANSI) · PS 5.1 ยังทำให้ stderr ของ npm
  กลายเป็น "ความล้มเหลว" ได้ด้วย (commit `602e5f3`)

---

## 7. เรื่องสำเนา — จบไปแล้ว 3 จุด เก็บไว้เป็นบทเรียน

1. ~~`bridge/` ถือสำเนาโค้ด PHP~~ **ลบสำเนาโค้ดทิ้งทั้งหมดแล้ว** (commit `9b9b551`)
   ตอนวัดก่อนลบ: `aim_forward.php` ต่างจากของจริง 12 บรรทัด · `api_aim_notify.php` ต่าง 9 บรรทัด
   และ **ขาด `is_scalar` ทั้ง 3 จุด** ที่ code-reviewer สั่งแก้ (D-P0-10) ⇒ ก๊อปทับเมื่อไรคือย้อนแพตช์ทิ้ง
   ซ้ำร้าย `test_aim_forward.ps1` เทสต์สำเนา ไม่ใช่ไฟล์ที่รันจริง = **ยามที่เฝ้าผิดตัว**
   ⛔ **ห้ามเอาสำเนาโค้ดกลับมาวางใน `bridge/` อีก** — ตอนนี้เหลือแต่ config ตัวอย่าง
2. ~~`vendor-ksk\docs\ai-manager\` เป็นสำเนาแช่แข็ง 4 ฉบับ~~ **ลบแล้ว เหลือ README ชี้มาที่ `aim\docs\`**
3. ~~`README.md`/`bridge/README.md` เขียนสถานะผิดจนรายงานผิดตาม~~ **แก้แล้ว** (commit `32ad837`)
   `README.md` เปลี่ยนบทบาทเป็น "ระบบคืออะไร" · สถานะย้ายมาอยู่ที่ไฟล์นี้ที่เดียว (commit `1dfaa07`)

⚠ **ยังเป็นประวัติศาสตร์ ไม่ใช่สถานะ:** `HANDOFF.md` · `QUESTIONS.md` (รอบ WP-P0-1..3, 2026-08-27)
· `docs/ARCHITECTURE.md` · `docs/DATABASE_SCHEMA.md` (ดีไซน์ 2026-08-26 ที่ถูกแทนที่แล้ววันถัดมา)

---

## 8. ยามที่เฝ้าระบบนี้ — มีแล้ว แต่ยังมีจุดบอดที่ต้องรู้

เดิมไฟล์นี้เขียนว่า "ระบบนี้อยู่นอกสายตายามทั้งหมดของ estate" — **ไม่จริงแล้ว** 📄 ตอนนี้มี:

| ยาม | ทำอะไร |
|---|---|
| `bin/selfcheck.php` ฝั่ง vendor-ksk | มี `/healthz` ของ aim อยู่ใน `$sites` แล้ว (D-P0-14) + คาดการณ์โควตา push ต่อเดือน |
| task `ksk-aim-task-sync` (ทุก 10 นาที) | ส่งสำเนางาน `tbl_task` → `POST /sync/tasks` (D-P0-17) |
| task `ksk-aim-reconcile` (ทุกคืน 23:30) | เทียบ `tbl_task` กับสำเนาบนคลาวด์ — **นี่คือตัวที่ประตู P4 ใช้ตัดสิน** |
| `GET /healthz` | คิวค้าง · งานที่ตายถาวร · event ค้างเกิน 15 นาที (commit `027d7c1`) |
| dump D1 กลับบ้านรายวัน | ปิดกฎ `IMPLEMENTATION_PLAN.md` §5 ข้อ 2 (commit `4c9378d`) |

**จุดบอดที่เหลือ:**
- 🔴 **ตัวนับสตรีคตอบ 0 ถ้ากดก่อน 23:30** — มีตัวนับแล้ว (`--streak`) แต่บั๊กทำให้คนที่กดกลางวันได้เลขผิด (§3)
- 📄 `lib/aim_notify.php` ยิง `ll_line_push_to()` ตรง ไม่ผ่านทะเบียนแจ้งเตือน ⇒ **ไม่มีสวิตช์ปิดรวม**
  (ด่านปฏิทินวันหยุดมีแล้วเฉพาะเส้นทางสรุปตามเวลา — D-P0-15)

---

## 9. เอกสารตัวจริงในรีโปนี้

| ไฟล์ | ใช้ตอนไหน |
|---|---|
| `docs/QUEUE.md` | **คิวงานปัจจุบัน** — Codex หยิบใบบนสุด · ข้อห้าม 6 ข้อ · เลขเทสฝั่ง LIVE ที่ห้ามลดลง |
| `AGENTS.md` | ทางเข้าของ Codex (ชี้ไป QUEUE.md) |
| `docs/DECISIONS_P0.md` | **อ่านก่อนเสมอ** — คำตัดสิน D-P0-01..18 ที่ห้ามรื้อโดยไม่มีข้อเท็จจริงใหม่ |
| `docs/IMPLEMENTATION_PLAN.md` | **อ่าน §1 ก่อน §6 เสมอ** — §1 คำสั่งที่ยึด 5 ข้อของพี่เต้ + ของฟรีที่มีอยู่แล้ว (รวมข้อที่บอกว่า vendor-ksk อยู่บน GitHub) · §2 สถาปัตยกรรม 4 ชั้น · §4 กฎข้อมูลคน · §5 กฎกันติดกับดักของฟรี · **§6 ลำดับเฟสและประตู ห้ามข้ามประตู** |
| `docs/WORK_PACKAGES_P0..P5.md` | ใบสั่งงานรายเฟส (P4/P5 เขียนล่วงหน้าไว้แล้ว) |
| `README.md` | ระบบคืออะไร · endpoint · schema · วิธีพัฒนา — **ไม่ใช่ที่เก็บสถานะ** |

**เอกสารตัวจริงของฝั่ง LIVE ไม่ได้อยู่ในรีโปนี้:** `mottosolar-gif/KSK-Decepticons` → `CLAUDE.md` ของมันเอง
คือต้นทางของ **เลขเทสฝั่ง LIVE · รายการยามทั้งหมด · ของใหม่ที่ฝั่งนั้นทำไปแล้ว**
⇒ ก่อนรายงานสถานะรวม **ต้องเปิดไฟล์นั้นด้วย** ไม่ใช่อ่านแต่ไฟล์นี้ (2026-09-04 เพิ่งจ่ายบทเรียนข้อนี้ไป)

⚠ **`DECISIONS_P0.md` มีเลขซ้ำ:** มี **`D-P0-15` สองใบ** (บรรทัด 356 = "Section A รอตรวจ" · บรรทัด 377 = "ปิดจบ +
ขึ้น production") ⇒ อ้าง "ตาม D-P0-15" เฉย ๆ จะกำกวม **ต้องระบุว่าใบไหน** จนกว่าจะมีคนแก้เลข

**เจอข้อความที่ขัดกันระหว่างเอกสารกับของจริงบนดิสก์ — ของจริงชนะเสมอ แล้วแก้เอกสารให้ตรง อย่าปล่อยไว้**
(ไฟล์นี้เองเคยค้างอยู่ 9 commit หลัง HEAD จนรายงานสถานะผิดไป 3 เฟส — 2026-08-29 ถึง 2026-09-03)
