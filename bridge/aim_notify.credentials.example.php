<?php
// ตัวอย่าง config สำหรับ api/aim_notify.php — คัดลอกไปเป็น config/aim_notify.credentials.php
// แล้วใส่ค่าจริง (ไม่เข้า git — เหมือน scrapbot.credentials.php)
return array(
    'enabled'    => false,
    'notify_key' => 'PASTE_RANDOM_SECRET_HERE',   // ความลับคนละดวงกับ aim.credentials.php (ทิศทางตรงข้าม)
);
