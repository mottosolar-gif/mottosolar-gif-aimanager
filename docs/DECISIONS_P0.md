# คำตัดสินของผู้ออกแบบ — P0 (มีผลเหนือใบสั่งงานเดิมเมื่อขัดกัน)

## D-P0-01 · `inbox_event` เก็บ metadata เท่านั้น ห้ามเก็บเนื้อความ
2026-08-27 · เกิดจาก Codex ทักว่า WP-P0-3 ("เก็บ event ดิบ") ขัดกับกฎห้ามข้อมูลบุคคลขึ้นคลาวด์ — **Codex ทักถูก**

**เก็บได้:**
- `event_id` (LINE `webhookEventId`) UNIQUE — กุญแจกันซ้ำ
- `event_type` (message / postback / join / leave / follow …)
- `message_type` (text / image / audio / location / sticker …) — ชนิดเท่านั้น
- `source_type` (user / group / room)
- `source_hash` — SHA-256 ของ userId/groupId + salt จาก env · **ห้ามเก็บ id ดิบ**
- `occurred_at` — เวลาที่เหตุการณ์เกิดจริง (จาก LINE timestamp)
- `received_at` — เวลาที่คลาวด์รับเข้ามา
- `payload_bytes` (INTEGER) · `payload_sha256` — พิสูจน์ว่าได้รับอะไรมาโดยไม่ต้องเก็บของจริง
- `status` · `attempts` · `last_error` (error ของระบบเท่านั้น)
- `body_ref` (TEXT NULL) — ช่องไว้ชี้ที่เก็บเนื้อความภายนอกในอนาคต · **เฟสนี้ต้องเป็น NULL เสมอ**

**ห้ามเก็บเด็ดขาด:** ข้อความที่ผู้ใช้พิมพ์ · ชื่อ/นามสกุล/ชื่อแสดงผล · เบอร์โทร · อีเมล · เหตุผลการลา · URL รูปที่ดาวน์โหลดได้ · LINE id ดิบ

**เหตุผล:** P0 มีเป้าหมายเดียว — พิสูจน์ว่าท่อทะลุและกันซ้ำได้ ซึ่ง metadata เพียงพอ
เนื้อความจะถูกอ่านโดยชั้นสมองที่รันบนเครื่องภายใน (homelab) ในเฟสถัดไป **ไม่เก็บถาวรบนคลาวด์**

**ยาม privacy ต้องตรวจ 3 อย่าง:**
1. schema ไม่มีคอลัมน์ต้องห้าม (`name` `fullname` `phone` `email` `reason` `text` `message`)
2. `body_ref` เป็น NULL ทุกแถว
3. ไม่มีค่าที่หน้าตาเป็น LINE id ดิบ (`^U[0-9a-f]{32}$` / `^C[0-9a-f]{32}$`) อยู่ในฐาน

**เทสบังคับเพิ่ม:** ยิง event ที่มีข้อความยาว → ตรวจว่าข้อความนั้นไม่ปรากฏที่ใดในฐานเลย

---

## D-P0-02 · ⛔ ห้ามแตะ Worker `mottosolar-line-relay`
2026-08-27 · ตรวจจากหน้า Cloudflare จริงของเจ้าของ

Worker ชื่อนี้มีอยู่แล้วในบัญชีเดียวกัน และ **ทำงานจริง 22,370 invocations ใน 24 ชม.** (~15 ครั้ง/นาที · Errors 0 · binding = Durable Object `QUEUE`)
เป็นของระบบ **homelab** ที่รับ LINE webhook ไปพักไว้ให้ปลายทางดึง — ถ้า deploy ทับ ระบบนั้นตายทันที

⇒ **Worker ของโครงการนี้ต้องชื่อ `aim-ingest`**
⇒ `wrangler.toml` อ่าน `account_id` จาก environment (`CLOUDFLARE_ACCOUNT_ID`) ห้ามฝังในไฟล์

**โควตาที่ต้องเคารพ:** แพลนฟรีให้ 100,000 invocations/วัน **ต่อบัญชี** และตัวเดิมใช้ไปแล้ว ~22%
⇒ ห้ามออกแบบให้ poll ถี่ · Cron ทุก 1 นาที = ~1,440 ครั้ง/วัน ถือว่าเหมาะสม

---

## D-P0-03 · ขอบเขตรอบนี้
ทำเฉพาะ **WP-P0-1 · WP-P0-2 · WP-P0-3**
**ห้ามทำ** WP-P0-4 (Cron+ตัวกินคิว) · WP-P0-5 (แตะ `line_webhook.php` ของระบบ LIVE) · WP-P0-6 — ทั้งสามใบรอคำอนุมัติจากเจ้าของระบบ

---

## D-P0-04 · ชี้แจงสถานะ P0-6 (แก้ความสับสนที่ code-reviewer ทักไว้ 2026-08-27)

**สิ่งที่เกิดขึ้นจริง:** พี่เต้สั่ง "ขึ้น cloudflare ได้เลยพร้อมแล้ว" (27 ส.ค. 2026) ⇒ ถือเป็นการอนุมัติ deploy จริงของ WP-P0-1..3
ผู้ตรวจอิสระ (Claude) จึง **ทดสอบด้วยมือ 6 ใน 7 ข้อของเกณฑ์ WP-P0-6** กับ Worker/D1 จริงบน Cloudflare
(healthz · 403 · idempotency 3 ครั้ง · payload พัง · privacy scan ตรงบนฐานจริง · db:dump)
เป็นการยืนยันคุณภาพของ **WP-P0-1..3 ที่ deploy ไปแล้ว** ไม่ใช่การประกาศว่า **WP-P0-6 ทั้งใบผ่านอย่างเป็นทางการ**

**ที่ยังไม่เกิดและยังห้ามทำ:**
- ข้อ 4 ของเกณฑ์ WP-P0-6 (ปิด cim-server แล้ว Cron คลาวด์ยังเดิน) ทดสอบไม่ได้จนกว่า **WP-P0-4** (Cron+ตัวกินคิว) จะถูกสร้าง — ยังไม่เริ่ม
- **WP-P0-5** (ต่อท่อจาก `line_webhook.php` ของระบบ LIVE) ยังไม่เริ่ม รอคำอนุมัติแยกตามที่ D-P0-03 กำหนดไว้เดิม เพราะแตะไฟล์ที่พนักงาน 11 คนใช้จริงทุกวัน

**สรุป:** D-P0-03 (ขอบเขต WP-P0-1..3 เท่านั้น ห้ามทำ P0-4/5/6 โดยไม่อนุมัติ) **ยังมีผลบังคับใช้เหมือนเดิม** — สิ่งที่เปลี่ยนคือ P0-1..3 ได้รับอนุมัติ deploy แล้วและผ่านการตรวจแล้ว ไม่ใช่ขอบเขตทั้งเฟสถูกอนุมัติ

## D-P0-05 · ผลตรวจ code-reviewer อิสระ (2026-08-27) — สรุปและติดตามผล

**ผ่าน** สำหรับขอบเขต WP-P0-1..3 — ไม่พบช่องโหว่ PII ที่ใช้ประโยชน์ได้จริงในโค้ดปัจจุบัน, constant-time comparison ถูกต้อง, idempotency ทนต่อ concurrent request จริง, secrets ไม่หลุดเข้า git

**MEDIUM ที่สั่งแก้แล้ว (round 7):** `schema/privacy-guard.mjs` เป็น denylist ชื่อคอลัมน์ — พิสูจน์แล้วว่าหลบเลี่ยงได้ด้วยชื่อคอลัมน์คำพ้องความหมาย (`note` ไม่อยู่ใน denylist) ⇒ เปลี่ยนเป็น allowlist ตายตัวต่อคอลัมน์ต่อตาราง (ดู `ALLOWED_COLUMNS` ใน round 7 instructions)

**LOW ที่สั่งแก้พร้อมกัน:** เพดานจำนวน event ต่อ request (defense-in-depth) · ผูก `privacy:check:remote` เข้า CI post-deploy (ตอนนี้เป็นแค่การรันมือ)

**บันทึกไว้เป็น backlog ไม่บล็อก P0 (ยังไม่ต้องแก้ตอนนี้):**
- `job_queue.idem_key` ใช้แค่ `event.eventId` เดี่ยว ๆ — พอสำหรับ P0 ที่มี kind เดียว ถ้าเฟสหน้ามีมากกว่า 1 job ต่อ event ต้องทบทวนคีย์ให้รวม `kind` ด้วย
- `extractMetadata` ใช้ `Promise.all` ครอบทุก event ในชุดเดียว ⇒ event เดียวผิดรูปทำให้ทั้งชุดถูกปฏิเสธ 400 หมด — ไม่ใช่ปัญหา privacy (ไม่มีอะไรถูกเก็บทั้งสองกรณี) แต่เป็นความเสี่ยง completeness ที่ต้องคิดตอนออกแบบ WP-P0-4/5 (ระบบเดิม cim-server ทนได้เพราะ fail แล้วเดินหน้าต่อ แต่ AI Manager จะเสีย event นั้นไปเงียบ ๆ ถ้าไม่ทำ retry แยกราย event)

---

## D-P0-06 · ⛔ ถอดเครื่องมือ `schema/privacy-guard.mjs` ออกทั้งหมด (2026-08-27 — เจ้าของระบบตัดสินเอง)

**บริบท:** เจ้าของระบบชี้ว่าชื่อในกลุ่ม LINE เป้าหมายเป็นชื่อเล่น/ชื่อปลอมทั้งหมด ไม่ใช่ชื่อจริง
ผู้ออกแบบ (Claude) ชี้แจงก่อนว่ายามนี้ไม่เคยแตะ display name เลยตั้งแต่แรก (VERIFIED จากโค้ด — grep ทั้ง repo ไม่เจอ)
สิ่งที่ยามป้องกันจริงคือ (1) LINE `userId` ดิบ ซึ่งเป็นรหัสถาวรผูกกับบัญชีจริงไม่ว่าจะตั้งชื่อโชว์เป็นอะไร
และ (2) เนื้อความที่พิมพ์ซึ่งอาจมีชื่อจริง/เบอร์โทร/เหตุผลลาได้เสมอไม่เกี่ยวกับชื่อโชว์ของผู้พิมพ์
เสนอ 3 ทางเลือก (คง denylist→allowlist เดิม / เก็บ userId ดิบไม่ hash / ถอดยามอัตโนมัติทั้งชุด) — **เจ้าของระบบเลือกทางที่ 3**

**ผลของคำตัดสิน:**
- ลบ `schema/privacy-guard.mjs` และการเรียกใช้ทั้งหมด (`npm run privacy:check` / `:local` / `:remote`,
  step ใน `.github/workflows/deploy.yml` ทั้งใน job `test` และ `deploy`)
- **ไม่กระทบกลไกอื่นที่แยกกันโดยสิ้นเชิง** — ยังคงอยู่เหมือนเดิมทุกจุด:
  - `hashSourceId()` ใน `crypto.ts` ยังแฮช LINE userId/groupId ก่อนเก็บทุกครั้ง (ไม่ได้ยกเลิกการ hash)
  - `body_ref TEXT CHECK (body_ref IS NULL)` ยัง enforce ที่ระดับฐานข้อมูลเหมือนเดิม
  - `schema/check-migration.mjs` ยังทดสอบ constraint เหมือนเดิม (คนละเครื่องมือกับที่ถอดออก)

**ผลกระทบที่ต้องรู้ไปข้างหน้า:** จากนี้ migration ใหม่จะเพิ่มคอลัมน์ชื่อ/เบอร์/ข้อความ/เหตุผลลาได้โดย **ไม่มีระบบอัตโนมัติเตือน** —
เป็นการตัดสินใจรับความเสี่ยงของเจ้าของระบบเอง (ทางเลือกที่ 1/2 เคยเสนอไว้เป็นทางที่คุมความเสี่ยงมากกว่าแต่ไม่ถูกเลือก)
คนที่รีวิว migration ใหม่ในอนาคตต้องตรวจคอลัมน์ที่เพิ่มด้วยตาเองแทน — บันทึกไว้เป็นความรู้สำหรับผู้ตรวจรอบถัดไป (Claude/code-reviewer)
ให้ยกเรื่องนี้ขึ้นมาตรวจด้วยตาทุกครั้งที่มี migration ใหม่ตั้งแต่นี้ไป

---

## D-P0-07 · ผลตรวจ code-reviewer รอบ P0-5 (ติดตั้งจริงบน LIVE) — 2026-08-27

**ผ่าน — ปลอดภัยที่จะรันต่อบน production** ไม่พบ finding ระดับ Critical/High จากการอ่านโค้ดของ
`lib/aim_forward.php`, `config/aim.credentials.php`, และ 2 จุดที่แทรกใน `api/line_webhook.php`
ยืนยันเพิ่มเติมจากที่ Claude ทดสอบรันจริงไว้ก่อนหน้า: timeout ครอบคลุมถึงเฟส DNS จริง (ตรวจ `curl_version()`
บนเครื่อง — libcurl มี `CURL_VERSION_ASYNCHDNS`), ไม่มีทาง `'' === ''` หลุดกรองกลุ่มทดสอบผิด (กันสองชั้น),
ไม่ log เนื้อหาผู้ใช้เลย, จุดแทรกไม่กระทบโค้ดเดิมแม้แต่บรรทัดเดียว

**M1 — บันทึกเป็นข้อจำกัดที่รู้แล้ว (ไม่บล็อก ไม่แก้ตอนนี้):**
`aim_forward_events($events)` เรียกครั้งเดียวต่อ 1 คำขอ webhook (ทั้งก้อน) ก่อนเข้า foreach — LINE
สามารถส่งหลาย event จากหลายกลุ่ม/หลายคนมาในคำขอเดียวกันได้ถ้าเกิดใกล้เคียงกัน ถ้า event ของกลุ่มทดสอบ
มากับ event ของพนักงานจริงในคำขอเดียวกัน พนักงานจริงคนนั้นจะโดนหน่วงไปด้วย (สูงสุด ~1.5 วิ กรณีคลาวด์ล่ม)
⇒ **เหตุผลที่ยอมรับความเสี่ยงนี้ใน P0:** ปริมาณข้อความของกลุ่มทดสอบต่ำมาก โอกาสชนกันในคำขอเดียวกันต่ำ
และเพดานความล่าช้าจริงคือ 1.5 วิ ซึ่งยังไกลจาก `set_time_limit(280)` ของระบบมาก
⇒ **ทางแก้ที่ถูกต้องสำหรับเฟสถัดไป (ถ้าปริมาณโตขึ้น):** เปลี่ยนเป็นเขียนคิวท้องถิ่น (ตาราง/ไฟล์) แล้วให้
cron แยกยิงออกไป แทนการยิง HTTP ตรงในเส้นทาง webhook เอง — ตัดการพึ่งพา latency ของคลาวด์ออกจาก
เส้นทางหลักไปเลย ไม่ใช่แค่จำกัด timeout

**M2 — ต้องสร้างก่อนปิด WP-P0-5 (ตามที่สเปกตัวเองกำหนดไว้):** ยังไม่มี `bin/smoke_aim_forward.php`
ถาวรที่จับ regression ได้ถ้าอนาคตมีคนแก้ไฟล์นี้แล้วเผลอถอด try/catch หรือขยาย timeout โดยไม่รู้ตัว
— ผู้ตรวจก่อนหน้ามีแค่การทดสอบมือครั้งเดียว ไม่ใช่ยามถาวร → ดำเนินการสร้างต่อทันที (ดูหัวข้อถัดไป)

---

## D-P0-08 · บั๊กจริงจากการใช้งานจริงวันแรก — `webhookEventId` ของ LINE ขึ้นต้นด้วยตัวเลขถูกปฏิเสธทุกครั้ง

**2026-08-27 — เจอจาก log จริง ไม่ใช่ทดสอบ:** พี่เต้พิมพ์ข้อความในกลุ่มทดสอบ 2 ครั้ง ระบบยิงไปคลาวด์สำเร็จ
(fan-out ทำงานถูกต้องตามสเปค) แต่คลาวด์ตอบ `HTTP 400` ทุกครั้ง

**สาเหตุ:** `worker/src/payload.ts` `requiredToken()` บังคับตัวอักษรตัวแรกของ `webhookEventId` ต้องเป็น
`A-Za-z` แต่ ID จริงของ LINE เป็นรูปแบบ ULID ที่เข้ารหัส timestamp เป็นอักขระตัวแรก ⇒ **มักขึ้นต้นด้วยตัวเลข**
⇒ event จริงแทบทุกตัวถูกปฏิเสธ ตั้งแต่วันแรกที่ deploy โดยไม่มีใครรู้ เพราะทุกรอบทดสอบก่อนหน้า (ทั้งของ Codex
และของผู้ตรวจ) ใช้ค่าจำลองที่ตั้งขึ้นเอง (`test-evt-001`, `php-runtime-test-...`) ซึ่งบังเอิญขึ้นต้นด้วยตัวอักษร
เสมอ — **นี่คือกับดักเดียวกับบทเรียนเลข B/L ในกติกาบ้าน: ค่าจำลองที่ผู้ทดสอบเลือกเองมักผ่านกฎที่ผู้ทดสอบ
ตั้งเอง แต่ไม่ผ่านรูปแบบจริงของโลกภายนอก**

**แก้แล้ว:** regex เป็น `^[A-Za-z0-9][A-Za-z0-9_-]{0,127}$` (อนุญาตตัวเลขนำหน้า) · เพิ่มเทสด้วยค่ารูปแบบ ULID
จริง · deploy แล้ว (Version `df4dbf15`) · ยืนยันด้วย curl ตรงว่า payload เดิมที่เคยล้มเหลวตอนนี้ผ่านแล้ว (HTTP 200)

**บทเรียนสำหรับ P0-6/เฟสถัดไป:** เทสที่ auto-generate ค่าจำลองเองต้องสุ่มให้ครอบคลุมรูปแบบขอบเขตจริงด้วย
(เช่น ขึ้นต้นด้วยตัวเลข/ตัวอักษรสลับกัน) ไม่ใช่ยึดรูปแบบเดียวที่สะดวกกับผู้เขียนเทส

---

## D-P0-09 · WP-P1-A1..A3 deploy จริงสำเร็จ + ยืนยัน FK/CHECK บน D1 จริง (2026-08-27)

Migration `0002_task.sql` (ตาราง `task`, `task_event`, `outbound_queue`) apply ขึ้น `aim-db` จริงแล้ว (8 commands)

**ปิดข้อสงสัย MEDIUM ที่ code-reviewer ทิ้งไว้เป็น INFERRED:**
- **FK enforcement บน D1 จริง — VERIFIED:** ยิง `INSERT INTO task_event` ที่ `task_ref` ไม่มีอยู่จริง →
  D1 ตอบ `SQLITE_CONSTRAINT_FOREIGNKEY` ทันที ⇒ D1 บังคับ foreign key จริง ไม่ใช่แค่ syntax เฉยๆ
- **CHECK constraint บน D1 จริง — VERIFIED:** ยิง `INSERT INTO task ... status='bogus'` →
  D1 ตอบ `SQLITE_CONSTRAINT_CHECK` ทันที

ทดสอบสร้าง/ลบ task จริงบนฐาน production สำเร็จ (ลบข้อมูลทดสอบออกแล้ว ไม่เหลือค้าง)

**สรุป WP-P1-A ทั้งชุดปิดจบสมบูรณ์** — ผ่านตรวจตัวเอง + code-reviewer อิสระ + แก้ HIGH finding แล้ว
+ verified บนฐานจริงครบทุกจุดที่เคยเป็นแค่ทฤษฎี

---

## D-P0-10 · WP-P1-B1+B2 ปิดจบ — ครบวงจร LINE ปุ่ม → คลาวด์เปลี่ยนสถานะ → การ์ดจริงกลับ LINE (2026-08-27)

**WP-P1-B1** (คลาวด์ประมวลผล postback): `worker/src/queue.ts` อ่าน `postback_data` จาก inbox → resolve
`person_code` จาก `source_hash` → เรียก `applyTransition`. Code-reviewer อิสระพบ HIGH (ไม่มี ownership check —
ใครก็ตัดสินใจแทนเจ้าของงานได้) → แก้ที่ `taskMachine.ts` ให้บังคับ `assigneePersonCode === actorPersonCode`
เมื่อ `source === 'line'` แล้ว · เทสครบ (11 valid transitions + reject-unauthorized) · deploy แล้ว

**WP-P1-B2** (endpoint ให้คลาวด์สั่ง vendor-ksk ส่งการ์ดจริง): สร้าง `lib/aim_notify.php` +
`api/aim_notify.php` บน vendor-ksk LIVE (commit `5fb196c`) — ยืนยันจริงด้วยการกดปุ่มบนโทรศัพท์พี่เต้เอง
("กดรับไปแล้ว") ครบวงจรครั้งแรก: LINE ปุ่ม → คลาวด์ → task เปลี่ยนสถานะ → การ์ดจริงกลับ LINE

**ผลตรวจ code-reviewer อิสระรอบ B2 (หลัง live test):** ไม่พบ Critical/High · พบ Medium 1 + Low 1 —
ทั้งคู่แก้แล้ว ยืนยันสด commit `a55a8ae`:
- **Medium — type confusion → path disclosure:** cast `(string)` ตรงจาก JSON โดยไม่เช็ค `is_scalar()`
  ก่อน ⇒ ถ้าคลาวด์ส่ง array/object จะเกิด PHP warning หลุด path เซิร์ฟเวอร์ (ยืนยันแล้วว่า `display_errors=On`
  จริงบนเซิร์ฟเวอร์นี้ จากเคสเดิมใน `leave.php`) → เพิ่ม `is_scalar()` guard ตกเป็นค่าว่าง ชนกับเช็ค required
  fields เดิมพอดี ไม่ต้องเพิ่ม branch ใหม่ · **VERIFIED สด:** ยิง `task_ref` เป็น array จริงผ่าน curl ตรงไป
  `https://www.fimandksk122.com/vendor/api/aim_notify.php` → ได้ 400 สะอาด ไม่มี warning หลุด
- **Low — log injection:** log ใช้ `$taskRef` ดิบแทนค่าที่กรองแล้ว (ฟังก์ชัน `aim_send_task_card` กรองแค่
  ภายในตัวมันเอง ไม่ได้ส่งค่าที่กรองแล้วกลับมาให้ endpoint ใช้ log) → เพิ่มกรองซ้ำก่อน log ด้วย pattern เดียวกัน
  · **VERIFIED สด:** ยิง `task_ref` ที่มี raw newline จริง → log บรรทัดเดียวสะอาด ไม่มี newline หลุด

**WP-P1-B ทั้งชุด (B1+B2) ปิดจบสมบูรณ์** — เหลือ **WP-P1-B3** (ขยาย filter ใน `lib/aim_forward.php` ให้ forward
postback จากแชท 1:1 ด้วย ไม่ใช่แค่ข้อความในกลุ่มทดสอบ) เป็นจิ๊กซอว์ชิ้นสุดท้ายที่ยังขาด — พิสูจน์แล้วด้วย log จริง
ว่า postback ถึง vendor-ksk แล้วแต่ไม่ถึงคลาวด์ เพราะ filter เดิมกรองทิ้ง

---

## D-P0-11 · WP-P1-B3 ปิดจบ — ครบทั้ง 3 ชิ้นของ WP-P1-B แล้ว (2026-08-27, อนุมัติโดยพี่เต้)

เพิ่มเส้นทางที่สองใน `lib/aim_forward.php`: forward เสมอถ้า event เป็น postback ที่ `data` ขึ้นต้นด้วย
`act=aim_` ไม่ว่าจะมาจากแชท 1:1/กลุ่ม/ห้อง — ปิดช่องที่ postback จากแชท 1:1 (ไม่มี `groupId`) ไม่เคยถูก forward
มาก่อน เขียนผ่าน Codex CLI ทั้งหมด (`bridge/` staging) ตามกฎเจ้าของระบบ

**รอบตรวจแรก (code-reviewer อิสระ) พบ 2 finding ต้องแก้ก่อนปิดงาน:**
- **early-return ครอบเกินขอบเขต (ตัวสำคัญ):** `if ($testGroupId === '') return;` เดิมอยู่ก่อน foreach ⇒ ครอบ
  เส้นทาง postback ใหม่ไปด้วยโดยไม่ตั้งใจ ถ้า `test_group_id` ถูกเคลียร์วันไหน postback forwarding ทั้งหมด —
  ซึ่งเป็นเป้าหมายหลักของ WP-P1-B3 เอง — จะหยุดเงียบสนิททันที ไม่มี log ใดๆ แก้โดยย้าย check ให้ scope เฉพาะ
  เส้นทางกลุ่มทดสอบ (`if ($testGroupId !== '') { ... }`) เส้นทาง postback เป็นอิสระจากค่านี้แล้ว
- **`groupId` cast ไม่มี `is_scalar()` กัน:** บั๊กคลาสเดียวกับที่เพิ่งแก้ใน `api/aim_notify.php` (D-P0-10) — เพิ่ม
  guard ให้สอดคล้องกับ 2 จุดใหม่ในฟังก์ชันเดียวกัน

**ยืนยันการแก้ 2 ชั้น:**
1. **Isolated regression test** (fixture config แยกต่างหาก ไม่แตะ production, `test_group_id=''`,
   endpoint ชี้ `127.0.0.1:1` ที่ไม่มีใครฟัง) — พิสูจน์ postback ยัง forward จริงเมื่อ `test_group_id` ว่าง
   (log `HTTP 0` เพราะ endpoint จงใจ unreachable แต่ถึงจุด try{} แล้ว) + sanity ว่าเส้นทางกลุ่มยังปิดถูกต้อง
2. **Code-reviewer รอบสอง** (agent เดิม, resume ด้วย SendMessage) สร้าง sandbox ของตัวเองแยกต่างหาก ยิง 6 เคส
   เอง (รวมเคสใหม่ที่ผมไม่ได้ทดสอบ เช่น `groupId` เป็น array, event ที่ตรงทั้ง 2 เงื่อนไขพร้อมกันต้อง forward
   แค่ครั้งเดียว) — **คำตัดสิน: ผ่าน ทั้ง 2 finding ปิดจริง ไม่มี regression ไม่พบปัญหาใหม่จากการย้าย logic**

**ติดตั้งจริงบน vendor-ksk แล้ว** (`lib/aim_forward.php` commit `7a5c852`) · `aim` repo commit `16d2fdf`

**สรุป WP-P1-B ทั้งชุด (B1+B2+B3) ปิดจบสมบูรณ์** — วงจรเต็ม LINE ปุ่ม (1:1 หรือกลุ่ม) → forward เข้าคลาวด์ →
เปลี่ยนสถานะงาน → ส่งการ์ดกลับ LINE พร้อมทำงานครบทุกจุดแล้วในทางทฤษฎี/โค้ด — **ยังไม่ได้ทดสอบวงจรเต็มจริงกับ
ปุ่มที่กดจากแชท 1:1 จริง** (เทสที่ผ่านมาทั้งหมดคือ synthetic events ยิงตรงเข้าฟังก์ชัน ไม่ใช่ผ่าน LINE webhook
signature จริง) — ขั้นถัดไปที่ควรทำคือทดสอบวงจรเต็มกับพี่เต้กดปุ่มจริงบนโทรศัพท์ในแชท 1:1 (ไม่ใช่กลุ่มทดสอบ)

**ค้างสองเรื่องเล็ก ไม่กระทบความปลอดภัย/ความถูกต้อง:**
- 2 แถวทดสอบใน D1 คลาวด์ (`test-b3-verify-pos-001`, `test-b3-verify-sanity-001`) ยังไม่ได้ลบ — เชลล์นี้ไม่มี
  `CLOUDFLARE_API_TOKEN` แบบ non-interactive ให้ wrangler ใช้ยิง `d1 execute` ได้ (รอบก่อนที่เคยลบข้อมูลทดสอบ
  สำเร็จน่าจะทำผ่านเทอร์มินัลแบบ interactive ที่ login ค้างไว้ ซึ่งเซสชันนี้ไม่มี) — เป็น dead data ไม่ผูกกับ
  person จริง ไม่มีผลต่อระบบ
- `api/line_webhook.php:51-52` มีคอมเมนต์เก่าเขียนว่า "เฉพาะกลุ่มทดสอบเท่านั้น" ไม่ตรงกับพฤติกรรมจริงหลัง
  WP-P1-B3 แล้ว (severity ต่ำ, code-reviewer พบเป็น finding #3) — **จงใจไม่แก้รอบนี้** เพราะไฟล์นี้มี hunk
  ที่ยังไม่ commit จากงานอีกเซสชันหนึ่งปนอยู่ (tool_loan/tasks.php) แตะแล้วเสี่ยงไปยุ่งกับงานคนอื่นที่ยังไม่เสร็จ
  — ต้องรอพี่เต้ตัดสินใจว่าจะแยก commit ของ `api/line_webhook.php` ยังไงก่อน

**ต่อไป:** ทดสอบวงจรเต็มจริง (พี่เต้กดปุ่มจริงในแชท 1:1) แล้วปิด WP-P1 ทั้งชุด

---

## D-P0-12 · WP-P1-B ปิดจบสมบูรณ์ — พิสูจน์วงจรเต็มจริงด้วยข้อมูลจริง 100% (2026-08-27)

พี่เต้ให้ `CLOUDFLARE_API_TOKEN` (custom token, สิทธิ์ D1:Edit เท่านั้น) เข้าถึง D1 จริงได้เป็นครั้งแรกในเซสชันนี้
(ก่อนหน้านี้ wrangler ใช้ไม่ได้เพราะไม่มี token แบบ non-interactive) — ใช้เฉพาะ env var ใน shell รอบนี้เท่านั้น
ไม่เขียนลงไฟล์ใดๆ ที่เข้า git

**สิ่งที่ทำ:**
1. ลบ 2 แถวทดสอบเก่าจาก B3 ที่ค้างใน D1 (`test-b3-verify-pos-001`, `test-b3-verify-sanity-001`) — ตรวจก่อนลบว่า
   ไม่มีผลข้างเคียง (job หนึ่งตาย `dead` เพราะ unlinked person ตามคาด อีกอันจบปกติ ไม่มี task ใดถูกสร้าง/แก้)
2. สร้าง task ทดสอบจริง `TEST-B3-LIVELOOP-001` มอบหมายให้ `P-EKAPUN` (bootstrap `person`/`person_link` มีอยู่
   แล้วจาก WP-P1-B1) → เรียก `aim_notify.php` ส่งการ์ดจริงไปที่โทรศัพท์พี่เต้
3. **เจอบั๊ก encoding ระหว่างทาง:** ข้อความไทยที่ส่งผ่าน curl เป็น inline shell argument บน Windows Bash โดน
   บิดเบือน (การ์ดขึ้น `??????????????` แทนภาษาไทย) — ตรวจสอบแล้วว่า D1 เก็บข้อความถูกต้อง 100% (ไม่ใช่บั๊กใน
   `aim_send_task_card`/`json_encode`) สาเหตุคือ Windows console codepage ตอนส่ง non-ASCII ผ่าน command-line
   argument โดยตรง **แก้โดยเขียน JSON payload ลงไฟล์ก่อน แล้วส่งด้วย `curl --data-binary @file`** แทนการพิมพ์
   inline — ยืนยันแล้วว่าการ์ดใบสองขึ้นภาษาไทยถูกต้อง (บทเรียนเดียวกับ `powershell-file-write-corrupts-encoding`
   ในความจำ แต่เป็นฝั่ง argument-passing ไม่ใช่ file-write — ต้องระวังทุกครั้งที่ส่งข้อความไทยผ่าน Bash tool เป็น
   inline argument บนเครื่อง Windows นี้ ไม่ใช่แค่ตอนเขียนไฟล์)
4. **พี่เต้กดปุ่ม "❌ ปฏิเสธ" จริงบนโทรศัพท์** → ตรวจ D1 ทันที:
   ```
   task.status: assigned -> rejected
   task_event: actor_person_code=P-EKAPUN, old_status=assigned, new_status=rejected, source=line
   ```
   **พิสูจน์วงจรเต็มจริงครบทุกจุด:** ปุ่มจริงบนโทรศัพท์ → LINE webhook (signature จริง ไม่ใช่ synthetic event
   อีกต่อไป) → `aim_forward_events` forward สำเร็จ (WP-P1-B3) → คลาวด์รับ postback (WP-P1-B1) → ownership check
   ผ่าน (`assigneePersonCode === actorPersonCode`) → `applyTransition` เปลี่ยนสถานะจริง → `task_event` บันทึกครบ
5. ลบ `task`/`task_event` ของ `TEST-B3-LIVELOOP-001` ทิ้งหลังทดสอบเสร็จ — D1 กลับสู่สถานะสะอาด ไม่เหลือข้อมูล
   ทดสอบค้างเลย (ตรวจซ้ำด้วย `SELECT count(*) ... LIKE 'test-b3%'` = 0)

**สรุป WP-P1-B (B1+B2+B3) ปิดจบสมบูรณ์ 100% — verified end-to-end ด้วยข้อมูลจริงทุกจุด ไม่ใช่แค่ synthetic
events หรือทฤษฎีอีกต่อไป** จิ๊กซอว์ AI Manager ชิ้นแรกที่ครบวงจรสองทาง (LINE ↔ คลาวด์) ใช้งานได้จริงแล้ว

**ค้างเดิม (ไม่กระทบ):** คอมเมนต์เก่าใน `api/line_webhook.php:51-52` ยังไม่แก้ (รอพี่เต้ตัดสินใจเรื่องแยก commit
งานอีกเซสชัน) · `CLOUDFLARE_API_TOKEN` อยู่ใน shell env ของเซสชันนี้เท่านั้น ยังไม่ถาวร — ถ้าอยากให้จำข้ามเซสชัน
ต้องบอกให้ตั้งเป็น persistent env var

**ต่อไป:** รอพี่เต้ตัดสินใจทิศทางถัดไปของ AI Manager (P2 ตามแผน — ขยายไปพนักงานคนอื่น / natural-language command /
ฟีเจอร์อื่นตาม master spec เดิม)

---

## D-P0-13 · P2 เริ่มแล้ว — พี่เต้อนุมัติ "ไล่ไปเลย P2-5" + WP-P2-A1 ปิดจบ (2026-08-27, commit `d79ccee`)

พี่เต้เคาะทิศทาง: เดินตาม 4 เฟสที่เหลือ (P2-P5) ตามลำดับ ห้ามข้ามประตู — แผนเต็มเขียนที่
`docs/WORK_PACKAGES_P2.md` (P3-P5 จะเขียนตอนถึงจุดนั้น ไม่ออกแบบล่วงหน้าเกินไปเพราะรายละเอียดจะเปลี่ยนตาม
สิ่งที่เจอจริงใน P2)

**จุดตัดสินสถาปัตยกรรม P2:** ตอบคำถามด้วย SQL template ตรงใน Worker+D1 แทนรอชั้น homelab/Ollama (ยังไม่พร้อม
ใช้งาน โปรเจกต์เครื่องแยกยังรอจัดซื้อ) — ตรงเกท P2 พอดี (20 คำถามมาตรฐาน ไม่แต่งคำตอบ) เริ่มได้ทันที

**WP-P2-A1 (visibility model) ปิดจบ:** `worker/src/visibility.ts::canView()` เป็นจุดตัดสินสิทธิ์เดียวที่
query engine (WP-P2-A2, ยังไม่เริ่ม) ทุกตัวต้องเรียกก่อนคืนข้อมูลของคนอื่น — fail-closed ทุก branch ตรวจ
2 รอบโดย code-reviewer อิสระ (ตรวจ agent เดิม resume ทั้งคู่รอบ):
- รอบแรก: ผ่าน แต่พบ finding department ว่างสองฝั่ง "เห็นกัน" โดยไม่ตั้งใจ + แนะนำ ESLint กัน floating-promise
  บน `canView()` (จุดตัดสินสิทธิ์เดียว ลืม `await` ครั้งเดียว = รั่วทั้งระบบ)
- แก้ผ่าน Codex ทั้งคู่ (เพิ่ม guard + สร้าง `eslint.config.mjs` พร้อม `@typescript-eslint/no-floating-promises`
  — พิสูจน์กฎทำงานจริงด้วยการทดสอบ FAIL ก่อน PASS ไม่ใช่แค่ตั้ง config เฉยๆ)
- รอบสอง: **ผ่าน** ยืนยันอิสระด้วย grep หา caller จริง (ยังไม่มี = ยังไม่มีผลกระทบข้างเคียง), พิสูจน์ lint rule
  ด้วยมือซ้ำอีกรอบ, ไล่ package-lock diff ทั้ง 101 แพ็กเกจว่ามาจาก npmjs.org ล้วนไม่มี install script แปลกปลอม
  มีข้อสังเกต non-blocking 2 จุดเก็บไว้ทำตอนเปิด write path จริงใน A2: (1) เปลี่ยน `=== ""` เป็นเช็ค falsy
  กัน `null` ที่หลุด NOT NULL มาได้ในอนาคต (2) เติม `vitest.config.ts` เข้า ESLint `files` scope

52/52 เทสผ่าน (42 เดิม + 10 ใหม่), typecheck สะอาด, lint สะอาด — commit แล้ว `d79ccee`

**ต่อไป:** WP-P2-A2 (query engine — 20 คำถามมาตรฐาน) ต้องเรียก `canView()` ทุกครั้งก่อนคืนข้อมูลคนอื่น +
ปิด 2 ข้อสังเกต non-blocking ข้างต้นตอนเปิด write path จริง

---

## D-P0-14 · WP-P2-A2/A3 ปิดจบ + WP-P2-B1/B2 เขียนเสร็จ (2026-08-28)

### WP-P2-A2/A3 — query engine 20 คำถาม · **ปิดจบแล้ว** (code-reviewer ตัดสิน CLOSE)

ผ่านการตรวจอิสระ **6 รอบ** แก้ **8 รอบ** · 232 เทสบน SQLite จริงทับ migration จริง

**ค่าคงที่ที่ยืนยันแล้วว่ายังอยู่ครบ:** `canView()` เป็นจุดตัดสินสิทธิ์รายคนจุดเดียว · ไม่มี role/department
ใน SQL หรือใน engine (มีเทส grep บังคับ) · ทุกตัวเลขมาจากคิวรีจริง · เทสรันบน SQLite จริง

**บทเรียนที่แพงที่สุดของแพ็กเกจนี้ — "ยามที่ตกไม่ได้ ไม่ใช่ยาม":**
รอบ 6 สร้าง class guard ไล่ทุก intent จากตาราง intent เอง เพื่อกันบั๊ก "ตัดแถวก่อนกรองสิทธิ์"
รอบ 7 ผู้ตรวจ**ลองเพิ่ม intent ที่ 21 ด้วยรูปทรงบั๊กเดิม** แล้วยามยังเขียว (ตาบอดกับ per-person aggregate)
รอบ 8 ผู้ตรวจ**ลบ scope ออกจาก `queryAcceptTimeCandidates` ตัวจริง** — 227 เทสยังเขียวหมด
ทั้งที่ชื่อคนข้ามแผนกหลุดออกมาจริง เพราะ fixture ให้คนที่ต้องมองไม่เห็นถือคนละ 1 งาน
ส่วนผู้ถามถือ 7 งาน ⇒ **คนที่ซ่อนอยู่ชนะอันดับไม่ได้เลยโดยโครงสร้าง ยามจึงไม่มีทางตกได้**
⇒ แก้ fixture ให้คนที่ซ่อนถือ *มากกว่า* ผู้ถาม · ตอนนี้ทำลาย scope ทีละจุดครบ 10 จุด เทสจับได้ทั้ง 10
**กติกาถาวรจากบทเรียนนี้: ทุกครั้งที่เขียนยาม ต้องพิสูจน์ว่ามันตกได้ ด้วยการทำให้มันตกจริงก่อน
แล้วค่อยคืนค่า — ไม่ใช่แค่ดูว่ามันเขียว**

**บทเรียนที่สอง — "แก้ตรงที่ถูกชี้ ไม่ใช่แก้ทั้งชนิด":** สามรอบติดกันที่ผู้เขียนแก้เฉพาะจุดที่ผู้ตรวจชี้
แล้วบั๊กชนิดเดียวกันในที่อื่นยังอยู่ครบ (รอบ 5 แก้ `stats_*` 4 ตัวที่ถูกชี้ ปล่อย `team_*` 4 ตัวที่ผิดแบบ
เดียวกันเป๊ะ) ⇒ ใบสั่งงานต้องเขียนเป็น **ข้อบังคับ + เทสที่ไล่ทุกกรณีจากทะเบียนเอง** ไม่ใช่ลิสต์จุดที่ต้องแก้

### WP-P2-B1/B2 — ถาม-ตอบผ่าน LINE · เขียนเสร็จ ยังไม่ปล่อย

- **B2** `POST /ask` บน Worker — ตรวจคีย์ constant-time (คนละดวงกับ ingest) · เรียก `askQuestion()`
  ตัวเดิมเท่านั้น · รับเฉพาะ `person_code` ที่มี `person_link` จริง โดยตอบ 403 เหมือนคีย์ผิดทุกตัวอักษร
  เพื่อไม่ให้ใช้เดาว่ามีใครอยู่ในระบบ · 245 เทส
- **B1** `lib/aim_ask.php` ฝั่ง LIVE — เปิดให้ **2 บัญชีเท่านั้น** (พี่เต้ + ผู้ช่วย) รายชื่อกลับด้านมาจาก
  `config/aim_person_map.php` ที่เดียว · ยามถาวร `bin/smoke_aim_ask.php` 29 ข้อ
- **ยังไม่ปล่อย:** `ask_enabled=false` และ **Worker ยังไม่ได้ deploy** (`/ask` ตอบ 404) —
  คำสั่ง deploy ถูก classifier ของเครื่องบล็อก ต้องให้พี่เต้รันเองหรือกด workflow_dispatch บน GitHub

### สิ่งที่เปลี่ยนไปนอกโค้ด (จดไว้กันหลง)

- repo มี remote แล้ว: `mottosolar-gif/mottosolar-gif-aimanager` (private) · branch `main`
  **ชื่อไม่ตรงกับที่แผนเดิมเขียนไว้ว่า `aim`** — ยึดของจริง
- **deploy เปลี่ยนเป็นสั่งด้วยมือ** (`workflow_dispatch` + ต้องพิมพ์ยืนยัน + ต้องอยู่บน `main`)
  เดิม push เข้า main แล้ว deploy อัตโนมัติ ซึ่งขัดกติกา "อนุมัติดีไซน์ ≠ อนุมัติปล่อยจริง"
- `npm run lint` เข้า CI แล้ว (เดิมไม่มี ⇒ กฎกัน `canView()` ลืม await ไม่มีใครบังคับใช้)
- aim เข้าสายตายามแล้วผ่าน `bin/selfcheck.php` ฝั่ง vendor-ksk (`/healthz` ใน `$sites`)

### ⚠ หนี้ที่ยังไม่จ่าย (ต้องปิดก่อนขยายออกนอก 2 บัญชีทดสอบ)

1. **ไม่มีโค้ด production ตัวไหนเขียน `person_link` เลย** — 2 แถวที่มีอยู่มาจากการพิมพ์
   `wrangler d1 execute` ด้วยมือทั้งคู่ ⇒ พนักงานคนที่ 3 เป็นต้นไปจะติด 403 ตลอดกาล
2. จุดแทรกใน `api/line_webhook.php` **ยังไม่ถูก commit** ทั้ง WP-P0-5 และ B1 เพราะไฟล์นั้นมีงานค้าง
   จากอีกเซสชันปนอยู่ · `git checkout --` ไฟล์นั้นเมื่อไร สะพานหลุดเงียบโดยโค้ดยังดูเหมือนต่ออยู่
3. **ยังไม่มี dump ข้อมูลกลับบ้านรายวัน** (กฎ §5 ข้อ 2 สั่งไว้) · `npm run db:dump` default เป็น local
4. WP-P0-5 ยังขาด `bin/smoke_aim_forward.php` ตามที่ D-P0-07 M2 กำหนดเป็นเงื่อนไขปิด

---

## D-P0-15 · WP-P2-B3 Section A แก้ตาม code-reviewer แล้ว — ยังรอตรวจอิสระ (2026-08-28 19:05)

พี่เต้อนุมัติแผนแก้ finding A1–A8 ฝั่งคลาวด์ใน `C:\WebApp\aim` และสั่งชัดว่าไม่แตะ Section B
ที่กำลังแก้คู่ขนานใน `C:\WebApp\vendor-ksk` · ห้าม commit · ห้าม deploy · ห้ามส่ง LINE

**สิ่งที่ Codex ลงมือ:** ป้องกัน `cron_tick` เดินหน้าเมื่อ summary pass ยังมีงาน retry/config error ·
ทิ้ง ledger trace เมื่อ pass หรือรายคนล้ม · แยกความผิดพลาดรายคน · ทำ config ที่ยังไม่พร้อมให้กู้กลับได้ ·
แยก 4xx ถาวรออกจาก 5xx/network ชั่วคราว · คง idempotency header ตามสัญญาที่ Section B กำลังรับไปใช้ ·
แก้ guard คำลงท้าย · seed `cron_tick` เพื่อเดินสาขา catch-up จริง · จำกัด catch-up 4 รอบล่าสุดต่อ tick
และลง ledger ระบุจำนวนรอบที่ข้าม

**หลักฐานจากเครื่องก่อนส่งตรวจ:** A6 mutation เพิ่มคำลงท้ายเป็น 2 ครั้งแล้วเทสต์ตก `2 != 1` ก่อนคืนค่าและผ่าน ·
A7 mutation เปลี่ยน `cron_tick` เป็น `cron_tock` แล้วเทสต์ตก (`missed` 1 แทน 4) ก่อนคืนค่าและผ่าน ·
เทสต์รวมก่อนแก้ 267/267 หลังแก้ 272/272 · `typecheck`/`lint` ผ่าน · migration check ผ่าน 3 ไฟล์สองรอบใน memory
และยืนยัน `body_ref` ยัง NULL-only

**สถานะ:** ยังห้ามถือว่าปิด WP-P2-B3 — ผลข้างต้นเป็นผลรันของผู้ลงมือเอง ต้องให้ Claude/code-reviewer
ตรวจ diff และพิสูจน์ซ้ำตามกติกา “Codex สร้าง · Claude ตรวจ” ก่อนตัดสิน CLOSE

---

## D-P0-16 · WP-P2-B3 ปิดจบ + ขึ้น production + ด่านปฏิทินวันหยุด (2026-08-28)

> ⚠ เดิมใบนี้เคยเลขซ้ำกับ D-P0-15 ข้างบน (ทั้งสองใบเขียน "D-P0-15" พร้อมกัน) — แก้เมื่อ 2026-09-04
> โดยเลื่อนเลขใบนี้และใบถัดไปทั้งหมดขึ้น 1 (D-P0-16 เดิม → 17, 17 เดิม → 18, 18 เดิม → 19)

**สถานะ: DEPLOYED** — Version `4fd4f31e-f98c-456f-9954-4fd873e547a5` · cron `*/1 * * * *`
ปลายทาง `https://aim-ingest.mottosolar.workers.dev`

### 1) ตรวจ B3 ครบ 10 finding แล้วปิด
code-reviewer อิสระพบ 10 ข้อ (คลาวด์ 8 · LIVE 2) แก้ครบ · น้องกุ้งตรวจซ้ำเองแล้วเจอเพิ่ม 1 ข้อ
(408/429 ถูกเหมาเป็นความผิดพลาดถาวร ทั้งที่ IIS/ARR ที่คั่นหน้าปลายทางตอบสองรหัสนี้ได้)

**สามข้อที่เป็น "ยามที่ตกไม่ได้" — จ่ายบทเรียนซ้ำเป็นครั้งที่สามในโปรเจกต์นี้:**
| | ก่อนแก้ | หลักฐานว่าแก้จริง |
|---|---|---|
| A6 เทสคำลงท้าย | `match(/ค่ะ$/gu)` ไม่มี flag `m` ⇒ ได้ 1 เสมอ | ข้อความมีคำลงท้าย 3 ครั้ง: regex เดิมได้ 1 · ใหม่ได้ 3 |
| A7 สาขา catch-up | ไม่มีเทสไหน seed `cron_tick` เลย | เขียน `cron_tock` → ตกทันที (เดิม 16 ข้อยังเขียวหมด) |
| B1 log ไม่มีเนื้อข้อความ | marker อยู่หลังตัวที่ 4,550 ถูกตัดทิ้งก่อน | ทำให้ log บันทึกเนื้อข้อความ → ตก 15/16 |

**บทเรียนที่ต้องจำ: "จำนวนเทสเพิ่มขึ้น" ไม่ใช่หลักฐานว่าเทสมีฟัน**
assertion ที่เพิ่มในเคสเดิมไม่ทำให้ตัวเลขขยับเลย ⇒ ต้องพิสูจน์ด้วยการทำให้ตกทุกครั้ง ไม่ใช่ดูตัวเลข

### 2) ด่านปฏิทินวันหยุด (พี่เต้สั่งหลัง deploy รอบแรก)
ตรวจหลังขึ้น production พบว่า `latestScheduledRound()` **ไม่มีด่านวันหยุดเลย** — ไม่เช็คเสาร์-อาทิตย์
ไม่เช็ค `tbl_holiday` ⇒ เสาร์ 08:00 จะส่งจริง ซึ่งขัดขนบ estate ที่ให้การเตือนพนักงานทุกช่องผ่านปฏิทินก่อน
พี่เต้เลือก "ใส่ด่านปฏิทิน" แทน "ปล่อยไว้เพื่อเก็บข้อมูลทดสอบเร็วขึ้น"

**ด่านอยู่ฝั่ง LIVE ไม่ใช่ฝั่งนี้** — ปฏิทินอยู่ `db_customs` และกติกา vendor-ksk บังคับว่าอ่านผ่าน
`lib/holiday.php` ที่เดียว ⇒ ส่งปฏิทินขึ้นคลาวด์ = สำเนา = ความจริงสองชุด
ฝั่งนี้มีหน้าที่เดียวคือ **แปลคำตอบให้ ledger จดความจริงถูก**

**ทำไมต้องมี terminal ตัวใหม่ `skipped_holiday` ไม่ยืมของเดิม:**
- ยืม `sent` = โกหก (ไม่ได้ส่ง)
- ยืม `dead:` = บอกว่าพัง ทั้งที่ทุกอย่างทำงานถูกหมด
- ไม่เป็น terminal เลย = โดนยิงซ้ำทุกนาทีตลอดวันหยุด

`checkpointSafe` ต้องเป็น `true` ในกรณีนี้ — รอบนั้นจบแล้วจริง ไม่มีอะไรค้างให้ตามเก็บ
(ต่างจาก retry ที่ต้องหน่วง checkpoint ไว้ตาม A1)

**ทำไมเลือก 412 ไม่ใช่ 200:** ต้องเป็นสถานะที่คลาวด์**รุ่นเก่าและรุ่นใหม่ตีความแล้วไม่โกหกทั้งคู่**
รุ่นเก่าเห็น 4xx = "ไม่ได้ส่ง ถาวร" (หยาบแต่จริง) · ถ้าตอบ 200 รุ่นเก่าจะจดว่า `sent`
⇒ จะมีช่วงที่ ledger เขียนความจริงผิดระหว่างรอ deploy ซึ่งยอมไม่ได้

### 3) หลักฐานจาก production จริง (ไม่ใช่ผลรันในเครื่อง)
- นาทีแรกหลัง deploy รอบแรก: `28/08/2569:evening → missed × 2 คน` — รอบ 17:00 อายุเกิน 60 นาที
  ⇒ ไม่ส่ง **และทิ้งร่องรอยไว้** = finding A1 ทำงานจริง (เดิม "ไม่ส่ง" กับ "พังจนไม่รู้ตัว" หน้าตาเหมือนกัน)
- cron เดินติดกัน 18 นาทีไม่ขาด แต่ `scheduled_summary` มี **2 แถวเท่านั้นทั้งฐาน**
  ⇒ กันซ้ำทำงานจริงใน production ไม่ใช่แค่ในเทส (ถ้าไม่กัน ป่านนั้นต้องมี 36 แถว)
- ปลายทาง callback เข้าถึงได้จากเน็ตจริงและปฏิเสธกุญแจผิดด้วย 403 + JSON
  (ยิงทดสอบด้วยกุญแจผิดโดยเจตนา — ความเสี่ยง "คลาวด์ยิงไม่ถึงฝั่ง LIVE" ปิดแล้ว)

⚠ **บทเรียนวิธีวัด:** ระหว่างตรวจเผลอใช้ `datetime('now','-20 minutes')` เทียบกับ `occurred_at`
ซึ่งเป็น ISO มี `T`/`Z` ⇒ เทียบสตริงแล้วเข้าหมด นับ cron ได้ 819 นาทีใน 20 นาที
**เป็นท่าเดิมที่เคยพลาดมาแล้วตอนวัดความต่อเนื่องของ cron** — ต้องใช้ `substr(occurred_at,1,16)` เสมอ

### 4) ที่ยังไม่จบ
- ประตู P2 ข้อ "ส่งสรุปได้ 3 วันติด" — เสาร์-อาทิตย์ถูกด่านปฏิทินกั้น ⇒ เริ่มนับจันทร์ 31 ส.ค.
- WP-P0-6 เกณฑ์ข้อ 6 ยังวัดไม่ได้ตามที่เขียนไว้ (อ้างสคริปต์ที่ถูกลบไปแล้วโดย D-P0-06) — เจ้าของใบสั่งต้องแก้ถ้อยคำ

---

## D-P0-17 · แก้ถ้อยคำเกณฑ์ WP-P0-6 ข้อ 6 + รับ `person_code` ที่พ้องชื่อคนเป็นข้อยกเว้น (2026-08-28 — เจ้าของระบบตัดสิน)

**เกณฑ์ข้อ 6 เดิมวัดไม่ได้ตลอดกาล** — เขียนว่า *"(ตรวจด้วยสคริปต์ ไม่ใช่ด้วยตา)"* ซึ่งหมายถึง
`schema/privacy-guard.mjs` ที่ **เจ้าของระบบสั่งถอดทิ้งเองใน D-P0-06** และในคำตัดสินนั้นระบุตรง ๆ ว่า
จากนี้ต้องตรวจด้วยตา ⇒ เกณฑ์บังคับสิ่งที่คำตัดสินอีกใบห้ามไว้ ไม่ว่าระบบจะปลอดภัยแค่ไหนก็ติ๊กไม่ผ่าน
**บทเรียน: ประตูที่อ้างเครื่องมือเป็นชื่อ ๆ จะตายพร้อมเครื่องมือนั้น — เขียนประตูให้อ้าง "สิ่งที่ต้องจริง" ไม่ใช่ "ใช้อะไรตรวจ"**

### `P-EKAPUN` พ้องกับชื่อจริง — เจ้าของระบบเลือก "รับไว้ ไม่เปลี่ยนรหัส"
เสนอ 2 ทาง (เปลี่ยนเป็นรหัสไม่มีความหมาย `P-001` / รับความเสี่ยงแล้วบันทึกเป็นข้อยกเว้น)
**เจ้าของระบบเลือกทางที่ 2** เหตุผล: ในกลุ่มไม่มีใครใช้ชื่อจริง และ LINE id ไม่หลุดอยู่แล้ว
สอดคล้องกับจุดยืนเดิมใน D-P0-06 ไม่ใช่คำตัดสินใหม่

⚠ **บันทึกไว้เตือนผู้ตรวจรอบหน้า (รวมถึงตัวเอง):** ผู้เสนอยกเรื่องนี้ขึ้นมาใหม่ทั้งที่ D-P0-06 ปิดไปแล้ว
เจ้าของระบบต้องตอบว่า *"เคยคุยกันแล้ว ไปย้อนดูเอง"* — **ก่อนเสนอเรื่องความเป็นส่วนตัว ต้องอ่านบันทึกคำตัดสิน
ให้ครบก่อนเสมอ** การถามซ้ำเรื่องที่ปิดแล้วไม่ใช่ความรอบคอบ แต่เป็นการโยนภาระกลับไปให้เจ้าของระบบ

### ผลตรวจจริงกับ production (คิวรีตรงกับ D1 · 2026-08-28)
| ตรวจ | ผล |
|---|---|
| คอลัมน์ทั้งหมด 9 ตาราง | ไม่มีคอลัมน์ชื่อ/เบอร์/อีเมล/LINE id เลยสักตัว ✅ |
| `inbox_event.source_hash` | 21/21 เป็น hex 64 ตัวครบ (แฮชหมด ไม่มีของดิบ) ✅ |
| `person_link.source_hash` | 2/2 เป็น hex 64 ตัวครบ ✅ |
| `inbox_event.body_ref` | 0 แถวที่ไม่ใช่ NULL (CHECK ระดับฐานยัง enforce) ✅ |
| ช่องข้อความอิสระทุกช่อง | `task.title`='งานทดสอบวงจร P1-B' · ที่เหลือว่างหมด ✅ |
| `postback_data` 3 แถว | มีแต่ `act=` และ `task=` ✅ |
| `person` | `P-EKAPUN` / `P-ASSIST` — พ้องชื่อ 1 รายการ **รับไว้ตามคำตัดสินนี้** 🟠 |

⚠ **บทเรียนวิธีวัดที่จ่ายไปในรอบนี้:** เช็ค "LINE id ดิบหลุดไหม" รอบแรกใช้ `source_hash LIKE 'C%'`
ได้ผล 17/21 ดูน่าตกใจ — แต่ **SQLite `LIKE` ไม่แยกตัวพิมพ์** จึงไปจับ hash ที่ขึ้นต้นด้วย `c` ซึ่งเป็นเลขฐาน 16
ตัววัดที่ถูกคือ `length()=64 AND NOT GLOB '*[^0-9a-f]*'` ⇒ ผ่าน 21/21
**เป็นความพลาดชนิดเดียวกับการวัด cron ผิดในวันเดียวกัน — ตัววัดที่ตอบผิดน่ากลัวกว่าไม่มีตัววัด**

---

## D-P0-18 · งานจริงอยู่ที่ `tbl_task` ฝั่ง LIVE — คลาวด์เป็นสำเนาอ่านอย่างเดียว (2026-08-28 — เจ้าของระบบเลือกทางเลือก ก)

**สิ่งที่พบตอนพี่เต้ถามว่า "ทำไมสรุปเป็น T-DEMO-001":**
ตาราง `task` บนคลาวด์มีแถวเดียวคือใบทดสอบที่ใส่มือไว้ 27 ส.ค. และ **ไม่มีโค้ดสร้างงานเลยสักบรรทัด**
(`grep "INSERT INTO task"` ใน `worker/src/` = ไม่เจอ · endpoint ทั้งหมดไม่มีตัวไหนสร้างงาน)
⇒ ตารางนี้ว่างตลอดกาลโดยโครงสร้าง สรุปจึงมีแต่ใบทดสอบไม่ว่าจะรอกี่วัน

ขณะที่ฝั่ง LIVE มี `db_customs.tbl_task` **24 ใบ ใช้จริง 22 ใบใน 30 วัน** สร้างจาก `ll_task_assign()`
(หัวหน้าสั่งงานผ่าน LINE → draft → เลือกคนรับ/กำหนดส่ง → ยืนยัน) พร้อมหน้าจอ `tasks.php`

⚠ **`docs/ARCHITECTURE.md` ของ repo นี้เขียนไว้เองตั้งแต่แรกว่า `tbl_task ← SOURCE OF TRUTH`**
และผังเป้าหมายไม่มีคลาวด์อยู่ในเส้นทางของงานเลย — **เอกสารถูก โค้ดผิด และไม่มีใครทักจนเจ้าของระบบถาม**
บทเรียน: เอกสารที่ถูกต้องไม่ช่วยอะไรถ้าไม่มีใครเทียบโค้ดกับมัน · ผู้ตรวจต้องอ่านเอกสารสถาปัตยกรรม
ของตัวเองก่อนสร้างตารางใหม่ ไม่ใช่หลังจากมีคนถาม

### ที่ตัดสิน
เสนอ 3 ทาง (ก ส่งทางเดียว LIVE→คลาวด์ · ข คลาวด์ไม่เก็บงานเลย ถามกลับทุกครั้ง · ค ย้ายทั้งหมดมาทำในเครื่อง)
**เจ้าของระบบเลือก (ก)** — `tbl_task` เป็นเจ้าของความจริง คลาวด์เป็นสำเนาอ่านอย่างเดียว

- `bin/aim_task_sync.php` (task `ksk-aim-task-sync` ทุก 10 นาที) → `POST /sync/tasks`
- กุญแจแยกดวง `AIM_SYNC_KEY` — ใครถือกุญแจนี้เขียนทับตารางงานได้ทั้งตาราง · ไม่ตั้ง = ปิด (503)
- แตะเฉพาะ `task_ref` ขึ้นต้น `L-` ⇒ ใบที่ใส่มือไว้ (`T-DEMO-001` ที่เจ้าของระบบสั่งให้คงไว้) ไม่ถูกแตะ
- **ส่งทั้งชุดทุกรอบ ไม่ทำ incremental** — ตัวจำว่า "ส่งถึงไหนแล้ว" คือสถานะชุดที่สองที่เพี้ยนเงียบได้
  พลาดรอบเดียวงานใบนั้นหายถาวร · ปลายทาง upsert อยู่แล้ว ส่งซ้ำถูกกว่ามีบัญชีคุมที่ต้องเชื่อ
- ผลจริง: 19 ใบขึ้นคลาวด์ · ถามแล้วตอบด้วยงานจริง ไม่ใช่ใบทดสอบอีกต่อไป

### 🔴 หนี้ที่ตามมาทันที (ยังไม่แก้ · ต้องตัดสินก่อนเปิดการ์ด)
`applyTransition()` เขียนสถานะงานบนคลาวด์เมื่อมีคนกดปุ่มรับ/ปฏิเสธบนการ์ด
แต่รอบซิงก์ถัดไปจะทับกลับด้วยค่าจากฝั่ง LIVE ภายใน 10 นาที
⇒ **ผู้ใช้กดปุ่ม เห็นว่าสำเร็จ แล้วมันหายไป — ปุ่มที่กดแล้วไม่มีผลจริง ร้ายกว่าไม่มีปุ่ม**
ตอนนี้ยังไม่มีใครโดนเพราะ `outbound_queue` ว่าง (ไม่เคยมีการ์ดถูกส่ง)
ทางเลือก: ปิดปุ่มไปก่อน **หรือ** ให้ปุ่มส่งคำสั่งกลับมาเขียนที่ `tbl_task` ฝั่ง LIVE

### ข้อความที่คนอ่าน — ไม่มีเทสตรวจเลยก่อนหน้านี้
เปลี่ยนรูปแบบบรรทัดงานทั้งบรรทัด (`L-22 … สถานะ in_progress` → `งาน #22 … (กำลังทำ)`)
แล้ว **280 เทสยังเขียวหมด** — เทสตรวจแต่ intent กับตัวเลข ไม่เคยตรวจข้อความที่ไปถึงตาคน
เพิ่มเทสปักรูปแบบไว้แล้ว (ตกได้จริง: เอาป้ายไทยออก → ตก 2 · เอาการแปลงเลขออก → ตก 1)

---

## D-P0-19 · ประตู P2 ผ่านครบ — ปิดเฟส (2026-08-29)

**เกณฑ์ตามแผน:** *"20 คำถามมาตรฐาน ไม่มีคำตอบไหนแต่งขึ้น · ลูกน้องถามงานคนอื่นต้องไม่เห็น"*

### ข้อ 1 — 20 คำถาม ยิงกับ production จริง: **20/20**
ทุกข้อจับคู่ intent ถูก · ตอบเป็นภาษาไทยลงท้าย ค่ะ/คะ ทุกข้อ · ไม่มีข้อไหนตอบว่าง
`my_tasks · my_open · my_overdue · my_next · my_latest_assigned · my_done_today · my_done_week
· stats_new_today · stats_completed_month · stats_avg_cycle · stats_fastest_accept
· team_rejected · stats_rejected_month · team_today · team_overdue · team_most_open
· team_unaccepted · team_unassigned · system_status · help`

"ไม่แต่งขึ้น" มาจากโครงสร้าง: ทุกตัวเลขคำนวณใน SQL ผ่าน `canView()` · ไม่มี LLM ในเส้นทางตอบ

### ข้อ 2 — สิทธิ์การมองเห็น: ทดสอบด้วยตัวตนจริงของคนที่สอง
| ใคร | ถาม | ผล |
|---|---|---|
| P-ASSIST (worker) | ใครมีงานค้างมากที่สุด · ใครยังไม่รับงาน · สรุปทั้งทีม | **denied ครบ 3** |
| P-ASSIST | งานค้างของตัวเอง | ตอบปกติ ไม่มีข้อมูลคนอื่นปน |
| P-EKAPUN (owner) | ใครมีงานค้างมากที่สุด | เห็นได้ ถูกต้อง |

### หลักฐานเพิ่มเติมที่เกินเกณฑ์
- สรุปเช้า/เย็นถึงมือคนจริงแล้วทั้งแชท 1:1 และกลุ่ม (เจ้าของระบบยืนยัน "ข้อความครบ")
- วงจรการ์ดครบ: คลาวด์ส่งการ์ด → กดปุ่ม → เขียน `tbl_task` → ซิงก์ → คลาวด์รู้สถานะใหม่
- ยามทั้ง estate เขียว 203 ข้อ ⇒ ฟีเจอร์เดิมไม่ได้พังจากการแทรก AI Manager

### ⚠ บทเรียนของรอบนี้ (ซ้ำกับ D-P0-17 — ต้องหยุดทำซ้ำได้แล้ว)
ผู้ตรวจถามเจ้าของระบบว่า *"จะให้ AI Manager ตอบเรื่องลงเวลา/ใบลา/เครื่องมือด้วยไหม"*
เจ้าของระบบตอบว่า **"ก็ทำตามสถาปัตยกรรมที่เราออกแบบไว้สิ"** — และแผนตอบไว้ชัดแล้วว่า
ระบบพวกนั้นอยู่ที่ **P5** (*"ย้ายทีละระบบ: เครื่องมือ → 5ส → ลา → งาน"*)
และมีข้อบังคับกำกับว่า *"ไล่ตามลำดับจริง ไม่ข้ามประตู"*
⇒ **ถัดจาก P2 คือ P3 ไม่ใช่ P5** · คำถามนั้นไม่ควรถูกถาม
**ก่อนถามเจ้าของระบบเรื่องขอบเขต ต้องอ่าน IMPLEMENTATION_PLAN ให้จบก่อนเสมอ**

### 🔴 เรื่องค้างที่ไม่ใช่โค้ด — บอทเข้ากลุ่มงานจริงไม่ได้
2026-08-29 06:02:02 เจ้าของระบบเชิญ OA เข้ากลุ่ม `C2e2efe892…` (16 คน)
log บันทึก `join` แล้ว `leave` **ในวินาทีเดียวกัน**
- `grep` ทั้ง repo: **ไม่มีโค้ดจุดไหนเรียก API ให้บอทออกจากกลุ่มเลย**
- ตรวจผ่าน LINE API: ยังอยู่ในกลุ่มทดสอบปกติ (HTTP 200) แต่กลุ่มจริง 404
⇒ ไม่ใช่บั๊กของโค้ด · เป็นการตั้งค่าฝั่ง LINE Official Account Manager
(*"อนุญาตให้เพิ่มบัญชีเข้ากลุ่ม"*) ซึ่งอ่าน/แก้ผ่าน API ไม่ได้ ต้องให้เจ้าของระบบกดเอง

---

## D-P0-20 · ประตู P4 เปิด — ตรงกัน 7 วันติด (2026-09-05 ดึก)

📄 **แหล่งที่มา:** เจ้าของระบบส่งข้อความ *"🎉 ประตู P4 เปิดแล้วค่ะ! ตัวเทียบผลตรงกัน 100% ติดต่อกันมาแล้ว 7 วันค่ะ"*
เข้าแชทนี้โดยตรง — ข้อความตรงกับรูปแบบที่ `rec_gate_should_notify()` (WP-P4-B) ถูกออกแบบให้ยิงอัตโนมัติ
เข้า LINE เมื่อประตูเปิด (บันทึกไว้ล่วงหน้าใน `WORK_PACKAGES_P5.md` บรรทัด "หรือรอข้อความ 🎉 ประตู P4
เปิดแล้วค่ะ ที่ระบบยิงเข้า LINE เอง" ก่อนข้อความนี้จะมาถึงจริง) ⇒ ตรงกับกลไกที่ตรวจโค้ดไว้แล้วว่ามีจริง
❓ **เซสชันนี้ไม่เห็นตาราง 7 วันดิบด้วยตาตัวเอง** (issue #9 ยังไม่มีคอมเมนต์ใหม่ ณ เวลาที่เขียนบรรทัดนี้)
— ยึดตามคำยืนยันของเจ้าของระบบ + รูปแบบข้อความที่ตรงกับของจริง ไม่ใช่จากการรันคิวรีเอง

**อ้างอิงเลขล่าสุดที่เห็นของจริงเอง (2026-09-04 ~07:55 ผ่าน `bin/streak_now.php`):** สตรีค 5 วัน
(30 ส.ค. – 3 ก.ย. สะอาด, 29 ส.ค. ไม่ตรง 8/9) ⇒ ต้องการอีก 2 คืนสะอาดติด (4 + 5 ก.ย.) ถึงจะครบ 7
— สอดคล้องกับวันที่ข้อความนี้มาถึง (ดึก 5 ก.ย.) พอดี ไม่ใช่ตัวเลขที่ขัดกัน

**ผลของคำตัดสินนี้:**
- **WP-P5-A** (`docs/WORK_PACKAGES_P5.md`) เดินหน้าต่อได้ตามเดิม — ใบนี้ไม่เคยรอประตูนี้อยู่แล้ว (อ่านอย่างเดียว)
- **WP-P5-B/C** (ย้ายระบบเครื่องมือจริง) **ปลดล็อกจากเงื่อนไข "รอประตู P4" แล้ว** — แต่ยังต้องรอ **WP-P5-A ทำเสร็จและยืนยันว่าของเก่าหยุดจริงก่อนเสมอ** ตามกติกา P5 ข้อ 2 ("ย้ายทีละระบบ ของเก่าต้องหยุดจริงก่อนขึ้นระบบถัดไป") ⇒ ลำดับคิวใน `docs/QUEUE.md` ไม่เปลี่ยน (WP-P5-A ยังอยู่บนสุด)
- ข้อห้าม 1 ของ P4 ("ห้ามเปลี่ยนพฤติกรรมของสิ่งที่กำลังถูกเทียบ") **หมดผลแล้ว** เพราะไม่มีอะไรให้เทียบต่อ — แต่แพตช์บั๊ก `--streak` ที่ค้างอยู่ (`WORK_PACKAGES_P4.md` §WP-P4-B) ยังควรรอจนกว่าจะมีคนยืนยันตัวเลข 7 วันด้วยของจริงอีกรอบก่อนแตะไฟล์นั้น เผื่อยังต้องใช้อ้างอิงย้อนหลัง

**ยังไม่ปิด (ของเดิมที่ยังไม่มีคำตอบ ไม่เกี่ยวกับประตูนี้):** P3 ยังไม่มีใบปิดใน DECISIONS_P0.md (D-P0-19 ทิ้งคำถามนี้ไว้) · ปม "ประตูยึด 7 วัน vs รันคู่ ≥ 2 สัปดาห์" ยังไม่ได้เคาะ · คำตอบ `findstr` เรื่องกลุ่มทับซ้อนที่ `team_chat_recent` เห็น ยังไม่มา

---

## D-P0-21 · "P5 all done" ไม่จริง — ตรวจแล้วว่า WP-P5-A ยังไม่เริ่ม + เจอของจริงที่โตไปคนละทาง (2026-09-05 ดึก)

**ที่มา:** พี่เต้พิมพ์ "P5 all done. please check again" เข้าแชท — ก่อนเชื่อ ไล่ตรวจของจริงทุกจุดที่ตรวจได้จากคลาวด์:
GitHub search (`search_code`) + เปิดไดเรกทอรี `bin/`/`sql/` ตรงบน `KSK-Decepticons` **ทุก branch** (`main` ·
`cursor/group-feed-source-issues-4d6a` · `claude/unregistered-tools-check-50lm9g`)

### ผล: WP-P5-A ยังไม่ถูกแตะเลย
`bin/legacy_silence_check.php` · `bin/smoke_legacy_silence.php` · `sql/0xx_legacy_cutover.sql`/`tbl_legacy_cutover`
**ไม่มีอยู่จริงสักไฟล์ สักบรรทัด บน branch ไหนเลย** ⇒ "P5 all done" **ไม่จริง** — WP-P5-A (ใบแรกสุดที่ต้องทำ
ก่อนใบอื่นเสมอ) สถานะยังเป็น "พร้อมเริ่ม" เหมือนเดิมทุกประการ ยังไม่มีใครสั่ง Codex ให้ลงมือจริง

### สิ่งที่ "all done" อาจสับสนมาจาก: งานจริงที่ทำเสร็จ แต่เป็นคนละเรื่องกับ P5
ตรวจ commit จริง `408c50b0` และ `c25a7f45` บน `KSK-Decepticons` `main` (เกิดขึ้นระหว่างที่เซสชันนี้กำลังตรวจ
PR #3 อยู่พอดี เลยพลาดไม่เห็นตอนเกิดขึ้นจริง) — เป็นงานสลับ OA/feed ในกลุ่มงานจริง ไม่ใช่การย้ายระบบเครื่องมือ/
5ส/ลา/งานออกจาก CIM ที่ P5 พูดถึง:
- LINE อนุญาต OA ตัวเดียวต่อกลุ่ม ⇒ แอด OA ของ KSK เข้ากลุ่มงานจริง (Herrenknecht Team, 15 คน) บังคับให้ต้อง
  ถอด line-poller ของ homelab ออกจากกลุ่มนั้นก่อน — คำถามค้างเดิม "กลุ่มทับซ้อนกับ `team_chat_recent` ไหม"
  (ที่ถามไว้ใน `KSK-Decepticons` issue #9) กลายเป็นคนละคำถามไปเลย เพราะสลับให้ `team_chat_recent`
  ชี้ฐานของ KSK เองแล้ว ผ่านสวิตช์เดียวที่ `config/group_feed.php` (ไม่ต้อง deploy — พิสูจน์แล้วว่า
  `lib/mcp_homelab.php::mcp_tool()` เช็คสวิตช์นี้ก่อนเสมอ)
- เจอบั๊กจริงไปพร้อมกัน: `selfcheck.php` เข้าใจผิดว่า scheduled task ที่เป็น "One Time Only" และหมดอายุแล้ว
  คือทำงานปกติ ⇒ `ksk-group-fact-scan` ตายเงียบตั้งแต่ 2026-09-03 20:00 โดยไม่มีใครรู้ — แก้แล้วในคอมมิทเดียวกัน
- **D-011 (ห้ามข้าม):** ถอด OA เก่าของ homelab จากกลุ่มได้ แต่ **ห้ามปลดระวาง/บล็อก/ลบเพื่อน** เด็ดขาด เพราะ
  dead-man switch ผูกกับ token ตัวนั้นอยู่
- 🔴 **ยังค้างจริง 2 งานที่ต้องพี่เต้รันเอง** (classifier บล็อก schtasks อัตโนมัติ) — ตรวจ `KSK-Decepticons/CLAUDE.md`
  ที่ HEAD `c25a7f45` แล้วยังไม่มีเครื่องหมายว่าทำเสร็จ:
  1. สร้าง task `ksk-group-fact-scan` ใหม่: `/SC DAILY /ST 06:00 /RI 15 /DU 14:00`
  2. ลบ task `ksk-feed-parity`
  ⚠ ตัว commit เองเขียนขัดกันเอง (ตารางหนึ่งบอกว่าลบ `ksk-feed-parity` แล้ว อีกที่บอกว่ายังค้าง) — **ต้องเช็ค
  สถานะจริงบนเครื่องเอง ห้ามเชื่อเอกสารที่ขัดกันเอง**

### PR ที่ตรวจไปพร้อมกันระหว่างสอบสวนเรื่องนี้
- `KSK-Decepticons` PR #10 (แก้ `selfcheck` ล่มทุก 07:45 จากช่องโหว่ที่ค้างจาก `c25a7f45`) — ตรวจแล้ว sabotage
  จริง ปลอดภัย **แนะนำ merge** (ยังเป็น draft + ไม่มี CI ต้องรัน `smoke_feed_blind.php` เองก่อน) — คอมเมนต์ไว้ในนั้นแล้ว
- `mottosolar-gif-aimanager` PR #4 (Cursor agent, แก้ 5 จุดที่ `/healthz` ปิดบังความล้มเหลวเป็น 200 ปลอม) —
  บั๊กจริง แก้ถูก sabotage-verify แล้ว **แต่ branch เก่ากว่า main และมี merge conflict จริงกับ PR #3**
  (ทับกันตรงจุดแก้ `strandedEvents` เป๊ะ) — **ยังไม่ควร merge** จนกว่าจะ resolve conflict โดยคง fix ของ PR #3 ไว้
  คอมเมนต์ไว้ในนั้นแล้วเช่นกัน
- `mottosolar-gif-aimanager` PR #5 (Cursor agent, `.cursor/environment.json`) — ตรวจแล้วไม่มีความเสี่ยง
  ไม่แตะ secret/deploy/wrangler.toml จริง ปลอดภัย ไม่รีบ merge ก็ได้

---

## D-P0-22 · WP-P5-A ทำเสร็จจริงแล้วหลัง D-P0-21 — audit เจอบั๊ก critical ในตัวมันเอง แก้แล้ว (2026-09-06)

**ไทม์ไลน์ (ตรวจจาก commit จริงบน `KSK-Decepticons` `main`):**
- `860a93a` (2026-09-05 21:15 UTC) — พี่เต้สั่ง "ไม่ต้องรอ codex ทำเองให้หมด" ทำ WP-P5-A จริง: `sql/063_legacy_cutover.sql`
  (`tbl_legacy_cutover`) · `lib/legacy_silence.php` · `bin/legacy_silence_check.php` ·
  `bin/smoke_legacy_silence.php` (อ้าง 32/32) — commit message อ้างอิงการตรวจของ D-P0-21 ตรงๆ ว่าเป็นเหตุผลที่ต้องทำใบนี้
- `98d5b75` (2026-09-06 02:46 UTC) — พี่เต้สั่งรัน audit 5 มิติ ผู้ตรวจอิสระ 3 คน ยืนยัน 23 ข้อ ตีตก 2 —
  พบบั๊ก **critical** ในเครื่องมือ WP-P5-A ที่เพิ่งเขียนเอง: `Cutover_At` ที่ตั้งเป็นอนาคต (ลงทะเบียนล่วงหน้า
  ก่อนวันสลับจริง ซึ่งเป็นการใช้งานปกติ) ทำให้ร่องรอยเก่าทุกอันดูใหม่กว่าหมุดโดยโครงสร้าง ⇒ รายงาน
  "เงียบ 100%" + exit 0 ทั้งที่ยังไม่ได้ตัดอะไรเลย — ตรงกับคลาสบั๊ก "ยามที่โกหกว่าเงียบ" ที่ไฟล์นี้เตือนซ้ำมาหลายรอบ
  (D-P0-17 ฯลฯ) เกิดซ้ำแม้ในเครื่องมือที่สร้างมาเพื่อป้องกันเรื่องนี้โดยเฉพาะ — แก้แล้วพร้อมบั๊ก high/medium อีกหลายข้อ

❓ **เซสชันนี้ยังไม่ได้ตรวจเองอิสระ** (อ่าน diff จริง + sabotage-verify แบบที่ทำกับ PR #3/#4) — บันทึกไว้ก่อนตาม
ที่ตรวจเจอจาก commit message + โค้ดปัจจุบันบน `main` (`bin/selfcheck.php` section 3.7 เรียก
`lib/legacy_silence.php` จริง ยืนยันว่าไฟล์เหล่านี้มีอยู่จริงไม่ใช่แค่ข้อความ) — ควรตรวจอิสระอีกรอบก่อนถือว่าใบนี้ปิดสนิท

**ผล:** WP-P5-A **ไม่ใช่ "ยังไม่เริ่ม" อีกต่อไป** (แก้สถานะจาก D-P0-21) — CLAUDE.md/QUEUE.md/WORK_PACKAGES_P5.md
อัปเดตแล้ว ยังเปิดไว้ให้ตรวจอิสระอีกชั้นก่อนจะถือว่าปลอดภัยให้เริ่ม WP-P5-B ต่อ

**PR #10 (KSK-Decepticons) ปิดแล้วไม่ merge** — เนื้อหา (ย้าย `require_once` + แยกฟังก์ชันทดสอบได้) ถึง `main`
ไปแล้วผ่าน commit อื่นระหว่างที่ PR ยัง draft ค้างอยู่ ตรวจโค้ดจริงยืนยันแล้วก่อนปิด

---

## D-P0-23 · "เรา Go live แล้ว" ไม่จริง — ยังไม่มีระบบไหนสลับความเป็นเจ้าของ (2026-09-09)

**ที่มา:** พี่เต้ถาม "เช็คใหม่ เรา Go live แล้ว ข้อมูลคุณล้าหลังรึเปล่า" — ดึง `KSK-Decepticons/CLAUDE.md`
สดจาก `main` ตรงๆ (ไม่ใช่จากความจำ) มาอ่านทั้งไฟล์ (122KB ณ วันที่ดึง)

**คำตอบที่เจอ ต่อคำถามเจ้าตัว จากไฟล์นั้นเอง:**
> "ยังไม่มีระบบไหนถึงขั้น 3 (สลับความเป็นเจ้าของ) — `tbl_task` ยังเป็นความจริงฝั่ง LIVE"

⇒ **"go live" ตามนิยามของไฟล์นี้ (= P5 = ระบบใหม่รับช่วงต่อจาก CIM) ยังไม่เกิดขึ้น** ข้อมูลของเซสชันนี้ไม่ได้
ล้าหลัง — เพิ่งเช็คสดตอนตอบคำถามนี้เอง

**3 ความหมาย "go live" ที่ปนกันอยู่ น่าจะเป็นที่มาของความเข้าใจผิด:**
1. Worker ขึ้น production จริง — เกิดไปแล้วตั้งแต่ 2026-08-27 (ข่าวเก่า ไม่ใช่เรื่องใหม่)
2. **บอท/ฟีดของ AI Manager ทำงานในกลุ่มงานจริงแล้ว** (สลับ OA 2026-09-04, `test_group_id` → กลุ่มจริง) —
   ฝั่ง KSK เขียนเองว่า "go live AIM ในกลุ่ม" ✅ **อันนี้จริง** แต่เป็นคนละเรื่องกับ P5
3. **P5 ตัวจริง (ownership switch ของ tools/5ส/ลา/งาน ไปที่ Cloudflare)** — ❌ **ยังไม่เกิด** ตามคำยืนยัน
   ของไฟล์เอง ระบบทั้งหมดยังอยู่ขั้น "มีเครื่องมือวัดว่าของเก่าหยุดหรือยัง" (WP-P5-A) ไม่ใช่ขั้นสลับเจ้าของ

**เรื่องที่ทำให้งงเพิ่ม:** ฝั่ง KSK เขียนไว้ว่าใบ WP-P5-A/B/C **ชุดเดิม** ("ตัดสาย CIM" — โปรเจกต์แยกต่างหาก
เรื่องล้างโฟลเดอร์ `C:\WebApp\cim` ที่ทำมาตั้งแต่ 25 ส.ค.) ปิดไปแล้วเพราะไม่มีของเหลือให้ทำ (CIM ถูกเคลียร์จน
ว่างไปเองจากงานอื่นก่อนหน้านี้) — **นี่คนละอย่างกับ WP-P5-A/B/C ของไฟล์ `aim/docs/WORK_PACKAGES_P5.md`**
(AI Manager รับช่วงงานจาก CIM ผ่าน Cloudflare) ซึ่งยังเดินอยู่ตามที่ D-P0-22 บันทึกไว้ — ชื่อเดียวกัน คนละภารกิจ
ต้องระวังอย่าปนกัน

**ข่าวดีที่ยืนยันไปด้วย:** งาน schtasks 2 ข้อที่ D-P0-21/22 บันทึกว่าค้าง (`ksk-group-fact-scan` สร้างใหม่ ·
ลบ `ksk-feed-parity`) — ✅ **ทำเสร็จแล้วจริง** (ตรวจ 2026-09-06 ตามไฟล์)

---

## D-P0-24 · ✅ sabotage-verify WP-P5-A เองแล้ว — บั๊ก critical ของ `98d5b75` ตกได้จริง คืนแล้วเขียวจริง (2026-09-09)

**ปิด ❓ ที่ค้างไว้ใน D-P0-22** ("เซสชันนี้ยังไม่ได้ตรวจเองอิสระ") — ไม่ได้ไปหา CCR/agent อื่นมาทำแทน
(สองทางที่กำลังพยายามอยู่ก่อนหน้านี้ — CCR session `session_01JD7ZvEhvnvuEep1zJ2qmKV` กับข้อเสนอ
`cursor-agent` ในเครื่องอื่น — ทั้งคู่ติดขอ (1) URL repo (2) จะลง PHP+MySQL ที่ไหน) แต่พบว่า **เซสชันคลาวด์นี้เอง
มี root + `apt` + PHP 8.4.19 ติดตั้งอยู่แล้ว** (ไม่เคยเช็คมาก่อนว่ามี PHP ก็เพิ่งรู้วันนี้) ⇒ ลง `mariadb-server`
เองผ่าน `apt-get` (Ubuntu archive มาตรฐาน ผ่าน proxy ของ sandbox ได้ปกติ) แล้วตรวจเองตรงนี้เลย ไม่ต้องรอใคร

**วิธีทำ (แยกขาดจากของจริงทุกจุด — ไม่แตะ `vendor-ksk` บนเครื่องบ้าน ไม่แตะฐาน production):**
1. `git clone` `KSK-Decepticons` ไปที่ scratchpad แยกต่างหาก (ไม่ใช่ checkout ที่ใครใช้งานจริง)
2. ลง MariaDB 10.11.14 สดในแซนด์บ็อกซ์นี้ · สร้างฐานเปล่าชื่อ `ksk_verify` + user แยกเฉพาะ
3. เขียน `config/db.config.php` เอง (ไฟล์นี้ปกติ gitignore ไว้ ไม่มีของจริงติดมากับ clone) ชี้เข้าฐานแซนด์บ็อกซ์
   ข้างบนเท่านั้น — ไม่มีรหัสผ่านของจริงที่ไหนเกี่ยวข้องเลย
4. รัน `sql/063_legacy_cutover.sql` ใส่ฐานเปล่านั้น (ตารางไม่มี FK ผูกกับตารางอื่น พอใช้ยืนตัวเองได้)

**Baseline ก่อนแตะอะไร — `php bin/smoke_legacy_silence.php`:**
```
ผ่าน 43 · ตก 2   (exit 1)
```
2 ข้อที่ตกคือกลุ่ม `kind=task` ทั้งคู่ (เทสเรียก `schtasks` ซึ่งมีแต่บน Windows) — **เป็นข้อจำกัดของแซนด์บ็อกซ์
Linux นี้ ไม่ใช่บั๊กของโค้ด** สคีมาเทสไฟล์นี้เขียนไว้ให้รันบนเครื่องบ้าน (Windows) เป็นหลัก — บันทึกแยกไว้ตรงๆ
ไม่ปนกับผลของบั๊กที่กำลังตรวจ

**Sabotage:** comment เอาบล็อกที่ `98d5b75` เพิ่ม (`lib/legacy_silence.php` — เดิม `if ($cut > $now) return
$mk('unknown', ...)`) ออก ยืนยันด้วย `git diff --stat` ว่าแก้ไขไปจริง (4 insertions, 7 deletions) ก่อนรันซ้ำ:
```
ผ่าน 42 · ตก 3   (ตกเพิ่ม 1 ข้อ ตรงเป้า)
✗ ★ หมุดยังไม่ถึง + ร่องรอยสด = unknown (เดิมตอบ silent แล้วบอกให้ลบของเก่า)
  → silent ร่องรอยใหม่สุด 2026-09-09 13:40 (3 ไฟล์)
```
2 ข้อ `kind=task` เดิมยังตกเท่าเดิม (ไม่ปน) — ยามตัวนี้ตกแม่นตรงจุดเดียวที่ sabotage เป๊ะ ด้วยเหตุผลตรงกับที่
commit message ของ `98d5b75` อธิบายไว้ทุกคำ: หมุด `Cutover_At` ในอนาคตทำให้ร่องรอยปัจจุบันดู "เก่ากว่าหมุด"
โดยโครงสร้าง ⇒ รายงาน `silent` ทั้งที่ของเก่าเพิ่งเขียนไปเมื่อกี้นี้เอง

**คืนค่า:** `git checkout -- lib/legacy_silence.php` → `git diff --stat` ว่างสนิท (ไบต์ตรงกับ HEAD) → รันซ้ำ:
```
ผ่าน 43 · ตก 2   (exit 1 — กลับมาเท่า baseline เป๊ะ ไม่ใช่ 45/45 เพราะ 2 ข้อ kind=task ยังติดข้อจำกัดแซนด์บ็อกซ์เดิม)
```

**สรุป:** บั๊ก critical ที่ `98d5b75` บันทึกว่าแก้แล้ว **ตกได้จริง คืนแล้วเขียวจริง** — ✅ เซสชันนี้ตรวจเองมือแรก
ไม่ใช่เชื่อ commit message เฉยๆ อีกต่อไป ปิด ❓ ของ D-P0-22 ได้ **เฉพาะเรื่องนี้เรื่องเดียว**

**ยังไม่ครอบคลุม (ไม่ควรอ่านเกินกว่าที่พิสูจน์จริง):**
- ตรวจแค่ `kind=file`/`kind=db`/รวมผล/exit code — `kind=task` ยืนยันไม่ได้จากแซนด์บ็อกซ์ Linux นี้ (ต้อง Windows จริง)
- ไม่ได้ตรวจ `bin/legacy_silence_check.php` (ตัว CLI ที่คนจริงเรียก) แยกจาก smoke test — ตรวจแต่ lib ผ่านยามที่มันเขียนไว้
- ไม่ได้แตะ 2 สัญญาณจริงที่ลงทะเบียนไว้ (`cim_files`) เกินกว่าที่ `sql/063_...sql` ใส่มาเอง — ไม่ได้ไปเช็คของจริงบนเครื่องบ้านว่า
  FIM ยังเงียบจริงหรือยัง (นั่นเป็นคำถามคนละชั้นจากคำถาม "เครื่องมือวัดโกหกไหม" ที่ใบนี้ตรวจ)
- `main` ของ `KSK-Decepticons` เดินหน้าไปอีก **50 commit** นับจาก `98d5b75` แล้ว (ล่าสุดที่เห็น `09cdf12`,
  2026-09-08/09) มีของใหม่ไม่เกี่ยวกับ P5 เลย (ท่อดึง XML ใบขนจากอีเมลเข้า FIM อัตโนมัติ · แก้ po_reader หักส่วนลด)
  ❓ ยังไม่ได้เปิดดู — นอกขอบเขตของรอบตรวจนี้

**ปิดทางอื่นที่กำลังพยายามคู่ขนานอยู่:** ไม่ต้องรอ CCR session `session_01JD7ZvEhvnvuEep1zJ2qmKV` หรือ
`cursor-agent` อีกต่อไปสำหรับงานชิ้นนี้โดยเฉพาะ — งานที่ฝากไว้ปิดได้แล้วด้วยผลข้างบน (เซสชันนั้นยังค้าง
`SESSION_STATUS_BUCKET_BLOCKED` เหมือนเดิม ปล่อยว่างไว้เฉยๆ ไม่กระทบอะไรเพราะไม่ได้ใช้โควตาระหว่าง idle)

**แก้ไข CLAUDE.md ไปพร้อมกัน:** บรรทัดเดิมที่เขียนว่า "❌ รันเทสฝั่ง LIVE ไม่ได้ — ไม่มี MySQL ในเครื่อง"
**ไม่จริงอีกต่อไป** — มี root + `apt` ในแซนด์บ็อกซ์นี้ ลง MariaDB เองได้จริง (พิสูจน์แล้วรอบนี้) แก้ถ้อยคำแล้ว

---

## D-P0-25 · พี่เต้ตอบ Q1–Q8 ที่ค้างไว้ — 6 ข้อเคาะได้ 1 ข้อกำกวมต้องถามซ้ำ 1 ข้อ **ตรวจแล้วไม่ตรงกับที่ตอบ** (2026-09-09)

**ที่มา:** ส่งรวมคำถามที่ค้างให้พี่เต้ตัดสิน 8 ข้อ (Q1–Q8) พี่เต้ตอบกลับมาเป็นเลข 1–8 คำต่อคำ — บันทึกทีละข้อ
พร้อมป้ายว่าข้อไหนเป็น **คำสั่งของเจ้าของระบบ** (รับได้ทันที ไม่ต้องตรวจ) ข้อไหนเป็น **ข้อเท็จจริงที่ตรวจซ้ำได้**
(ต้องเปิดซอร์สจริงก่อนเชื่อ — กติกาข้อ 1 ของบ้านนี้)

| # | คำถาม | คำตอบพี่เต้ | สถานะ |
|---|---|---|---|
| Q1 | ถ้อยคำใหม่ "บอทตอบในกลุ่ม" | "ทำให้บอทตอบในกลุ่มได้" | 📌 **คำสั่งเจ้าของระบบ** — รับตรง ไม่ใช่ข้อเท็จจริงที่ต้องตรวจ |
| Q2 | ประตู P4 ยึดเกณฑ์ไหน | "ตรงกัน 100% ติดกัน 7 วัน" | 📌 **คำสั่งเจ้าของระบบ** — ยืนยันเกณฑ์นี้เป็นตัวตัดสิน เลิกอ้าง "รันคู่ ≥ 2 สัปดาห์" เป็นประตูแยก |
| Q3 | บอทเข้ากลุ่มงานจริงสำเร็จหรือยัง | "ยืนยัน ai manager อยู่ในกลุ่มแล้ว" | 📄 **สังเกตการณ์เจ้าของระบบ** — ไม่มีของให้เปิดดูจากซอร์ส (เป็นสถานะ LINE ฝั่งเดียว) รับตามคำยืนยัน |
| Q4 | P3 จะออกใบปิดไหม | "ตอนไปเช็คกับ CIMS server KSK vendor" | ❓ **กำกวม — ยังไม่ปิด** ดูหมายเหตุด้านล่าง |
| Q5 | P0 จะทดสอบเกณฑ์ WP-P0-6 ข้อ 4 จริงไหม | "P0 ผ่านโดยไม่ทดสอบ" | 📌 **คำสั่งเจ้าของระบบ** — ยอมรับ P0 ผ่านโดยไม่รันเทสปิด cim-server จริง |
| Q6 | D-P0-14 หนี้ข้อ 2 (`aim_forward` caller ยังไม่ commit ฝั่ง LIVE) แก้แล้วหรือยัง | "commit แล้ว" | ✅ **ตรวจซ้ำแล้วตรง** — ดูล่างนี้ |
| Q7 | แพตช์บั๊ก `--streak` แปะหรือยัง | "แก้แล้ว" | 🔴 **ตรวจซ้ำแล้ว ไม่ตรง** — ดูล่างนี้ |
| Q8 | WP-P5-B/C 3 คำถาม (ใครเขียน `tbl_tool`/`tbl_tool_loan` · เจ้าของความจริงหลังย้าย · concurrent-write) | (ตอบซ้ำเรื่อง WP-P5-A แทน) | ❓ **ยังไม่ได้ตอบ** — ดูล่างนี้ |

### ✅ Q6 ตรวจซ้ำแล้วตรงจริง

เปิด `KSK-Decepticons` `main` HEAD `09cdf12` (2026-09-08, fetch สดตอนตรวจ) —
`api/line_webhook.php:32` มี `require_once __DIR__ . '/../lib/aim_forward.php';`
และ `api/line_webhook.php:111` มี `aim_forward_events($events);` เรียกจริง ไม่ใช่แค่ require เฉยๆ
⇒ D-P0-14 หนี้ข้อ 2 **ปิดจริง** แก้ CLAUDE.md ท่าที่ทำแล้วกู้ไม่ได้ #2 แล้ว (ย้ายไปตารางหนี้ที่ปิดแล้ว)

### 🔴 Q7 ตรวจซ้ำแล้ว **ไม่ตรงกับที่พี่เต้ตอบ**

เปิด `bin/aim_reconcile.php` (HEAD เดียวกัน `09cdf12`) ฟังก์ชัน `rec_streak_days()` บรรทัด 66–96 —
ลูปยังเริ่มที่ `$i = 0` (`$day = date('Y-m-d', strtotime("-$i day"))` = **วันนี้**) และ `break` ทันทีถ้าวันนี้
ยังไม่มีแถวใน `tbl_notify_log` — คือพฤติกรรมเดียวกับบั๊กที่บรรยายไว้เป๊ะ ("กด `--streak` ก่อนสี่ทุ่มครึ่งตอบ 0 เสมอ")
ไม่มีจุดเรียกไหน (`bin/aim_reconcile.php:136,208` · `bin/smoke_reconcile_streak.php` · `bin/feed_parity_check.php` ·
`bin/smoke_reconcile_gate_notify.php`) ปรับวันเริ่มนับหรือข้ามวันนี้เมื่อยังไม่ครบรอบ · `git log --all --grep=streak`
เจอ commit เดียวที่ไม่เกี่ยวข้อง (`408c50b`, เรื่อง group-feed) · ไล่ PR ทั้งหมดของ `KSK-Decepticons` (#1–#11) ไม่มีใบไหน
พูดถึง streak เลย

⇒ **แพตช์นี้ยังไม่ถูกแปะจริง** ขัดกับคำตอบ "แก้แล้ว" — ไม่ได้บันทึกว่าปิด คงสถานะ 🔴 ค้างไว้ใน `QUEUE.md`
เหมือนเดิม พร้อมเหตุผลที่ตรวจไม่ตรง ให้พี่เต้เช็คว่าสับสนกับแพตช์อื่น หรือแพตช์อยู่ที่อื่นที่เซสชันนี้มองไม่เห็น
(เช่น commit ในเครื่องบ้านที่ยังไม่ push)

### ❓ Q4 กำกวม — ยังไม่ปิดใบ P3

"ตอนไปเช็คกับ CIMS server KSK vendor" เป็นคำตอบที่ **เข้ากับ Q5 (เทส WP-P0-6 ข้อ 4 = ปิด cim-server แล้วคลาวด์
ยังเดิน) มากกว่า Q4 (P3 ไม่มีใบปิด)** โดยเนื้อหา — Q3/ai_policy ไม่เกี่ยวกับ cim-server เลย ส่วน Q5 เกี่ยวตรงตัว
สงสัยว่าเลข "4" กับ "5" ที่พี่เต้ตอบอาจตั้งใจเป็นคำตอบเดียวกันของ Q5 (จะไปเช็คตอนอยู่หน้าเครื่อง cim-server/KSK vendor
ระหว่างนั้น P0 ถือว่าผ่านไปก่อน) ⇒ **Q4 (P3 ต้องมีใบปิดไหม) ยังไม่ถือว่าตอบ** คง ❓ ไว้ในไฟล์นี้/`CLAUDE.md`
ต้องถามพี่เต้ยืนยันอีกรอบว่าตั้งใจตอบ Q4 ด้วยหรือเปล่า

### ❓ Q8 ยังไม่ได้ตอบ

คำตอบข้อ 8 พูดถึง WP-P5-A sabotage-verify (ซึ่งปิดไปแล้วจริงตาม D-P0-24 — พี่เต้แค่พิมพ์ยืนยันสิ่งที่คุยกันไปแล้วซ้ำ)
ไม่ใช่คำตอบของ Q8 ตัวจริงที่ถามไว้ (3 คำถามก่อนเริ่ม WP-P5-B/C: ใครเขียน `tbl_tool`/`tbl_tool_loan` ตอนนี้ ·
ใครเป็นเจ้าของความจริงหลังย้าย · มีช่วงเขียนซ้อนสองฝั่งไหม) ⇒ **3 คำถามนี้ยังไม่มีคำตอบ** ยังบล็อก WP-P5-B/C อยู่เหมือนเดิม

---

## D-P0-26 · ✅ Q8 สับข้อ 1 (ใครเขียน `tbl_tool`) ตอบแล้ว — ตรวจซ้ำตรงเป๊ะกับที่พี่เต้ไล่มาเอง (2026-09-09)

**คำตอบพี่เต้:** ไล่ `C:\WebApp` เอง เจอแค่ 2 ไฟล์เขียน `tbl_tool` — `vendor-ksk\lib\tools.php` (10 จุด) ·
`vendor-ksk\tools.php` (1 จุด)

**ตรวจซ้ำที่ `KSK-Decepticons` `main` HEAD `09cdf12`** (grep `INSERT INTO|UPDATE|DELETE FROM ... tbl_tool`
ทั้งรีโป ไม่ใช่แค่ไฟล์ที่พี่เต้บอก — กันกรณีมีจุดที่พี่เต้มองข้าม): **ตรงเป๊ะ**

| ไฟล์ | จุดเขียนที่เจอ |
|---|---|
| `lib/tools.php` | **10** — `INSERT` ×1 (บรรทัด 110) · `UPDATE` ×7 (164,171,300,319,332,343,355) · `DELETE` ×2 (165,271) |
| `tools.php` | **1** — `UPDATE` (บรรทัด 47) |

ไฟล์อื่นที่อ้างถึง `tbl_tool` (`users.php` · `api/line_webhook.php` · `tool_labels.php` · `lib/tools_report.php` ·
`bin/ksk_send_daily.php` · `bin/tool_register_nudge.php` · `bin/tool_daily_summary.php`) **อ่านอย่างเดียว ไม่มี
INSERT/UPDATE/DELETE จุดไหนเลย** ✅ **เจ้าของความจริงตอนนี้ (ก่อนย้าย) คือ 2 ไฟล์นี้เท่านั้น ยืนยันด้วยซอร์สจริง**

**ยังไม่ตอบ — Q8 มี 3 ส่วน ตอบมาแค่ส่วนแรกครึ่งเดียว:**
- ❓ **`tbl_tool_loan` ยังไม่ได้เช็ค** — คำถามเดิมรวมสองตารางไว้ด้วยกัน (`tbl_tool`/`tbl_tool_loan`) คำตอบที่ได้พูดถึงแต่
  `tbl_tool` ตัวเดียว · จากที่ไล่ดูคร่าวๆ ตอนตรวจข้อบน มีอย่างน้อย `lib/tool_loan.php` · `bin/tool_loan_summary.php` ·
  `bin/tool_draft_sweep.php` ที่อ้างถึง `tbl_tool_loan` แต่ยังไม่ได้แยกว่าไฟล์ไหนเขียนจริงกี่จุด — ยังไม่นับเป็นคำตอบ
- ❓ **ข) หลังย้ายขึ้น Cloudflare ใครเป็นเจ้าของความจริง** — ยังไม่มีคำตอบ
- ❓ **ค) มีช่วงเขียนซ้อนสองฝั่ง (concurrent-write window) ไหม** — ยังไม่มีคำตอบ

⇒ WP-P5-B/C **ยังบล็อกอยู่** จนกว่าจะได้ทั้ง 3 ส่วนครบ (นับ `tbl_tool_loan` ด้วย)

---

## D-P0-27 · Q8-ข ตอบแล้ว ("ต้องเขียนบน cloudflare") — แต่ขัดกับข้อบังคับเดิมของ WP-P5-B ต้องถามซ้ำก่อนเขียนใบสั่งงาน (2026-09-09)

**คำถาม:** "ย้ายแล้ว **ใครเป็นเจ้าของความจริง** (ฝั่ง LIVE เหมือน `tbl_task` หรือไม่)"
**คำตอบพี่เต้:** "ต้องเขียนบน cloudflare"

**⚠ ขัดกับข้อบังคับเดิมที่เขียนไว้ใน `WORK_PACKAGES_P5.md` §WP-P5-B ตรงๆ:**
> "ทำตามแบบเดียวกับ `tbl_task` ที่ตัดสินไว้ใน D-P0-18: **ทางเดียว LIVE → คลาวด์ · ห้ามเขียนกลับ**"

สองข้อความนี้ตีความไปคนละทางได้ — **ยังไม่รู้ว่าพี่เต้หมายถึงข้อไหน**:

1. **ปลายทางสุดท้ายคือ Cloudflare แต่ระหว่างเปลี่ยนผ่านยังทำแบบ `tbl_task`** (LIVE ยังเป็นเจ้าของชั่วคราว ส่งทางเดียวมาคลาวด์
   ห้ามเขียนกลับ จนกว่า WP-P5-A ยืนยันว่าของเก่าเงียบจริงแล้วค่อยตัดสาย LIVE ให้ Cloudflare รับช่วงเขียนเต็มตัว)
   — ตรงกับข้อบังคับเดิมและปรัชญา P5 ทั้งแผน ("ย้ายของจริง แล้วค่อยตัดสาย CIM")
2. **ตั้งแต่ WP-P5-B เริ่ม ต้องเขียนตรงที่ Cloudflare เลย** — ไม่มีช่วง LIVE เป็นเจ้าของชั่วคราว · ถ้าเป็นแบบนี้
   `lib/tools.php`/`tools.php` ต้องถูกแก้ให้เรียก Cloudflare API แทนการเขียน MySQL ตรง (งานใหญ่กว่าที่ข้อบังคับเดิมคาดไว้มาก)
   และข้อบังคับเดิมในเอกสารต้องแก้ทิ้งทั้งท่อน

**ยังไม่แก้ข้อบังคับใน `WORK_PACKAGES_P5.md`** จนกว่าจะรู้ว่าพี่เต้หมายถึงข้อไหน — เขียนใบสั่งงาน WP-P5-B จริงผิดทาง
ตอนนี้แพงกว่าที่คิด เพราะกระทบทั้งสถาปัตยกรรมของใบ ไม่ใช่แค่รายละเอียดเล็กน้อย ⇒ ถามกลับไปแล้วผ่าน `AskUserQuestion`

**✅ พี่เต้เลือกแล้ว (ผ่าน `AskUserQuestion` รอบเดียวกัน):** **"ปลายทางสุดท้ายเท่านั้น"** — ระหว่างเปลี่ยนผ่านยังทำแบบ
`tbl_task` ตามข้อบังคับเดิมทุกตัวอักษร (LIVE เป็นเจ้าของชั่วคราว ส่งทางเดียวมาคลาวด์ ห้ามเขียนกลับ) รอ WP-P5-A ยืนยันของเก่า
เงียบจริงก่อน ถึงตัดสาย LIVE ให้ Cloudflare รับช่วงเขียนเต็มตัว **⇒ ข้อบังคับเดิมใน `WORK_PACKAGES_P5.md` §WP-P5-B ถูกต้อง
อยู่แล้ว ไม่ต้องแก้** — Q8-ข **ปิดแล้ว**

---

## D-P0-28 · ไปเช็ค `KSK-Decepticons` issue #9 เอง — ตาราง 7 วันดิบยังไม่เคยถูกโพสต์ที่ไหน ❓ ยังค้างจริง (2026-09-09)

**ที่มา:** พี่เต้ถามว่า P4 "ใครเขียน" คำตอบคือไม่มีใครเขียน เป็นข้อความอัตโนมัติ — เสนอไปเช็คตาราง 7 วันดิบเอง พี่เต้ตอบ "ไปดึงมา"

**✅ เปิด issue #9 อ่านทั้งสองคอมเมนต์ที่มีอยู่จริง (ผ่าน `issue_read`):**
- Comment 1 (2026-09-04 00:33): สคริปต์ `bin/preflight_golive.php` (อ่านอย่างเดียว) — วางไว้ให้รัน ยังไม่มีผลลัพธ์
- Comment 2 (2026-09-04 05:34): **ผลจริงจาก `bin/streak_now.php`** วัดตอน ~07:55 —
  ```
  2026-09-03  2/2  สะอาด    2026-08-31  1/1  สะอาด
  2026-09-02  1/1  สะอาด    2026-08-30  1/1  สะอาด
  2026-09-01  1/1  สะอาด    2026-08-29  8/9  <-- ไม่ตรง
  สตรีค 5 วัน · บันทึกล่าสุด 2026-09-03 · หยุดที่ 2026-08-29
  ```
  (ตัวเลขเดียวกับที่บันทึกไว้ใน `CLAUDE.md` อยู่แล้ว — ไม่ใช่ของใหม่)

**issue มี comment แค่ 2 อัน `updated_at` ค้างที่ 2026-09-04T05:34:48Z — ไม่มีใครโพสต์อะไรเพิ่มเลยตั้งแต่นั้น**
แปลว่า **ไม่เคยมีใครเอาผลของ 2026-09-04/09-05 (2 วันที่เหลือที่จะทำให้ครบ 7) มาโพสต์ที่นี่หรือที่ไหนที่เซสชันนี้เข้าถึงได้เลย**

**คณิตศาสตร์ตรงกันก็จริง:** ถ้า 30 ส.ค.–3 ก.ย. สะอาด (5 วัน) แล้ว 4–5 ก.ย. สะอาดต่อ = ครบ 7 วันพอดีคืน 5 ก.ย. —
ตรงกับวันที่ D-P0-20 อ้างว่าประตูเปิด **แต่นี่คือการคำนวณย้อนจากข้อความแจ้งเตือน ไม่ใช่การเห็นแถวจริงของ 4–5 ก.ย.**

**สรุป: ไปเช็คแล้ว ไม่พบหลักฐานใหม่ — ❓ ของ D-P0-20 ยังคงค้างอยู่จริง ไม่ใช่แค่ยังไม่มีใครไปดู**
เซสชันคลาวด์นี้ต่อ MySQL production ไม่ได้ (แซนด์บ็อกซ์ MariaDB ที่ตั้งเองใน D-P0-24 เป็นฐานแยกต่างหาก คนละตัวกับของจริง)
⇒ ทางเดียวที่จะปิด ❓ นี้ได้จริง: **มีคนที่เข้าเครื่อง/ฐานจริงได้ รัน `bin/streak_now.php` ใหม่วันนี้ (2026-09-09) แล้วโพสต์ผล**
— ถ้าระบบยังเดินปกติต่อเนื่อง ตัวเลขที่ควรเห็นคือสตรีคประมาณ 10-11 วัน (30 ส.ค. ถึงวันนี้) ถ้าน้อยกว่านั้นมาก แปลว่าสตรีค
เคยขาดไปแล้วหลัง 5 ก.ย. โดยไม่มีใครรู้

---

## D-P0-29 · ✅ ตาราง 7 วันดิบเห็นแล้วจริง — สตรีค 10 วันติดกัน ยืนยันอิสระว่าประตู P4 เปิดคืน 5 ก.ย. จริง (2026-09-09)

**พี่เต้รัน `bin/streak_now.php` กับ MySQL ของเครื่องจริง** (ไม่ใช่แซนด์บ็อกซ์) — ก่อนรันตรวจโค้ดเองด้วย: อ่านทั้งไฟล์
มีคิวรีเดียวเป็น `SELECT` ไม่มี `INSERT`/`UPDATE`/`DELETE` จริง (คำพวกนี้ที่ grep เจอมีแค่ในคอมเมนต์หัวไฟล์ที่อธิบาย
ว่าไม่มี) ไม่เขียนไฟล์ ไม่ยิง LINE — ตรงกับที่อ้างไว้ตอนโพสต์ทั้งหมด

**ผลจริงจาก `tbl_notify_log`:**
```
2026-09-08  1/1  สะอาด     ← บันทึกล่าสุด (ห่างวันนี้ 1 วัน — ปกติ ยังไม่ถึงรอบคืนนี้)
2026-09-07  1/1  สะอาด        2026-09-02  1/1  สะอาด
2026-09-06  1/1  สะอาด        2026-09-01  1/1  สะอาด
2026-09-05  1/1  สะอาด        2026-08-31  1/1  สะอาด
2026-09-04  3/3  สะอาด        2026-08-30  1/1  สะอาด
2026-09-03  2/2  สะอาด        2026-08-29  8/9  🔴 มีรอบที่ไม่ตรง ← หยุดตรงนี้
```

**นับเอง: 30 ส.ค. ถึง 8 ก.ย. = 10 วันติดกัน สะอาดทุกวัน ไม่มีวันไหนขาดบันทึกเลย**

**ทำไมข้อนี้สำคัญ:** ถ้านับจาก 30 ส.ค. ครบ 7 วันพอดีคือ **5 ก.ย.** — ตรงกับวันที่ D-P0-20 บันทึกไว้ว่าประตูเปิด
เป๊ะ **นี่คือครั้งแรกที่เห็นแถวจริงยืนยันตัวเลขนั้น** ไม่ใช่การคำนวณย้อนจากข้อความแจ้งเตือนอีกต่อไป (ต่างจาก D-P0-20/24/28
ที่ทำได้แค่อนุมานว่าน่าจะถูก) และหลังจากนั้นสตรีคก็เดินต่อเนื่องไม่ขาดอีกเลยถึง 8 ก.ย. — **ไม่ได้หยุด/รีเซ็ตไปเงียบ ๆ
อย่างที่กังวลไว้ใน D-P0-28** แค่ไม่มีใครรันซ้ำเพื่อดูเฉย ๆ

**⇒ ปิด ❓ "เซสชันนี้ยังไม่เห็นตาราง 7 วันดิบเอง" ที่ค้างซ้ำมาตั้งแต่ D-P0-20 → D-P0-24 → D-P0-28 ได้จริงแล้ว**
✅ P4 ผ่านประตูจริง มีหลักฐานแถวดิบรองรับครบ ไม่ใช่แค่ 📄 relay อีกต่อไปสำหรับจุดนี้โดยเฉพาะ
