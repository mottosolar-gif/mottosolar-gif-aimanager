# ใบสั่งงาน P4 — ทดสอบเต็มระบบ (รันคู่ · เทียบผล · ปิดช่องว่างที่ปลอดภัย)

> **ผู้เขียนใบสั่ง:** Claude (สถาปัตยกรรม + ตรวจงาน) · **ผู้ลงมือ:** Codex
> **กติกาบ้าน:** Codex เขียนโค้ด/เทส/migration · **ห้ามตรวจงานตัวเอง** — ส่งให้ Claude ตรวจก่อนถือว่าเสร็จ

---

## เป้าหมาย (ตาม IMPLEMENTATION_PLAN.md §6)
> P4 · รันคู่ของเดิม ≥ 2 สัปดาห์ในกลุ่มทดสอบ + คนจริงกลุ่มเล็ก · เทียบผลทุกคืน
> **ประตู:** ผลตรงกัน 100% ติดกัน 7 วัน · โควตา push ต่อเดือนไม่เกินที่มี

**สถานะ ณ 2026-08-29:** เครื่องมือของประตูครบแล้วทั้งสองตัว
- `bin/aim_reconcile.php` + task `ksk-aim-reconcile` (ทุกคืน 23:30) — เทียบ `tbl_task` กับสำเนาบนคลาวด์
- `bin/selfcheck.php` คาดการณ์โควตาต่อเดือน (วัดจริง 36.8/วัน ⇒ 1,103/เดือน จาก 15,000)

⇒ **P4 เริ่มนับแล้ว** สิ่งที่เหลือคือ "เวลา" ซึ่งเร่งไม่ได้

---

## ⛔ ข้อบังคับที่สำคัญที่สุดของเฟสนี้ — อ่านก่อนแตะอะไร

**ห้ามเปลี่ยนพฤติกรรมของสิ่งที่กำลังถูกเทียบ**

ตัวเทียบผลกำลังนับ "ตรงกัน 7 วันติด" อยู่ · ถ้าแก้อะไรที่กระทบ **สถานะงาน · ชื่องาน · การซิงก์**
ตัวนับจะรีเซ็ตและ P4 ต้องเริ่มใหม่ ⇒ งานในเฟสนี้ต้อง **เพิ่มของใหม่ ไม่แก้ของที่ถูกวัดอยู่**

**ห้ามทำในเฟสนี้ (รอ P5):**
- เพิ่มสถานะงานจาก 6 เป็น 12 (§7–8) — เปลี่ยนสิ่งที่ตัวเทียบวัดโดยตรง
- แตะ `bin/aim_task_sync.php` · `lib/escalation.php` · ตารางแปลงสถานะ
- ย้ายระบบเครื่องมือ/5ส/ลา (เป็นงานของ P5 ตามแผน)

---

## WP-P4-A · เก็บข้อมูลรูปงานให้ครบ (§12) — ✅ **ทำเสร็จแล้ว** (ตรวจของจริง 2026-09-04)

**หลักฐาน:** `sql/053_task_photo_meta.sql` มีอยู่จริงใน `KSK-Decepticons` (ชื่อไฟล์ตรงกับที่ใบสั่งกำหนด)
· ยาม `bin/smoke_task_photo_meta.php` (📄 19 ข้อ · ใช้ `Task_ID 999901` แล้วลบเอง ไม่แตะ `tbl_task`)
**ข้อความข้างล่างเก็บไว้เป็นสเปกที่ใช้ตอนทำ ไม่ใช่งานค้าง**

`tbl_task_photo` ตอนนี้มีแค่ `Path` + `Created_At` (ยืนยันจาก ARCHITECTURE.md §12)
ขาด: `file_hash` · ขนาดไฟล์ · ผู้ส่ง · เวลาถ่ายจริง

**ทำอะไร**
1. `sql/053_task_photo_meta.sql` — เพิ่มคอลัมน์แบบ **nullable ทั้งหมด** (ของเดิมต้องไม่พัง)
   `File_Hash CHAR(64) NULL` · `Bytes INT NULL` · `Sender_User_ID INT NULL` · `Taken_At DATETIME NULL`
   + `KEY idx_hash (File_Hash)`
2. จุดที่บันทึกรูป (หาเอง — อยู่ใน `api/line_webhook.php` เส้นทางรูปของงาน) เติมค่าใหม่
   - `File_Hash` = `hash_file('sha256', $path)` — ใช้ตรวจว่ารูปซ้ำ/ถูกแก้
   - `Taken_At` **อย่าเดา** ถ้าอ่านจากไฟล์ไม่ได้ให้เป็น NULL

**ห้ามทำ**
- ❌ **ห้ามเก็บพิกัด GPS ของคน** — ARCHITECTURE.md §3 ข้อ 1 เขียนไว้ว่าบ้านนี้ยังไม่มีระบบ consent
  เก็บได้เฉพาะ "พิกัดของงาน (ที่หมาย)" ซึ่งเป็นงานคนละใบ ยังไม่อนุมัติ
- ❌ ห้ามแก้รูปเดิมย้อนหลัง — ของเก่าปล่อยเป็น NULL ไป

**เกณฑ์ผ่าน**
- `php bin/smoke_tasks_render.php` ยังผ่านเท่าเดิม (36 ข้อ)
- เทสใหม่: บันทึกรูปแล้วได้ hash ตรงกับ `hash_file` ของไฟล์จริง · ไฟล์อ่านไม่ได้ = NULL ไม่ใช่สตริงว่าง
- `php bin/aim_reconcile.php` ยังตรงกัน 100% (พิสูจน์ว่าไม่กระทบสิ่งที่ถูกวัด)

---

## WP-P4-B · ยามเฝ้าความต่อเนื่องของ P4 เอง — ✅ **ทำเสร็จแล้ว** (ตรวจของจริง 2026-09-04)

> ⚠ **บทเรียนของใบนี้เอง:** 2026-09-04 มีการเขียน "ใบสั่งงานฉบับเต็ม" ให้ใบนี้ทั้งที่ **ของทำเสร็จไปแล้ว**
> สาเหตุ: ผู้เขียนใบเชื่อว่าโค้ดฝั่ง LIVE อยู่แต่ในเครื่องที่บ้าน จึงไม่เคยเปิดดู
> **ความจริงคือ `C:\WebApp\vendor-ksk` มีบ้านบน GitHub ชื่อ `mottosolar-gif/KSK-Decepticons`**
> ⇒ ก่อนเขียนใบสั่งงานที่แตะฝั่ง LIVE **ต้องเปิด repo นั้นดูก่อนเสมอ**

### ของที่มีอยู่จริง

| ของ | ที่ไหน |
|---|---|
| โหมด `--streak` | `bin/aim_reconcile.php` — `rec_streak_days()` |
| ยามของตัวนับ | `bin/smoke_reconcile_streak.php` (📄 8 ข้อ) |
| **แถมที่ใบสั่งไม่ได้ขอ** — แจ้ง LINE อัตโนมัติเมื่อประตูเปิด | `rec_gate_should_notify()` + `bin/smoke_reconcile_gate_notify.php` (📄 12 ข้อ) |

**ตัวนับแก้กับดักหลักไว้ถูกแล้ว 3 ข้อ** (ตรวจจากซอร์สจริง): ยุบหลายรอบในวันเดียวเป็น 1 วัน ·
วันที่มี ok ปนกับไม่ ok ถือว่าไม่ผ่าน · วันที่ไม่มีบันทึก = ตัดสตรีค ไม่ใช่ข้าม
คอมเมนต์ในโค้ดยังบันทึก sabotage ที่เคยทำไว้ด้วย ⇒ ทำตามกติกา "ยามที่ตกไม่ได้ ไม่ใช่ยาม" จริง

---

### 🔴 บั๊กที่เหลืออยู่ — `--streak` ตอบ 0 วันละ 23 ชม.ครึ่ง (เจอ 2026-09-04 · **ยังไม่แปะแพตช์**)

`rec_streak_days()` เดินถอยหลัง **จากวันนี้** (`$i = 0`) แต่ `ksk-aim-reconcile` รัน **23:30**
⇒ ใครกด `--streak` ก่อนสี่ทุ่มครึ่ง จะเจอ "วันนี้ไม่มีบันทึก" แล้ว `break` ทันที **ได้ 0 เสมอ**
ซึ่งเป็นคำสั่งที่ `AGENTS.md` บอกให้ใช้ตอบว่า *"P4 ผ่านประตูหรือยัง"* พอดี

- **เส้นทางอัตโนมัติไม่กระทบ** — ตัวแจ้งประตูเรียก `nt_log()` บันทึกผลคืนนี้ก่อนแล้วค่อยนับ (โค้ดคอมเมนต์ไว้ว่าตั้งใจ)
  ⇒ **P5 ไม่มีทางถูกพลาด** เสียแค่คำสั่งที่คนกดเอง
- **ทำไมยามเดิมจับไม่ได้:** เคสทดสอบทั้ง 6 ข้อ **seed "วันนี้" ทุกเคส** ⇒ ยามไม่เคยเห็นสภาพจริงที่คนเจอบ่อยที่สุด
  — บทเรียนเดียวกับ D-P0-08 (ค่าจำลองที่ผู้เขียนเลือกเอง ย่อมผ่านกฎที่ผู้เขียนคนเดียวกันตั้ง)

**แพตช์ที่เตรียมไว้** (ยังไม่ push — เซสชันคลาวด์ขอสิทธิ์ push ไม่ผ่าน):
1. เริ่มนับจาก **วันล่าสุดที่มีบันทึก** แทนวันนี้ · คืน `latest_day` + `stale_days` เพิ่ม
2. ปิดช่องโหว่ที่ตามมา — ยามที่ตายไป 5 วันแต่ก่อนตายสะอาด 7 วัน **ต้องไม่ประกาศว่าประตูเปิด**
   ⇒ `rec_gate_should_notify()` ปฏิเสธเมื่อ `stale_days > 1` · `--streak` พิมพ์ "บันทึกล่าสุดห่างกี่วัน" เสมอ
3. เพิ่มเคส 8–10 ลง `smoke_reconcile_streak.php` ให้ครอบสภาพจริง

**หลักฐานที่มีแล้ว:** 6 เคสเดิมได้ผล **เท่าเดิมทุกเคส** · เคสใหม่ N1 เดิม 0 → 5 · N2 นับ 7 แต่ไม่ประกาศประตู ·
sabotage เอาการเริ่มจากวันล่าสุดออก → **ตก 3 ข้อ** · sabotage เอาด่านกันสตรีคเก่าออก → **ตก 1 ข้อ**
⚠ ทั้งหมดนี้พิสูจน์ **ตรรกะ PHP เท่านั้น** (ดึงฟังก์ชันจริงมารันกับข้อมูลปลอม) — **ยังไม่ได้รันยามตัวจริงกับ MySQL**
⇒ **ก่อนเชื่อ ต้อง `php bin/smoke_reconcile_streak.php` ที่เครื่องบ้านให้ผ่านครบก่อน**

---

## WP-P4-C · เอกสาร §29 consent / retention / export-delete — 🟠 **น่าจะเสร็จแล้ว แต่ไม่ได้อยู่บน GitHub**

📄 `KSK-Decepticons/CLAUDE.md:307` อ้างถึง **`aim/docs/DATA_POLICY.md` ข้อ 2.2** ราวกับมีอยู่จริง
✅ แต่ **รีโปนี้ไม่มีไฟล์นั้น** ⇒ ไฟล์อยู่แต่ในเครื่อง `C:\WebApp\aim` **ยังไม่ถูก push**
⚠ นี่แปลว่า **รีโปนี้ตามหลังเครื่องที่บ้านอยู่** — ของที่ commit แล้วแต่ไม่ push ยังหายไปกับเครื่องได้
⇒ งานที่เหลือของใบนี้คือ **push ให้ขึ้นมา** ไม่ใช่เขียนใหม่ (เขียนใหม่ = ความจริงสองชุด)

ARCHITECTURE.md §29 บอกว่า **"ไม่มีเอกสารเลย"** และเขียนกำกับว่า *"ต้องมีก่อนแตะ GPS"*
ตอนนี้ยังไม่แตะ GPS แต่ระบบเก็บข้อมูลคนไปแล้วหลายอย่าง (รูป · เวลาทำงาน · งานที่ได้รับมอบหมาย)

**ทำอะไร** — `docs/DATA_POLICY.md` ตอบ 4 คำถามด้วยของจริงที่วัดได้ ไม่ใช่ข้อความทั่วไป
1. เก็บอะไรบ้าง · เก็บที่ไหน (ฝั่ง LIVE / คลาวด์) — **ไล่จากตารางจริง ไม่ใช่จากความจำ**
2. เก็บนานแค่ไหน · อะไรลบอัตโนมัติแล้วบ้าง (เช่น heartbeat 7 วันใน `tbl_bridge_read`)
3. ใครดูได้ (อ้าง `canView()` และตารางสิทธิ์จริง)
4. ถ้าพนักงานขอให้ลบข้อมูลตัวเอง ต้องทำอะไรบ้าง — **ระบุชื่อตารางและลำดับการลบ**

**ห้าม** เขียนนโยบายที่ระบบทำไม่ได้จริง — เอกสารที่สัญญาเกินของที่มี แย่กว่าไม่มีเอกสาร

---

## เหลืออะไรจริง ๆ (2026-09-04)
1. **แปะแพตช์ `--streak`** แล้วรัน `smoke_reconcile_streak.php` ให้ผ่าน — ดู §WP-P4-B
2. **push `docs/DATA_POLICY.md`** ขึ้นรีโปนี้ — ดู §WP-P4-C
3. ที่เหลือคือ **รอเวลา** ให้สตรีคครบ 7 วัน ซึ่งเร่งไม่ได้

## ประตูของทุกใบในเฟสนี้
```
php bin/aim_reconcile.php          → ต้องยังตรงกัน 100%
php bin/selfcheck.php --dry        → ต้อง 0 ปัญหา
php bin/smoke_escalation.php       → 32 ข้อ
php bin/smoke_tasks_render.php     → 36 ข้อ
php bin/smoke_aim_ask.php          → 59 ข้อ   ← เคยเขียนผิดเป็น 41
php bin/smoke_aim_notify.php       → 27 ข้อ
php bin/smoke_reconcile_streak.php → 8 ข้อ (10+ หลังแปะแพตช์)
php bin/smoke_task_photo_meta.php  → 19 ข้อ
```
**เลขชุดนี้ลอกจาก `KSK-Decepticons/CLAUDE.md` — ที่นั่นคือต้นทาง อย่าจำเอง**
**จำนวนเทสห้ามลดลง** · ของใหม่ทุกชิ้นต้องมียามที่ **พิสูจน์แล้วว่าทำให้ตกได้จริง**
ก่อนบอกว่าเสร็จ — ไม่ใช่แค่เขียวแล้วจบ
