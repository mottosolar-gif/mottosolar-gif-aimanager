# ใบปิดโปรเจกต์ AI Manager — ประตูที่ผ่านจริง + checklist ส่งมอบ (2026-09-10)

ไฟล์นี้คืองาน **C.1** และ **C.5** ของ `PROJECT_CLOSEOUT_PLAN.md`: รวมใบปิดทุกเฟสไว้ที่เดียว
และตอบคำถามเดียวต่อประตู — **"ผ่านด้วยอะไรจริง"** ไม่ใช่ "ใช้เครื่องมืออะไรตรวจ" (บทเรียน D-P0-17)
ทุกช่อง ✅ ในไฟล์นี้อ้างอย่างใดอย่างหนึ่ง: ผลรันจริงคืน 2026-09-10 (§0) · แถวในฐานจริง · หรือเลขคำตัดสินใน `DECISIONS_P0.md`

> **ป้าย:** ✅ ผ่านด้วยของจริง (รันหรือดูเองคืนนี้) · 📌 ผ่านด้วยคำตัดสินเจ้าของระบบ ไม่ได้รันเทส · 🟡 หลักฐานบางส่วน · ⛔ ตัดสินว่าไม่ทำ
>
> สภาพตอนรัน: เครื่อง HKServer-CIMS · `KSK-Decepticons` = `a3f5d28` (= `origin/main`) · `aim` = `e6d22c9` (= `origin/main`)
> · Worker `/healthz` ตอบ `version = e6d22c93b653` = HEAD · Workers Builds ของ `e6d22c9` = `success`

---

## 0. ยามที่รันจริงคืนนี้ (2026-09-10 22:42–22:50)

อ่านหัวไฟล์ทุกตัวก่อนรันตามกติกา — ทุกตัวในตารางประกาศว่าไม่ยิง LINE และไม่แตะ `tbl_task`
(`smoke_legacy_silence` / `smoke_task_photo_meta` / `smoke_reconcile_*` เขียนแถวทดสอบของตัวเองแล้วลบเอง)

| คำสั่ง (รันใน `C:\WebApp\vendor-ksk` เว้นที่ระบุ) | ผลคืนนี้ | ใช้เป็นหลักฐานของ |
|---|---|---|
| `php bin\aim_reconcile.php --streak` | **ตรงกันติดกันมาแล้ว 12 วัน** (30 ส.ค.–10 ก.ย.) · หยุดที่ 2026-08-29 · "ประตู P4 ต้องการ 7 วันติด ⇒ ผ่านแล้ว" | ประตู P4 · P5 ขั้น 2 |
| `php bin\streak_now.php` (อ่านอย่างเดียว) | ตารางดิบ 13 วัน: 10 ก.ย. 1/1 · 9 ก.ย. 1/1 · … · 30 ส.ค. 1/1 สะอาดทุกวัน · 29 ส.ค. 8/9 🔴 (จุดตัดสตรีคเก่า) · บันทึกล่าสุดห่าง 0 วัน | ประตู P4 (แถวดิบ ไม่ใช่ตัวนับ) |
| `php bin\selfcheck.php --dry` | **พบปัญหา 64 รายการ** — 63 รายการคือ `db_customs_attic/* · ชนิดสัญญาณไม่รู้จัก: 'table'` (ของอีก estate ดู §5) · 1 รายการคือ event ค้างในคิว spool (ไฟล์ทดสอบ DIAG ดู §5) · **ส่วนที่เป็นของ AI Manager เขียวทั้งหมด:** aim-worker HTTP 200 · คิว 0 · ตายถาวร 0 · ค้าง 0 · รายชื่อคุยกับน้องกุ้ง 12 คน ตรงกับฐาน · โควตา LINE เหลือ 14,529 · อัตรา push 79.4/วัน ⇒ คาดการณ์ 2,381/เดือน จาก 15,000 | ประตู P4 (โควตา) · นิยามจบข้อ 1, 5 |
| `php bin\smoke_escalation.php` | ✅ PASS ผ่าน 32 · ตก 0 | ประตู P3 ข้อ ①③ |
| `php bin\smoke_aim_ask.php` | ✅ 67/67 | ประตู P2 · หนี้ person_link |
| `php bin\smoke_aim_notify.php` | ✅ 28/28 | ประตู P1 (การ์ด) |
| `php bin\smoke_person_registry.php` | ผ่าน 19 · ไม่ผ่าน 0 | นิยามจบข้อ 1 (ทะเบียนอ่านฐานจริง) |
| `php bin\aim_person_map_sync.php --check` | ตรงกับฐาน · 12 คน (exit 0) | นิยามจบข้อ 1, 4 (ทางถอยไม่ล้าสมัย) |
| `php bin\smoke_reconcile_streak.php` | ✅ PASS ผ่าน 25 · ตก 0 (เดิม 8 · เพิ่มหลังแก้ตัวนับ 2026-09-10) | ประตู P4 (ตัวนับ) |
| `php bin\smoke_reconcile_gate_notify.php` | PASS ผ่าน 12 · ตก 0 | ประตู P4 (ตัวประกาศ) |
| `php bin\smoke_task_photo_meta.php` | PASS ผ่าน 19 · ตก 0 | WP-P4-A |
| `php bin\smoke_aim_forward.php` | ✅ 16/16 | ประตู P0 ข้อ 3 · WP-P0-5 |
| `php bin\smoke_tasks_render.php` | ✅ 36 ข้อ | หน้างานทีม (ประตู P4 ชุดยาม) |
| `php bin\smoke_legacy_silence.php` | ผ่าน 45 · ตก 0 (ลบแถว/ตารางทดสอบครบ) | P5 ขั้น 4 · WP-P5-A |
| `npm run typecheck` (ใน `C:\WebApp\aim`) | tsc ไม่มี error | Worker |
| `npm test` (ใน `C:\WebApp\aim`) | **Test Files 9 passed · Tests 311 passed** | ประตู P1, P2 ฝั่งคลาวด์ · นิยามจบข้อ 5 |
| `curl https://aim-ingest.mottosolar.workers.dev/healthz` | `ok:true · version:"e6d22c93b653" · builtAt 2026-09-10T15:31:09Z · queueDepth 0 · deadJobs 0 · strandedEvents 0` | deploy ตรง HEAD · คิวไม่ค้าง |
| GitHub check-runs ของ `e6d22c9` | `Workers Builds: aim-ingest` = success · `test` = success | GitHub-first (D-2026-09-10-ข) |
| `git status -sb` ทั้งสองรีโป | **ณ 22:42 ก่อนเขียนชุดเอกสารนี้:** สะอาด ตรง `origin/main` ทั้งคู่ (aim มี `docs/REPO_OFFSITE_PLAN.md` untracked ของอีกเซสชัน — คงไว้) · **หลังชุดนี้:** aim มี 5 ไฟล์แก้/ใหม่ (`CLAUDE.md` · `AS_BUILT.md` · `IMPLEMENTATION_PLAN.md` · `PROJECT_CLOSEOUT_PLAN.md` · ไฟล์นี้) รอ commit + push ⇒ ตามกติกา "ยังไม่อยู่บน `main` = ยังไม่เสร็จ" ชุดนี้จะเสร็จเมื่อ checklist ข้อ 1 ผ่าน | นิยามจบข้อ 2 (สายเดียว) |

**ยามที่ไม่ได้รันคืนนี้โดยตั้งใจ:** `smoke_line_register.php` (ฮาร์ดโค้ดพอร์ต 8412 ยิงถึงฐานจริงได้) · `smoke_webhook_spool.php`
(แตกโปรเซสจริง ทิ้งบรรทัด `WORKER FATAL` ใน log ของจริง — D-2026-09-10-ง) · `aim_reconcile.php` แบบไม่มีอาร์กิวเมนต์
(บันทึกผลลง `tbl_notify_log` — ปล่อยให้รอบ 23:30 ของ task `ksk-aim-reconcile` เป็นคนบันทึก)

---

## 1. ตาราง "ประตูที่ผ่านจริง" ทุกเฟส

ประตูตามแผนคัดจาก `IMPLEMENTATION_PLAN.md` §6 คำต่อคำ · "ผ่านด้วยอะไรจริง" อ้าง §0 หรือคำตัดสิน

| เฟส | ประตูตามแผน | ผ่านด้วยอะไรจริง | สถานะ | ใบปิด / คำตัดสิน |
|---|---|---|---|---|
| **P0** ท่อและกระดูก | ข้อความจากกลุ่มทดสอบเดินทางครบวง + มี log ทุกจุด (แตกเป็น 7 ข้อใน WP-P0-6) | ข้อ 1 event ถึงคลาวด์จริง (D-P0-08 curl 200 · D-P0-12 ปุ่มจริง) · ข้อ 2 ยิงซ้ำ 3 ครั้งบันทึกครั้งเดียว (D-P0-04) · ข้อ 3 ปิดคลาวด์ระบบเดิมยังครบ — `smoke_aim_forward` **16/16 คืนนี้** · ข้อ 5 `db:dump` ดึงกลับบ้านได้ (D-P0-04 · task `ksk-aim-dump` 03:20 ทุกวัน) · ข้อ 6 privacy scan บนฐานจริง (D-P0-04 · ข้อยกเว้น D-P0-17) · ข้อ 7 code review สะสม (D-P0-05/07/10/11) · **ข้อ 4 "ปิด cim-server แล้ว cron คลาวด์ยังเดิน" ไม่เคยรันเทสจริง** | 📌 **ปิดแล้ว 2026-09-10** — 6/7 ด้วยของจริง · ข้อ 4 ด้วยคำตัดสิน | `WORK_PACKAGES_P0.md` §WP-P0-6 · D-P0-25 Q5 |
| **P1** ประสาทสัมผัส | เดินครบเส้นทางในกลุ่มทดสอบ · ยิงซ้ำ 3 ครั้งไม่เกิดงานซ้ำ | พี่เต้กดปุ่ม "❌ ปฏิเสธ" จริงบนโทรศัพท์ → `assigned → rejected` เปลี่ยนจริงในฐานคลาวด์ (D-P0-12) · idempotency ทดสอบกับ D1 จริง (D-P0-04) · คืนนี้ `worker/test/endpoints.test.ts` ส่ง `/sync/tasks` ซ้ำผ่าน `fetch` จริงแล้วไม่เกิดแถวซ้ำ (อยู่ใน 311/311) · `smoke_aim_notify` 28/28 | ✅ **ปิดจบ 2026-08-27** | `WORK_PACKAGES_P1.md` "ลำดับที่ทำไปแล้ว" · D-P0-09..12 |
| **P2** สายตา + ปาก | 20 คำถามมาตรฐาน ไม่มีคำตอบไหนแต่งขึ้น · ลูกน้องถามงานคนอื่นต้องไม่เห็น (+ WP-P2 เพิ่ม: สรุปเช้า-เย็นถึงจริง ≥ 3 วันติด) | 20/20 ยิงกับ production จริง ทุกตัวเลขมาจาก SQL ผ่าน `canView()` ไม่มี LLM ในเส้นทาง (D-P0-19) · ตัวตนคนที่สอง (worker) ถาม 3 คำถามของทีม → denied ครบ 3 (D-P0-19) · สรุป 08:00/17:00 เดินอัตโนมัติถึง 4 คนทุกวัน (`AS_BUILT.md` §2) · คืนนี้ `smoke_aim_ask` 67/67 · `visibility.test.ts` + `queryEngine.test.ts` ใน 311/311 · **สถิติที่ไม่มีข้อมูล (`stats_avg_cycle` · `stats_fastest_accept`) ตอบตรง ๆ ว่ายังไม่มี ไม่แต่งตัวเลข** (`f84ff1c` 2026-09-10) | ✅ **ปิดจบ 2026-08-29** | D-P0-19 · `WORK_PACKAGES_P2.md` §1 ประตูชุด B |
| **P3** ความคิดริเริ่ม | จำลอง 10 สถานการณ์ได้ผลตามกฎทุกครั้ง · ไม่เตือนซ้ำเกิน 1 ครั้งต่อระดับ · นับโควตา push ที่ใช้จริงต่อวัน | ① `smoke_escalation` **32/32 คืนนี้** (10 เคสเรียงตามประตู `bin/smoke_escalation.php:41-89`) · ② กันซ้ำที่ฐานจริง `sql/051_escalation.sql:28` UNIQUE + `esc_claim()` INSERT IGNORE — **ยามพิสูจน์ด้วยการอ่านซอร์ส ไม่ได้ยิงซ้ำกับฐาน** · ③ `selfcheck` คืนนี้นับ push 79.4/วัน จาก `tbl_notify_log` 7 วัน (`bin/selfcheck.php:405-419`) · `tbl_escalation` มีแถวจริง 3 แถว (ล่าสุด 2026-08-29) · **ไม่มี scheduled task เรียก `bin/task_escalate.php` — ตั้งใจ** | 📌 **ปิดเฟส 2026-09-10 ในสถานะ "พร้อมแต่ยังไม่เปิดสวิตช์"** (① ✅ · ② 🟡 · ③ ✅) — ปิดเพราะเจ้าของระบบเคาะว่าไม่เปิดสวิตช์ ไม่ใช่เพราะประตูผ่านครบสามข้อ (D-2026-09-10-ค พูดไว้ตรง ๆ) · เปิดสวิตช์ = งานแยกใบ ขออนุมัติใหม่ | `WORK_PACKAGES_P3.md` §3 · D-2026-09-10-ค |
| **P4** ทดสอบเต็มระบบ | ผลตรงกัน 100% ติดกัน 7 วัน · โควตา push ต่อเดือนไม่เกินที่มี | `streak_now` คืนนี้: **สะอาด 12 วันติด 30 ส.ค.–10 ก.ย.** (แถวดิบ `tbl_notify_log`) · ประตูเปิดคืน 5 ก.ย. (D-P0-20) ยืนยันแถวดิบ 9 ก.ย. (D-P0-29) · push 79.4/วัน ⇒ ~2,381/เดือน = 16% ของ 15,000 (โตตามจำนวนคน: 610 เมื่อ 27 ส.ค. → ~1,103 เมื่อ 29 ส.ค. → 2,381 คืนนี้ที่ 12 คน — เพิ่มคนเมื่อไรต้องวัดใหม่ `CLAUDE.md` §2 ข้อ 10) · ตัวนับแยก "ไม่ตรง / ยังไม่ถึงรอบ / ยามไม่ได้รัน" แล้ว (`26af4f5` · `smoke_reconcile_streak` 25/25 คืนนี้) · "รันคู่ ≥ 2 สัปดาห์" ไม่ใช่ประตูแยก (D-P0-25 Q2) | ✅ **ผ่านประตู 2026-09-05 · ยังเดินต่อไม่ขาดถึงคืนนี้** | `WORK_PACKAGES_P4.md` · D-P0-20 · D-P0-29 · D-2026-09-10-จ (ไม่ลบ `L-9` เพื่อไม่ให้สตรีคขาด) |
| **P5** ทยอยย้ายขึ้น Cloudflare (ของ aim) | ขั้น 2 ทุกระบบ = ประตูเดียวกับ P4 · ขั้น 4 = ยามยืนยันว่าของเก่า "หยุดจริง" | ขั้น 1–2 ของ `tbl_task`: mirror ทางเดียวตรง 12 วันติด (แถวบน) · **ขั้น 3 (สลับเจ้าของ `tbl_task` ไปคลาวด์) ตัดสินว่าไม่ทำ** — รูปแบบปัจจุบันพิสูจน์แล้วว่าใช้ได้ · ยังไม่มีสคริปต์ถอย · ปุ่ม LINE เป็น postback ถึงเครื่องในบ้านเท่านั้น (`AS_BUILT.md` §6 · D-2026-09-10-ก) · ขั้น 4: เครื่องมือ WP-P5-A พร้อม — `smoke_legacy_silence` **45/45 คืนนี้** · sabotage-verify แล้ว (D-P0-24) · ยังไม่มีระบบไหนถึงขั้น 3 (D-P0-23) · WP-P5-B/C (ระบบเครื่องมือ) ยังไม่เริ่มในความหมายใหม่ — กล่องสถานะบน `WORK_PACKAGES_P5.md:10` (§สถานะรวมด้านล่างของไฟล์นั้นเขียน "ปิด" ในความหมายเก่า "ตัดสาย CIM" ซึ่งหมดอายุแล้ว ดู D-P0-21) | 📌 **ปิดตามคำตัดสิน** — สิ่งที่วัดได้ผ่านครบ (ขั้น 2 + เครื่องมือขั้น 4) · การย้ายระบบถัดไปเป็นงานใหม่ทีละใบตามทางเดิน D-2026-09-10-ก ไม่ผูกกับการปิดโปรเจกต์นี้ | `WORK_PACKAGES_P5.md` §สถานะรวม · D-2026-08-29-ก · D-P0-23 · D-P0-24 · D-2026-09-10-ก |

**ขั้นที่ 2 ของแผนปิด (ด่านหน้า LINE store-and-forward): ⛔ ไม่สร้าง** — downtime ที่พิสูจน์ได้ = 0 ชม./เดือน ทั้ง 30 และ 60 วัน
(probe ทุก 15 นาที 2,923 จุด ไม่มีช่องว่าง > 20 นาที · IIS ไม่มี 5xx · ไม่ reboot ตั้งแต่ 2026-06-14) — D-2026-09-10-ง · `tunnel_watchdog` ยังวัดต่อ ห้ามถอด

**ประตูปิดขั้นที่ 1 ของแผนปิด (`PROJECT_CLOSEOUT_PLAN.md` §2 ขั้นที่ 1): 🟡 ยังเดินอยู่** — ข้อความประตูคือ *"สตรีค P4 นับได้ถูกต้องอย่างน้อย 7 วันหลังแก้ 0.2"*
ตัวนับใหม่ลง `main` เมื่อ 2026-09-10 20:29 (`26af4f5`) ⇒ รอบแรกที่ตัวนับใหม่วัดจริงคือ 23:30 คืนนี้ · ครบ 7 รอบคืน **2026-09-17** —
ไม่บล็อกการส่งมอบ (นิยาม "จบ" §0 ไม่ได้อ้างข้อนี้) แต่ต้องมีคนกด `--streak` อีกครั้งวันที่ 17 ก.ย. แล้วจดผล (checklist ข้อ 5ข)

---

## 2. นิยาม "จบโปรเจกต์" 6 ข้อ — หลักฐานรายข้อ

| # | นิยาม (`PROJECT_CLOSEOUT_PLAN.md` §0) | ผ่านด้วยอะไรจริง | สถานะ |
|---|---|---|---|
| 1 | ไม่มีงานประจำวันไหนที่ต้องมีคนรันสคริปต์ด้วยมือ | 3 ตัวที่เคยต้องรันมือ: `aim_link_person --all` → ผูกให้เองเมื่อคลาวด์ตอบ 403 (`26af4f5`) · `aim_person_map_sync` → ทะเบียนอ่านฐานจริง ไฟล์เหลือเป็นทางถอย (`6b991c5` · `--check` ตรง 12 คน คืนนี้) · `admin/summary` → cron ของ Worker ยิงเอง 08:00/17:00 · งานประจำวันที่เหลือเป็น scheduled task ทั้งหมด (ตรวจ `Get-ScheduledTask` คืนนี้): `ksk-aim-task-sync` ทุก 10 นาที · `ksk-aim-reconcile` 23:30 · `ksk-aim-dump` 03:20 · `ksk-aim-mail-cmd` ทุก 30 นาที · `ksk-selfcheck` 07:45 · `ksk-git-pull` ทุก 1 นาที | ✅ |
| 2 | ทุกประตู P0–P5 มีใบปิดที่อ้างของจริง · เอกสารสองสายรวมเป็นสายเดียว | ตาราง §1 (ทุกแถวชี้ใบปิด + คำตัดสิน) · สายเอกสารรวมแล้ว `7d6ccb8` (เลข D-P0 ชนกันแก้ด้วยตารางแปลงหัว `DECISIONS_P0.md` + เลขแบบวันที่) · ทั้งสองรีโปตรง `origin/main` ณ 22:42 — **ชุดเอกสารปิดนี้เองยังไม่อยู่บน `main`** จนกว่า checklist ข้อ 1 จะ push | 🟡 → ✅ เมื่อ push |
| 3 | ระบบไม่ตายตามเครื่องเดียว **หรือ** มีตัวเลขพิสูจน์ว่าเครื่องแทบไม่ดับ | ปิดด้วยทางเลือกที่สอง: 0 ชม./เดือน (D-2026-09-10-ง) — ตัวเลขข้างบน | ✅ (ด้วยตัวเลข) |
| 4 | ทุกทางที่ย้ายไปแล้วมีสคริปต์ถอยกลับที่ซ้อมบน clone แล้ว | **ไม่มีการย้ายความเป็นเจ้าของสักระบบ** (D-P0-23 · ขั้น 3 ของ `tbl_task` ตัดสินไม่ทำ) ⇒ ไม่มีสคริปต์ถอยที่ต้องซ้อม · ทางถอยที่มีจริง: mirror เป็นทางเดียว หยุดได้ด้วยปิด task `ksk-aim-task-sync` โดยไม่มีอะไรเขียนกลับ (D-P0-18) · ทะเบียนคนถอยไป `config/aim_person_map.php` ซึ่ง `--check` ยืนยันคืนนี้ว่าตรงฐาน | 📌 (ไม่มีของให้ถอย — ยืนบนคำตัดสิน ไม่มีสคริปต์ที่ซ้อมจริง) |
| 5 | ยามครอบทุกจุดที่เคยพังเงียบ โดยเฉพาะ "ยิงผ่านทางเข้าจริง" | ฝั่ง Worker: `endpoints.test.ts` ยิง `/sync/tasks` · `/ask` · `/ingest/line` · `/link` · `/healthz` ผ่าน `fetch` ของ default export จริง (`f84ff1c` · อยู่ใน 311/311 คืนนี้) · ฝั่ง PHP: `smoke_aim_forward` ต้องตกเมื่อ config จริงหาย (16/16 คืนนี้) · `smoke_webhook_spool` แตกโปรเซสจริง 53 ข้อ · `smoke_kb_match` ยิงผ่านทางเข้าจริง 49 ข้อ · `smoke_site_activity_photo` ยิงผ่าน worker จริง 66 ข้อ (สามตัวหลังตามทะเบียน `vendor-ksk/CLAUDE.md` ไม่ได้รันคืนนี้) · จุดที่เคยพังเงียบและมียามแล้ว: ULID (D-P0-08) · `--streak` ตอบ 0 (WP-P4-B) · `Cutover_At` อนาคต (D-P0-24) · ยามที่ตกไม่ได้ (D-P0-14) | ✅ |
| 6 | เอกสาร 3 ไฟล์ตรงกับของจริง: `IMPLEMENTATION_PLAN.md` §2 · `AS_BUILT.md` · `CLAUDE.md` | §2 เขียนใหม่ `e96ab66` · `AS_BUILT.md` 2026-09-10 21:33 · `CLAUDE.md` §0 GitHub-first + §3 สถานะเฟส (แก้คืนนี้: P0/P3 ปิดแล้ว · P5 ไม่สลับเจ้าของ · §1 หัวข้อบอกชัดว่า "Codex เขียน" เป็นรูปแบบเดิม · §8 บั๊กสตรีคปิดแล้ว · §2 ข้อ 10 โควตาวัดใหม่) · §2.4 ตารางหนี้แก้ให้ตรง `f84ff1c` คืนนี้ (ข้อ 2, 3, 4, 6 ปิดแล้ว) · เลขโควตาต่างวันกันสามค่า (610 · 1,103 · 2,381) ระบุวันวัดกำกับทุกที่แล้ว ไม่ใช่ขัดกัน | ✅ |

---

## 3. ใบปิดรายเฟส — อยู่ไหน

| เฟส | ใบปิด | จุดที่ต้องอ่านให้ถูก |
|---|---|---|
| P0 | `WORK_PACKAGES_P0.md` §WP-P0-5 (16/16) · §WP-P0-6 (7 ข้อรายตัว) | ข้อ 4 ผ่านด้วยคำตัดสิน · "ปิด cim-server" หมายถึงเลิกพึ่งพา ไม่ใช่ปิดเครื่อง — cim-server ยังเป็นจุดตายจุดเดียวของทางออก LINE (`AS_BUILT.md` §1) |
| P1 | `WORK_PACKAGES_P1.md` "ลำดับที่ทำไปแล้ว" ข้อ 1–5 · D-P0-12 | วงจรเต็มพิสูจน์ด้วยปุ่มจริง ไม่ใช่ synthetic |
| P2 | D-P0-19 · `WORK_PACKAGES_P2.md` §1 "ประตูของชุด B" | ไม่แต่งขึ้นมาจากโครงสร้าง (SQL ล้วน) ไม่ใช่จากการอ่านคำตอบทีละข้อ |
| P3 | `WORK_PACKAGES_P3.md` (ใบปิด ไม่ใช่ใบสั่งงาน) · D-2026-09-10-ค | ปิดเพราะเจ้าของระบบเคาะ "ไม่เปิดสวิตช์" ไม่ใช่เพราะประตูผ่านครบ 3 ข้อ (② ยังพิสูจน์ด้วยการอ่านซอร์ส) |
| P4 | `WORK_PACKAGES_P4.md` · D-P0-20 · D-P0-29 · D-2026-09-10-จ | ห้ามลบ `L-9` (ลบแล้วสตรีคขาด) · ตัวกรองสถานะใน Worker เป็นตัวซ่อนใบทดสอบแทน |
| P5 | `WORK_PACKAGES_P5.md` §สถานะรวม · D-P0-23/24 · D-2026-09-10-ก | "go live" ≠ บอทเข้ากลุ่มจริง (นั่นเกิด 2026-09-04) · ระบบเครื่องมือ WP-P5-B/C ยังไม่เริ่ม เป็นงานใหม่ |
| ปิดโปรเจกต์ | `PROJECT_CLOSEOUT_PLAN.md` (กล่องสถานะบนสุด) · ไฟล์นี้ | C.2 §2 ใหม่ · C.3 ปิด P3 · C.4 DATA_POLICY เลื่อน ทบทวน 2026-10-15 · 3.13 แผน OA แยกโปรเจกต์ |

---

## 4. คำตัดสินที่ยังผูกมัดหลังปิด (อ่านก่อนเสนออะไรใหม่)

- **D-P0-18** `tbl_task` ฝั่ง LIVE เป็นเจ้าของความจริง · คลาวด์อ่านอย่างเดียว · ห้ามสร้าง endpoint เขียนกลับ
- **D-2026-08-29-ก** ปลายทางยังเป็น Cloudflare · aim คือ jigsaw ชิ้นแรก — ยังมีผล
- **D-2026-09-10-ก** เปลี่ยนทางเดิน: ไม่ย้ายสมอง · ปิดหนี้ก่อน → mirror ตารางอ่านทีละตัว · ด่านหน้าไม่ทำ (ตัวเลข)
- **D-2026-09-10-ข** GitHub เป็นจุดควบคุมเดียว — "ยังไม่อยู่บน `main` = ยังไม่เสร็จ" · ห้ามแก้ไฟล์ใน `vendor-ksk` ตรง ๆ (task ดึงเองทุก 1 นาที)
- **D-2026-09-10-ค** P3 พร้อมแต่ยังไม่เปิดสวิตช์ · เปิด = ใบใหม่ ขออนุมัติใหม่
- **D-2026-09-10-จ** ไม่ลบ `L-9` · ไม่แตะ `tbl_task` เพื่อความสะอาดของสรุป
- `DATA_POLICY.md` 3 ข้อค้าง (consent · สคริปต์ลบ · หน้า export) เลื่อน — ทบทวน **2026-10-15** · ห้ามแตะ GPS จนกว่าจะถึงวันนั้น
- ไม่เก็บข้อความดิบลง D1 (ข้อ 3.8) · แผนยุบ OA เป็นโปรเจกต์แยก (3.13) · ระบบเครื่องมือ (`tbl_tool*`) คงไว้ตามเดิม ไม่ย้ายรอบนี้
- ของก่อน as-built ไม่แตะ: ไทม์โซน `php.ini` · `Taken_At = NOW()` ในรูปงานกลาง (`AS_BUILT.md` §7ข)

---

## 5. พบคืนนี้ นอกขอบเขตปิดโปรเจกต์ — จดไว้ ไม่แก้ในงานนี้ (คำสั่ง 2026-09-10 "ยึดตามแผน")

1. **`selfcheck` แดง 63 รายการจากอีก estate** — `tbl_legacy_cutover` มี 63 แถว `System_Key = db_customs_attic` · `Kind = 'table'` ·
   เป้าหมาย `db_customs.zz_retired_*` · หมุด 2026-09-10 03:10 · `Created_By = attic_retire` — ผู้เขียนคือ `C:\WebApp\ksk-next\bin\attic_retire.php:85-87`
   (สนามผ่า identity ของ KSK/FIM ไม่ใช่ AIM) แต่ `vendor-ksk/lib/legacy_silence.php:165` รับเฉพาะ `LSC_KINDS` (file · log · task · db) ไม่รู้จัก `table`
   ⇒ ยาม 3.7 ตอบ "ตรวจไม่ได้" ทุกวันจนกว่าฝั่ง ksk-next จะเปลี่ยนเป็น `Kind = 'db'` หรือเพิ่มชนิดใน lib (แก้ที่ `KSK-Decepticons` ผ่าน GitHub เท่านั้น)
   · ผลข้างเคียง: `ksk-selfcheck` 07:45 จะฟ้องเรื่องนี้ (state-change ครั้งเดียว)
2. **ไฟล์ทดสอบค้างใน spool** — `C:\ProgramData\ksk_webhook_spool\ev_20260910_110952_0c6f019e4eb09773.json` (299 ไบต์ · 18:09:52)
   เนื้อในเป็น event สังเคราะห์: `message.id = DIAG2…` · `replyToken` ว่าง · `groupId` ขึ้นต้น `Cdiagl` (ไม่ใช่รูปแบบกลุ่มจริง) · ข้อความ "…ใส่รูปนี้ในใบงาน 145"
   ⇒ เศษจากการทดสอบฟีเจอร์แนบรูป (`19468d2` 19:03) ไม่ใช่ข้อความคนจริงที่หาย · `ws_purge()` จะลบเองเมื่ออายุเกิน 24 ชม. ตอน webhook ถัดไปสุ่มเรียก
   (`lib/webhook_spool.php:180-188`) · ไม่พบสคริปต์ในรีโปที่สร้าง `DIAG` ⇒ ผู้สร้างคือเซสชัน/เอเจนต์ที่ยิงตรง — **ไม่ได้ลบให้ เพราะไม่ใช่ของที่เซสชันนี้สร้าง** (ดู checklist ข้อ 7)
3. **`vendor-ksk/CLAUDE.md:439`** ยังเขียนลำดับ "เมื่อมีคนเข้า/ออก: `aim_person_map_sync.php` → `aim_link_person.php --all` → `--check`" เป็นขั้นบังคับ — หลัง `26af4f5` สองขั้นแรกเป็นทางสำรอง ไม่ใช่งานประจำ (บรรทัด `:433-438` จดเรื่อง autolink ไว้แล้ว เหลือบรรทัดเดียวที่ยังพูดแบบเก่า) · แก้ผ่าน GitHub รอบหน้า
4. **เลขสตรีคที่คาดไว้ในบันทึกเปิดเซสชัน** ("รอบ 23:30 คืน 10 ก.ย. ควรได้ 13") **ผิดเลข** — วันที่ 10 ก.ย. มีบันทึกสะอาดแล้ว 1 รอบก่อน 23:30 (ตาราง `streak_now`)
   ⇒ รอบ 23:30 คืนนี้ทำให้ 10 ก.ย. เป็น 2/2 สตรีคยัง **12** · จะเป็น 13 หลังรอบ 23:30 ของ **11 ก.ย.**

---

## 6. Checklist ส่งมอบ (C.5) — เจ้าของระบบกดเอง ~15 นาที

กติกา: ทำตามลำดับ · ทุกข้อมี "ต้องเห็น" — ถ้าไม่ตรง หยุดแล้วบอกน้องกุ้ง ไม่ต้องแก้เอง · คำสั่งอ่านอย่างเดียว **ยกเว้น** ข้อ 1 (push) · ข้อ 7 (ถ้าเลือกลบไฟล์ DIAG) · ข้อ 8 (ยามบางตัวเขียนแถวทดสอบแล้วลบเอง ไม่แตะ `tbl_task`) · ข้อ 13 (ประกาศปิด)

| ☐ | ขั้น | คำสั่ง (PowerShell) | ต้องเห็น |
|---|---|---|---|
| ☐ | 1 · ส่งของขึ้น GitHub | `git -C C:\WebApp\aim log --oneline -3` แล้ว `git -C C:\WebApp\aim push origin main` | commit บนสุดคือใบปิดนี้ (`docs: ใบปิดโปรเจกต์ …`) · push ผ่าน ไม่มี reject |
| ☐ | 2 · build เอง | รอ ~1 นาที แล้ว `curl.exe -s https://api.github.com/repos/mottosolar-gif/mottosolar-gif-aimanager/commits/main/check-runs \| Select-String conclusion` | `"conclusion": "success"` ของ `Workers Builds: aim-ingest` |
| ☐ | 3 · deploy ตรง HEAD | `curl.exe -s https://aim-ingest.mottosolar.workers.dev/healthz` และ `git -C C:\WebApp\aim rev-parse --short=12 HEAD` | `ok:true` · `version` = 12 ตัวของ HEAD · `queueDepth 0` · `deadJobs 0` · `strandedEvents 0` |
| ☐ | 4 · ฝั่ง LIVE สะอาดและดึงเอง | `git -C C:\WebApp\vendor-ksk status -sb` และ `Get-Content C:\WebApp\vendor-ksk\logs\git_pull_deploy.log -Tail 3` | `## main...origin/main` ไม่มีไฟล์แก้ค้าง · log บรรทัดล่าสุด `deployed … -> a3f5d28` (หรือใหม่กว่า ถ้ามี push ฝั่งนั้น) |
| ☐ | 5 · ประตู P4 ยังเดิน (หลัง 23:30) | `php C:\WebApp\vendor-ksk\bin\aim_reconcile.php --streak` | คืน 10 ก.ย.: **12 วัน** (10 ก.ย. = 2/2) · คืน 11 ก.ย. ขึ้นไป: **13+** · บรรทัด "ประตู P4 ต้องการ 7 วันติด ⇒ ผ่านแล้ว" |
| ☐ | 5ข · ประตูปิดขั้นที่ 1 (ทำวันที่ **17 ก.ย. หรือหลังจากนั้น**) | `php C:\WebApp\vendor-ksk\bin\aim_reconcile.php --streak` | ≥ 19 วัน · ไม่มีวันไหนหลัง 10 ก.ย. ถูกนับเป็น "ไม่ตรง" หรือ "ยามไม่ได้รัน" ⇒ ตัวนับใหม่นับถูก 7 รอบติด — จดผลลงกล่องสถานะ `PROJECT_CLOSEOUT_PLAN.md` |
| ☐ | 6 · แถวดิบ | `php C:\WebApp\vendor-ksk\bin\streak_now.php` | ทุกวันตั้งแต่ 30 ส.ค. "สะอาด" · "บันทึกล่าสุด … ห่างจากวันนี้ **0 วัน** ถ้ากดหลัง 23:30 (หรือ **1 วัน** ถ้ากดก่อนรอบของวันนั้น — ไม่ใช่ความผิดปกติ) |
| ☐ | 7 · ยามเฝ้ายาม | `php C:\WebApp\vendor-ksk\bin\selfcheck.php --dry` | ส่วน AIM: `aim-worker: HTTP 200` · `คิว 0 · ตายถาวร 0 · ค้าง 0` · `รายชื่อคุยกับน้องกุ้ง: 12 คน · ตรงกับฐาน` · จำนวนปัญหารวมคาดว่า **63** (§5 ข้อ 1 ของอีก estate) หรือ **64** ถ้าไฟล์ DIAG ยังไม่ถูกกวาด — **ถ้าต้องการให้เป็น 63 ทันที** ลบเอง: `Remove-Item C:\ProgramData\ksk_webhook_spool\ev_20260910_110952_0c6f019e4eb09773.json` (ดูเนื้อในก่อนด้วย `Get-Content`) |
| ☐ | 8 · ยาม AIM ฝั่ง PHP | ใน `C:\WebApp\vendor-ksk`: `php bin\smoke_escalation.php; php bin\smoke_aim_ask.php; php bin\smoke_aim_notify.php; php bin\smoke_aim_forward.php; php bin\smoke_reconcile_streak.php; php bin\smoke_reconcile_gate_notify.php; php bin\smoke_person_registry.php; php bin\smoke_legacy_silence.php` | 32 · 67 · 28 · 16 · 25 · 12 · 19 · 45 — ตก 0 ทุกตัว (เลขห้ามลดลง) |
| ☐ | 9 · ยาม Worker | ใน `C:\WebApp\aim`: `npm run typecheck; npm test` | typecheck ไม่มี error · `Tests 311 passed` (หรือมากกว่า) |
| ☐ | 10 · ทะเบียนคน | `php C:\WebApp\vendor-ksk\bin\aim_person_map_sync.php --check` | `ตรงกับฐาน · 12 คน` (หรือจำนวนคนปัจจุบันในหน้า users.php) |
| ☐ | 11 · เส้นทางจริงจากมือถือ | พิมพ์ในแชท 1:1 กับน้องกุ้ง: `งานค้างของฉัน` | ตอบเป็นภาษาไทยลงท้าย ค่ะ ภายในไม่กี่วินาที · ไม่มี "บัญชียังไม่เปิด" · ตัวเลขตรงกับหน้า tasks.php |
| ☐ | 12 · เช้าวันถัดไป | ดู LINE ตอน 08:00 และ `Get-Content C:\WebApp\vendor-ksk\logs\selfcheck.log -Tail 20` (ถ้ามีไฟล์) | สรุปเช้ามาถึงคน 4 คน · `ksk-selfcheck` 07:45 ไม่ฟ้องส่วน AIM |
| ☐ | 13 · ประกาศปิด | บอกน้องกุ้ง "ส่งมอบแล้ว" หรือแก้กล่องสถานะบนสุดของ `docs/PROJECT_CLOSEOUT_PLAN.md` เป็น `✅ ส่งมอบแล้ว <วันที่>` · commit · push | กล่องสถานะบน GitHub ตรงกับความจริง |

ถ้าข้อ 1–11 ผ่านครบในคืนเดียว ถือว่า **C.5 เสร็จ** — ข้อ 12 เป็นการยืนยันซ้ำเช้าถัดไป ไม่บล็อกการปิด
