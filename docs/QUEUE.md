# คิวงานสำหรับ Codex — หยิบจากบนลงล่าง

> ## 🔴 ตั้งแต่ 2026-09-10 ไฟล์นี้ไม่ใช่คิวงานหลักแล้ว
> **งานที่เหลือทั้งหมดอยู่ที่ `docs/PROJECT_CLOSEOUT_PLAN.md`** — ใครก็หยิบงานจากที่นั่นได้ (Codex ·
> เอเจนต์ Claude ตัวไหนก็ได้ · เซสชันคลาวด์) ไม่ได้ผูกกับ Codex อย่างเดียวอีกต่อไป (ดู `CLAUDE.md` §0/§1 · D-2026-09-10-ข)
> **กติกาตัดสินสถานะงาน = "ยังไม่อยู่บน `main` = ยังไม่เสร็จ"** — commit ที่ค้างอยู่ในเครื่องใดเครื่องหนึ่งไม่นับว่าเสร็จ
> **deploy อัตโนมัติเมื่อ push แล้ว ไม่ต้องกดมือ** — ฝั่ง `aim` ผูก Cloudflare Workers Builds ตรงกับ repo
> (push `main` → build + deploy เอง) · ฝั่ง `vendor-ksk` มี task `ksk-git-pull` ดึงจาก `main` ทุก 1 นาที
> **ไฟล์นี้เก็บไว้ต่อเพราะยังมี "ข้อห้าม" (ด้านล่าง) + เลขเทสฝั่ง LIVE ที่ยังอ้างอิงได้ — ไม่ใช่ที่หยิบงานใหม่อีกต่อไป**

> อัปเดตล่าสุด **2026-09-05 ดึก** (เนื้อหาด้านล่างเป็นของเดิม — ยังใช้อ้างอิงได้ ดูกล่องแดงด้านบนก่อนหยิบงานใหม่)
> เดิม: **Claude เขียนใบสั่ง+ตรวจ · Codex ลงมือ · ห้ามตรวจงานตัวเอง**
> ทำเสร็จใบไหน ให้ commit แล้วเขียนสรุปสั้น ๆ ว่า "พิสูจน์ว่ายามตกได้จริงอย่างไร"
> **ห้ามข้ามลำดับ · ห้ามทำหลายใบพร้อมกัน**
>
> ⚠ **ไฟล์นี้เคยล้าหลังจนสั่งงานซ้ำ** — 2026-09-04 พบว่า WP-P4-A/B **ทำเสร็จไปแล้ว** ตั้งแต่ก่อนหน้านั้น
> แต่คิวยังเขียนว่า "ยังไม่มีใครทำ" · เกือบมีคนลงมือทำซ้ำ
> **ก่อนหยิบใบไหน ให้เปิดของจริงใน `mottosolar-gif/KSK-Decepticons` ดูก่อนเสมอ ไม่ใช่เชื่อคิวนี้**

## ✅ ทำเสร็จแล้ว (ตรวจของจริงที่ `KSK-Decepticons` 2026-09-04)

| ใบ | หลักฐาน |
|---|---|
| **WP-P4-B** นับสตรีค | `bin/aim_reconcile.php --streak` + `rec_streak_days()` · ยาม `smoke_reconcile_streak.php` **แถม** `rec_gate_should_notify()` ที่แจ้ง LINE อัตโนมัติเมื่อประตูเปิด + `smoke_reconcile_gate_notify.php` (ใบสั่งไม่ได้ขอ) |
| **WP-P4-A** เก็บ meta ของรูปงาน | `sql/053_task_photo_meta.sql` · ยาม `smoke_task_photo_meta.php` |
| **WP-P4-C** เอกสาร consent/retention | 📄 `aim/docs/DATA_POLICY.md` ถูกอ้างถึงจาก `KSK-Decepticons/CLAUDE.md:307` ⇒ มีอยู่จริงในเครื่อง **แต่ยังไม่ถูก push ขึ้น GitHub** — รีโปนี้ยังไม่มีไฟล์นั้น |

### ✅ ปิดเพิ่มวันนี้ (2026-09-10) — ตรวจซ้ำในรีโปจริงแล้วทั้ง 3 ข้อ

| ของที่ปิด | หลักฐาน |
|---|---|
| **`person_link` ผูกเองอัตโนมัติเมื่อคลาวด์ตอบ 403** (ปิดหนี้ D-P0-14 ข้อ 1) | `KSK-Decepticons` commit `26af4f5` (2026-09-10 20:29) — `aim_ask_handle()` เรียก `aim_ask_autolink()` แล้วถามซ้ำครั้งเดียว, บ้านเดียวของ `POST /link` คือ `aim_ask_link_person()` (`lib/aim_ask.php`) · ยาม `bin/smoke_aim_ask.php` โตจาก 59 → **67/67** (ดู `PROJECT_CLOSEOUT_PLAN.md` 0.1: "mutation แดง 3 ข้อจริง") |
| **ตัวนับสตรีค P4 แยก "ไม่ตรง / ยังไม่ถึงรอบวัด (`pending_today`) / ยามไม่ได้รัน (`unknown`)"** | `KSK-Decepticons` commit เดียวกัน `26af4f5` — `rec_streak_days()` ใน `bin/aim_reconcile.php:80-135` เพิ่ม flag `pending_today`/`unknown` + `rec_gate_should_notify()` (`:154-157`) บล็อกเปิดประตูเมื่อ `unknown=true` · รันจริงวันนี้: `php bin/smoke_reconcile_streak.php` → **25/25 ผ่าน** (เดิม 8 ข้อ — ตัวเลข "8" ในตารางเทสด้านล่างยังไม่อัปเดต ดูหมายเหตุ) — ปิดบั๊กที่ค้างมาตั้งแต่ D-P0-25 Q7 |
| **รวมสายเอกสารสองเครื่องเป็นสายเดียว** (เลขคำตัดสิน D-P0 ชนกัน 3 รอบ) | repo `aim` commit `7d6ccb8` (2026-09-10 20:30, อยู่บน `main`/`origin/main` แล้ว) "merge: รวมสายเอกสารสองเครื่อง + เลขคำตัดสินแบบวันที่ + as-built และแผนปิดโปรเจกต์" — ผลลัพธ์คือตารางแปลงเลขที่หัว `docs/DECISIONS_P0.md` + `docs/AS_BUILT.md` + `docs/PROJECT_CLOSEOUT_PLAN.md` |

### ✅ ปิดแล้ว — ประตู P4 เปิด (2026-09-05 ดึก, D-P0-20)

**คำถามเดิม "สตรีค P4 เดินมากี่วันแล้วจริง ๆ" ตอบแล้ว: ครบ 7 วัน ตรงกัน 100%**
✅ **ยืนยันด้วยแถวดิบจริงแล้ว 2026-09-09** (`bin/streak_now.php` รันกับฐานจริง — เห็น `tbl_notify_log` สด
10 วันติดกัน 30 ส.ค.–8 ก.ย. นับได้ 7 พอดีตอน 5 ก.ย.) ไม่ใช่แค่เชื่อข้อความแจ้งอัตโนมัติอีกต่อไป
รายละเอียด ดู `DECISIONS_P0.md` D-P0-29 (ต่อจาก D-P0-20)

### ✅ ปิดแล้ว (2026-09-10) — บั๊กใน `--streak` (เจอ 2026-09-04)

`rec_streak_days()` เดินถอยหลัง**จากวันนี้** แต่งานจริงรัน 23:30 ⇒ กด `--streak` ก่อนสี่ทุ่มครึ่ง
**ตอบ 0 เสมอ** ทั้งที่สตรีคจริงอาจเดินมาหลายวัน · เส้นทางแจ้งอัตโนมัติไม่กระทบ (บันทึกก่อนนับ)
แต่คำสั่งที่คนกดเพื่อถามว่า "ผ่านประตูหรือยัง" ตอบผิดวันละ 23 ชม.ครึ่ง
⇒ มีแพตช์พร้อมแล้ว **ยังไม่ได้แปะ** · รายละเอียดใน `WORK_PACKAGES_P4.md` §WP-P4-B

⚠ **2026-09-09:** พี่เต้ตอบว่า "แก้แล้ว" (D-P0-25 Q7) — ตรวจซ้ำที่ `KSK-Decepticons` `main` HEAD `09cdf12`
**ไม่พบการแก้จริง**: `rec_streak_days()` (`bin/aim_reconcile.php:66-96`) ยังเริ่มนับที่ `$i=0` (วันนี้) และ
`break` ทันทีถ้าวันนี้ยังไม่มีแถว — พฤติกรรมเดิมเป๊ะ · ไม่มี commit/PR ไหนพูดถึง streak fix เลย (เช็คแล้วทั้ง
`git log --all --grep=streak` และ PR #1–#11 ทั้งหมด) — คงสถานะ 🔴 ค้างไว้ ให้พี่เต้เช็คว่าสับสนกับแพตช์อื่น
หรือแพตช์อยู่ในเครื่องบ้านที่ยังไม่ push

✅ **2026-09-10 ปิดจริงแล้ว** — `KSK-Decepticons` commit `26af4f5` (20:29) แก้ `rec_streak_days()` ให้แยก
`pending_today` (ยังไม่ถึงรอบวัด 23:30) ออกจาก `unknown` (เลยรอบแล้วไม่มีบันทึก = วัดไม่ได้) ออกจาก "ไม่ตรงจริง" ·
`rec_gate_should_notify()` ห้ามเปิดประตูเมื่อ `unknown=true` แม้ `days≥7` · รันยามจริงวันนี้จาก
`C:\WebApp\vendor-ksk`: `php bin/smoke_reconcile_streak.php` → **✅ PASS ผ่าน 25 · ตก 0** (ดูตาราง "ปิดเพิ่มวันนี้"
ด้านบน) — ปิด ❓ ของ D-P0-25 Q7 ได้จริงแล้ว ไม่ใช่แค่คำยืนยันปากเปล่าอีกต่อไป

## ✅ WP-P5-A ปิดแล้ว — sabotage-verify เองจริงแล้ว (2026-09-09, D-P0-24)

`sql/063_legacy_cutover.sql` · `lib/legacy_silence.php` · `bin/legacy_silence_check.php` ทำจริงตั้งแต่ `860a93a`
audit แก้บั๊ก critical แล้วที่ `98d5b75` — **เซสชันคลาวด์นี้ revert บั๊กนั้นเองในแซนด์บ็อกซ์ MariaDB แยกต่างหาก
(ไม่แตะของจริง) → `bin/smoke_legacy_silence.php` ตกตรงเป้า 1 ข้อจาก 43/45 → คืนโค้ด → เขียว 43/45 เท่าเดิม**
รายละเอียดเต็มดู D-P0-24 · เลข "32/32" ที่ commit เดิมอ้างล้าหลังแล้ว ไฟล์จริงมี 45 เคส (2 ข้อตกเพราะต้องการ
`schtasks` ของ Windows ไม่ใช่บั๊ก)

## 🔨 ทำได้เลย — ใบบนสุดของคิวตอนนี้

**ใบเดียวที่เคยอยู่ตรงนี้ (แปะแพตช์บั๊ก `--streak`) ปิดแล้ววันนี้ 2026-09-10** — ดูตาราง "ปิดเพิ่มวันนี้" ด้านบน
ตอนนี้ไม่มีใบด่วนค้างอยู่ในไฟล์นี้อีก **⇒ หยิบงานถัดไปจาก `docs/PROJECT_CLOSEOUT_PLAN.md` แทน** (ดูกล่องแดงหัวไฟล์)

> ⚠ **สองสายเคยเขียนสถานะขัดกัน — เก็บไว้ทั้งคู่ตามวันที่ อย่าเชื่อข้อเดียว** (รวมสาย 2026-09-10)
>
> | เขียนเมื่อ | ว่าอย่างไร | สถานะ |
> |---|---|---|
> | **2026-08-29** (สายเครื่องนี้) | *"ทั้ง 3 ใบของ P4 เสร็จแล้ว — ไม่มีใบว่างในคิว"* รวมทั้ง **WP-P4-B ✅ เสร็จ** | ⏪ เก่ากว่า |
> | **2026-09-09** (สาย `origin/main`) | บั๊ก `--streak` ของ WP-P4-B **ยังค้างจริง** — ตรวจซ้ำที่ `KSK-Decepticons` HEAD `09cdf12` ไม่พบการแก้ (ดูหัวข้อ 🔴 ด้านบน) | ✅ **อันนี้คือของปัจจุบัน** |
>
> ⇒ WP-P4-A และ WP-P4-C ปิดจริงทั้งคู่ (ไม่มีใครเถียง) · **WP-P4-B ปิดแค่ตัวฟีเจอร์ `--streak` แต่บั๊กของมันยังเปิดอยู่**

**ของที่ส่งมอบจริงของ P4 ทั้ง 3 ใบ (บันทึกไว้ 2026-08-29 · ข้อมูลนี้มีเฉพาะในสายของเครื่องนี้ ห้ามทิ้ง):**

| ใบ | สถานะ | ของที่ส่งมอบ | พิสูจน์ว่ายามตกได้จริงอย่างไร |
|---|---|---|---|
| **WP-P4-B** | 🟠 ฟีเจอร์เสร็จ · **บั๊กยังค้าง** (ดูกล่องข้างบน) | `--streak` ใน `bin/aim_reconcile.php` · `bin/smoke_reconcile_streak.php` (8 ข้อ) | ก่อกวนเป็น `GROUP BY Sent_At` → ตกจริง (เดิมเทสแยกไม่ออกเพราะ seed เวลาเหมือนกันหมด แก้ให้ต่างกันแล้ว) |
| **WP-P4-A** | ✅ เสร็จ | `sql/053_task_photo_meta.sql` (ลงฐานแล้ว) · `tk_photo_taken_at()` + `tk_pending_photo_claim($senderUserId)` · `bin/smoke_task_photo_meta.php` (19 ข้อ) | ก่อกวน 3 แบบ ตกทุกแบบ: fallback filemtime → ตก 3 · sender 0 แทน NULL → ตก 1 · hash ไฟล์ต้นทาง → ตก 2 · คืนไฟล์แล้ว diff ตรงต้นฉบับ |
| **WP-P4-C** | ✅ เสร็จ | `docs/DATA_POLICY.md` | ไล่จากตารางจริงทั้ง 24 ตาราง + ยิง D1 จริง · **ระบุสิ่งที่ยังทำไม่ได้ไว้ครบ** (ไม่มีสคริปต์ลบ · ไม่มี export · ไม่มี consent) |

**ที่เจอระหว่างทำ WP-P4-C — ต้องมีใบสั่งของตัวเอง:**
1. `tbl_userlog` 458,663 แถว เก็บ IP รายคน **ไม่มีตัวกวาด ไม่มีใครเคาะอายุ**
2. คลาวด์มีคอลัมน์ `task.location_lat`/`lng` ค้างอยู่ — ไม่มีใครเขียน (0 แถว) แต่ช่องเปิดค้าง
3. `tbl_bridge_read` 849 แถว ไม่มีตัวกวาด — **`CLAUDE.md` ทำให้เข้าใจผิดว่ามี** (ของจริงกวาดแต่ `tbl_bridge_event`)

## รอสัญญาณก่อน (ห้ามเริ่ม)

| ใบ | รออะไร | เช็คยังไง |
|---|---|---|
| WP-P5-B · C (ย้ายระบบจริง) | ~~P4 ผ่านประตู~~ **✅** (D-P0-20) · ~~WP-P5-A ทำเสร็จ~~ **✅ ตรวจจริงแล้ว** (D-P0-24) — ตอนนี้รอ **2 ข้อสุดท้าย**: ~~ใครเขียน `tbl_tool` ตอนนี้~~ **✅** (D-P0-26: `lib/tools.php` 10 จุด · `tools.php` 1 จุด) แต่ `tbl_tool_loan` ยังไม่เช็ค · ~~ใครเป็นเจ้าของความจริงหลังย้าย~~ **✅ ปิดแล้ว** (D-P0-27: ปลายทาง Cloudflare แต่ระหว่างเปลี่ยนผ่านทำแบบ `tbl_task` — LIVE เป็นเจ้าของชั่วคราว) · มีช่วงเขียนพร้อมกันสองฝั่งไหม ❓ | `tbl_tool_loan` + concurrent-write window ยังไม่มีใครตอบ — ต้องถามฝั่ง KSK/พี่เต้ก่อนร่างใบสั่งงาน B/C จริง |

---

## ⛔ ข้อห้ามที่ใช้กับทุกใบ (อ่านทุกครั้ง)

**1. ห้ามเปลี่ยนพฤติกรรมของสิ่งที่กำลังถูกเทียบ**
ตัวเทียบกำลังนับ 7 วันติดอยู่ · แตะแล้วตัวนับรีเซ็ต P4 เริ่มใหม่
ห้ามแตะ: `bin/aim_task_sync.php` · `lib/escalation.php` · สถานะงานใน `tbl_task` · ตารางแปลงสถานะ

**2. ยามที่ตกไม่ได้ ไม่ใช่ยาม**
ทุกของใหม่ต้องมีเทส และต้อง **ทำให้ตกจริงก่อน** แล้วค่อยแก้ให้ผ่าน
รายงานผลต้องบอกว่า "sabotage อะไร → ตกกี่ข้อ → คืน → ผ่านเท่าไร"
⚠ ตัว sabotage ต้องยืนยันว่าแทนที่สำเร็จก่อนเสมอ — เคยพลาด: sed ไม่ตรงช่องว่าง แล้วรายงานว่าเขียว
ทั้งที่ไม่ได้แก้อะไรเลย ผลออกมาเหมือนกันเป๊ะกับตอนยามแข็งแรงจริง

**3. จำนวนเทสห้ามลดลง**
✅ ฝั่งคลาวด์ `npm test` **284** (รันเองที่ HEAD `252be4f` · 2026-09-03 — เลขนี้ยังไม่ได้ตรวจซ้ำรอบนี้)
📄 ฝั่ง LIVE — อัปเดต **2026-09-10** จากตารางยามจริงใน `vendor-ksk/CLAUDE.md` (เปิดไฟล์อ่านเอง ไม่ได้รันทุกตัว):
`smoke_aim_ask` **67** (เดิม 59 — โตขึ้นเพราะ `person_link` auto-link ใหม่ ดู "ปิดเพิ่มวันนี้" ด้านบน) ·
`smoke_kb_match` 49 · `smoke_fim_qa` 43 · `smoke_aim_mail_cmd` 40 · `smoke_attend_claim` **67** (เดิม 34) ·
`smoke_aim_notify` **28** (เดิม 27) · `smoke_line_register` **23** (เดิม 20) · `smoke_task_photo_meta` 19 ·
`smoke_reconcile_gate_notify` 12 ·
`smoke_reconcile_streak` **25** (เดิม 8 — ✅ รันจริงวันนี้แล้ว ไม่ใช่แค่ลอกตาราง ดูหัวข้อ `--streak` ด้านบน ·
ตารางยามใน `vendor-ksk/CLAUDE.md` เองยังเขียน "8 ข้อ" ค้างอยู่ ณ ตอนตรวจ — เอกสารฝั่งนั้นเองล้าหลังกว่าโค้ดจริง) ·
`smoke_person_registry` **19** (ของใหม่ ไม่เคยอยู่ในลิสต์นี้มาก่อน) ·
`smoke_site_activity_photo` **66** (ของใหม่ ไม่เคยอยู่ในลิสต์นี้มาก่อน)
❓ **ตรวจไม่ได้รอบนี้:** `smoke_escalation` (เคยเขียน 32) และ `smoke_tasks_render` (เคยเขียน 36) — ไฟล์ทั้งสอง
ยังมีอยู่จริงใน `vendor-ksk/bin/` แต่ **ไม่ปรากฏในตารางยามปัจจุบันของ `vendor-ksk/CLAUDE.md` อีกแล้ว** (อาจถูกรวม/ย้ายที่พูดถึง
หรือแค่ตกหล่นจากตาราง) — ไม่รันเองในรอบนี้ (ขอบเขตงานนี้จำกัดแค่ docs) เลข 32/36 เดิม **ถือว่ายังไม่ยืนยัน ห้ามเชื่อ 100%**
⚠ เลข `smoke_aim_ask` ในไฟล์นี้เคยเขียนว่า **41** ซึ่งล้าหลังไป 18 ข้อ — ฝั่ง LIVE เดินหน้าไปแล้ว
**เลขชุดนี้ต้องลอกจาก `KSK-Decepticons/CLAUDE.md` ทุกครั้ง อย่าจำเอง**

**4. ห้ามยิง LINE หาคนจริงระหว่างทดสอบ**
ใช้ `--dry` หรือ `--test` เสมอ · `--test` ส่งถึงพี่เต้คนเดียวและไม่จดลงฐาน
⚠ เคยพลาดมาแล้ว 2 ครั้งในวันเดียว: รัน `task_escalate` ไม่ใส่ `--test` (ถึงพนักงานจริง 2 คน)
และ `smoke_line_register` ที่ยิงแจ้งเตือนความปลอดภัยปลอมถึงพี่เต้ (แก้แล้ว)

**5. ค่าที่อ่านไม่ได้ ให้เป็น NULL ห้ามเดา**
กติกาเดิมของบ้านนี้ตั้งแต่เคส KSK000197 — ตัวอ่านไฟล์เดาเลข invoice แล้วพิมพ์ลงเอกสารจริง

**6. ไม่รู้จัก = ปฏิเสธ ไม่ใช่ปล่อยผ่าน**
`aip_decide()` คืน ASK สำหรับสิ่งที่ไม่มีในทะเบียน · ด่านใหม่ทุกตัวต้อง fail-closed แบบเดียวกัน

---

## ประตูที่ต้องรันก่อนบอกว่าเสร็จ (ทุกใบ)
```
php bin/aim_reconcile.php          ต้องตรงกัน 100%
php bin/selfcheck.php --dry        ต้อง 0 ปัญหา
php bin/smoke_escalation.php       32   # ❓ ไม่อยู่ในตารางยามของ vendor-ksk/CLAUDE.md แล้ว ณ 2026-09-10 — เลขนี้ยังไม่ยืนยันซ้ำ
php bin/smoke_tasks_render.php     36   # ❓ เหมือนกัน — ไม่อยู่ในตารางยามปัจจุบัน ยังไม่ยืนยันซ้ำ
php bin/smoke_aim_ask.php          67   # อัปเดต 2026-09-10 (เดิม 41 แล้วเคยแก้เป็น 59 — ตอนนี้ 67 ตามตารางยามจริง)
php bin/smoke_aim_notify.php       28   # อัปเดต 2026-09-10 (เดิม 27)
cd C:\WebApp\aim && npm run typecheck && npm test && npm run lint
```

## เอกสารที่ต้องอ่านก่อนตัดสินใจอะไร
- `C:\WebApp\aim\CLAUDE.md` — กติกาประจำรีโป
- `C:\WebApp\vendor-ksk\CLAUDE.md` — กติกาฝั่ง LIVE (ยาวแต่ต้องอ่าน)
- `docs/DECISIONS_P0.md` — **คำตัดสินที่ปิดไปแล้ว ห้ามรื้อโดยไม่มีข้อเท็จจริงใหม่**
  (เคยพลาด 2 ครั้ง: ถามเรื่องที่ D-P0-06 และ IMPLEMENTATION_PLAN ตอบไว้แล้ว)
- `docs/IMPLEMENTATION_PLAN.md` §6 — ลำดับเฟสและประตู **ห้ามข้ามประตู**
