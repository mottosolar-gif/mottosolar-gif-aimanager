# IMPLEMENTATION_PLAN v2 — AI Manager (บ้านอยู่นอกเครื่องนี้)

เขียน 2026-08-27 · ~~**แผนเท่านั้น ยังไม่มีโค้ดแม้บรรทัดเดียว**~~ **ล้าสมัยแล้ว — ระบบขึ้น production และมีคนใช้จริงทุกวัน**
v1 (บ้านใหม่บน cim-server) ถูกแทนที่ด้วยฉบับนี้ตามคำสั่งพี่เต้ 2026-08-27
⚠ **§2 ถูกเขียนใหม่ 2026-09-10 ให้ตรงของจริง** (as-built) · แผนสถาปัตยกรรมฉบับ 2026-08-27 ย้ายไปเป็น **§2-เดิม**
· ส่วนอื่นของไฟล์นี้ยังเป็น "แผน" ตามเดิม — สภาพจริงเทียบรายชั้นอยู่ที่ `docs/AS_BUILT.md`

## คำสั่งที่ยึด

| ข้อ | พี่เต้ตัดสิน | ผลต่อการออกแบบ |
|---|---|---|
| ① | AI พูดกับทุกคนได้ รวมพี่เต้ | ต้องมีชั้นสิทธิ์ว่าใครถามอะไรได้ (ลูกน้องเห็นเฉพาะงานตัวเอง) |
| ② | เตือน+เสนอได้เอง · เคาะงานง่าย ๆ เองได้ (แบบใบลาวันนี้) | ชั้นกฎ 3 ระดับ AUTO / PROPOSE / ASK |
| ③ | **บ้านอยู่นอกเครื่องนี้** — ใช้ของฟรีข้างนอก (Cloudflare · GitHub · homelab · Notion · Firebase ฯลฯ) · ฝั่งนี้เหลือแค่ **ตัวอ่านอย่างเดียว** แล้วทำทางให้บ้านใหม่มาอ่าน | เปลี่ยนสถาปัตยกรรมทั้งหมด (แผนที่วางไว้ = §2-เดิม · **ของจริงที่รันอยู่ = §2**) |
| ④ | OA ตัวเดิม + **แอดเข้ากลุ่มใหม่ไว้ทดสอบ** | ไม่ต้องขอ OA ใหม่ · แต่ต้องมีตัวแยกว่าข้อความมาจากกลุ่มทดสอบหรือกลุ่มจริง |
| ⑤ | ทดสอบเต็มระบบ | ไม่ทำแค่ pilot เฉพาะใบลา |

---

## 1. ของฟรีที่ estate นี้ **เป็นเจ้าของอยู่แล้ว** (VERIFIED — ไม่ต้องสมัครใหม่)

| ของ | สถานะจริง | หลักฐาน |
|---|---|---|
| **Cloudflare (บัญชีพี่เต้)** | มี Worker **`mottosolar-line-relay`** รันจริงตั้งแต่ 7 ส.ค. — รับ LINE webhook แล้วพักใน Durable Object ให้ปลายทางมาดึง | `docs/DECISIONS_homelab.md` §10.1 |
| **GitHub** | repo จริง `mottosolar-gif/KSK-Decepticons` (vendor-ksk push อยู่) | `git remote -v` |
| **homelab (เต้น้อย)** | เครื่องจริง · Docker · MCP server (read-only) · **ถือ token OA กลุ่ม** · heartbeat เข้ามาทุก 15 นาที ล่าสุด 23:42 | `tbl_bridge_event` 646 แถว |
| **โดเมน + HTTPS** | `www.fimandksk122.com` ใช้งานจริง + cloudflared quick tunnel เป็นตัวสำรองอัตโนมัติ | `bin/tunnel_watchdog.ps1` |
| **Tailscale** | เชื่อม cim-server ↔ homelab อยู่แล้ว | `100.79.23.116` ↔ `100.97.46.101` |

### ⚠ ข้อจำกัดที่ต้องรู้ก่อนออกแบบ (VERIFIED)

1. **โควตา LINE push — ไม่ใช่ข้อจำกัดจริง (แก้ 2026-08-27)**
   ถามจาก LINE API ตรง ๆ: `GET /v2/bot/message/quota` → `{"type":"limited","value":15000}` · `quota/consumption` → `{"totalUsage":610}`
   ⇒ **15,000 ข้อความ/เดือน ใช้ไป 610 เหลือ 14,390** (พี่เต้ทักเอง 27 ส.ค. ว่าตัวเลขที่ผมอ้างผิด — ตรวจแล้วพี่เต้ถูก)
   ⚠ `TEAM_TOOLS_INVENTORY.md` §A9 ยังเขียนว่า *"300/เดือน ใช้ไป ~200 งบตึง"* — **ตัวเลขนั้นผิด/ล้าสมัย และเอกสารนั้นถูกเสิร์ฟให้ฝั่ง homelab อ่านด้วย** ⇒ ต้องแก้ ไม่งั้นทั้งสองฝั่งจะออกแบบรัดเกินจริง
   ⇒ ประเมินภาระจริง: 11 คน × 30 วัน × 10 ข้อความ ≈ 3,300/เดือน = ~22% ของโควตา ⇒ push รายคนได้สบาย
   ⇒ ยังคงหลัก "reply ก่อน push" ไว้ (reply ไม่นับโควตาเลย) และส่งกลุ่ม = 1 ข้อความไม่คูณคน — แต่เป็นเรื่องความสุภาพต่อผู้ใช้ ไม่ใช่เพราะโควตาบีบ
2. **เครื่องนี้ deploy Cloudflare เองไม่ได้** — ไม่มี `wrangler` ไม่มี API token · MCP ที่ต่อ Cloudflare มีแต่เครื่องมืออ่าน
   ⇒ ต้องได้ token สิทธิ์ `Workers Scripts:Edit` หรือให้ homelab เป็นคน deploy
3. **ของฟรีเปลี่ยนเงื่อนไขได้เสมอ** — เอกสารบ้านนี้เขียนเตือนตัวเองไว้แล้ว ⇒ ทุกชิ้นต้อง **ย้ายบ้านได้** (ดู §5)

---

## 2 · สถาปัตยกรรมที่รันจริง (as-built 2026-09-10)

> หัวข้อนี้คือ **ของจริงที่เดินอยู่ทุกวัน** · แผนเดิมของ 2026-08-27 ย้ายลงไปเป็น **§2-เดิม** ด้านล่าง (เก็บเป็นประวัติ ห้ามลบ)
> หลักฐานรายบรรทัด + ตัวเลขฐานอยู่ที่ `docs/AS_BUILT.md` · เอกสารกับของจริงขัดกันเมื่อไร **ของจริงชนะเสมอ** (`docs/AS_BUILT.md:6-7`)

### 2.1 · ใครทำอะไรจริง (แทนที่ตาราง 4 ชั้นของแผนเดิม)

| บ้าน | บทบาทจริงวันนี้ | หลักฐาน |
|---|---|---|
| **cim-server / vendor-ksk (PHP)** | **ประตูหน้าของ LINE** (รับ webhook เอง ตอบ 200 ใน ~100 ms) · **สมอง** — ชั้นตอบ 6 ชั้น · เรียก LLM เองผ่าน `config/ai.config.php` · **เขียนฐานทุกอย่าง** · **เจ้าของความจริง** | `AS_BUILT.md:17` · `AS_BUILT.md:52` · `vendor-ksk/api/line_webhook.php:111` |
| **Cloudflare `aim-ingest` + D1** | **สำเนาอ่านอย่างเดียว** + **เครื่องตอบ 20 intent ด้วย SQL ล้วน ไม่มี LLM สักจุดเดียว** · cron ทุก 1 นาที · สรุปรายคนวันละ 2 รอบ 08:00 / 17:00 · **deploy เองด้วย Workers Builds ทุก push เข้า `main`** | `AS_BUILT.md:15` · `worker/src/queryEngine.ts:82-238` · `worker/wrangler.toml:13-14` · `worker/src/summary.ts:103-104,156` · D-2026-09-10-ข ข้อ 2 |
| **homelab (เต้น้อย)** | **ไม่ใช่สมองแล้ว** เหลือ 4 บทบาท: คลังไฟล์รูป (D-009) · `team_chat_day_summary`/`team_monitor_status` ประกอบ DSR · ทะเบียนชื่อ `team_members` · heartbeat | `AS_BUILT.md:16` |
| **GitHub** | **จุดควบคุมเดียว** — "ยังไม่อยู่บน `main` = ยังไม่เสร็จ" · ฝั่ง LIVE ดึง `main` เองทุก 1 นาที · ฝั่ง Worker build+deploy เองทุก push | D-2026-09-10-ข ข้อ 1–3 |

**สองข้อที่แผนเดิมเขียนไว้แต่ไม่เกิดขึ้นจริง — พูดตรง ๆ กันเข้าใจผิด:**
- **Notion ไม่เคยถูกใช้เลยสักครั้ง** (`AS_BUILT.md:18`) ⇒ "ชั้น ④ GitHub + Notion" ของแผนเดิมเหลือแค่ GitHub
- **KV ไม่ได้ใช้** — `worker/wrangler.toml` มีแต่ binding D1 ตัวเดียว (`worker/wrangler.toml:7-11`) ไม่มี `kv_namespaces` เลย
  (แผนเดิมเขียนว่า "เก็บสถานะงาน (D1/KV)" — ดู §2-เดิม)

### 2.2 · ทางเดินข้อมูลจริง

```
 [LINE]  พนักงานที่ผูกบัญชีแล้ว (แชท 1:1 + กลุ่มงานจริง)
    │
    ▼  webhook
 ┌──────────────────────────────────────────────────────────────┐
 │  cim-server / vendor-ksk — api/line_webhook.php              │
 │  ├─ ตอบเอง: ชั้นตอบ 6 ชั้น + LLM → reply/push กลับ LINE       │
 │  └─ aim_forward_events()  ── fire-and-forget ──┐             │
 │     (lib/aim_forward.php:21,70 · webhook:111)  │             │
 └────────────────────────────────────────────────┼─────────────┘
                                                  ▼
                                  POST /ingest/line   ┌──────────────────────┐
                                  ──────────────────► │  Worker `aim-ingest` │
   tbl_task (LIVE = เจ้าของความจริง)                   │  + D1 `aim-db`       │
   ── ทุก 10 นาที · ทางเดียว ──► POST /sync/tasks ───► │  cron ทุก 1 นาที      │
   (bin/aim_task_sync.php:108-121 · D-P0-18)          │  สรุป 08:00 / 17:00   │
                                                      └──────────┬───────────┘
                                                                 │ AIM_NOTIFY_URL
                                                                 ▼
                                       vendor-ksk api/aim_notify.php ──► LINE push
```

🔴 **ข้อบังคับที่เปลี่ยนวิธีคิดทั้งแผน: Worker ไม่มี LINE token**
`Env` ของ Worker มีแค่กุญแจ/URL 8 ค่ากับ D1 binding — **ไม่มี channel token ของ LINE สักตัว** (`worker/src/types.ts:1-13`)
⇒ **ทุกข้อความขาออกต้องวิ่งกลับมาออกทาง cim-server** ผ่าน `AIM_NOTIFY_URL` (`worker/src/outbound.ts:28-42` · `worker/src/summary.ts:221`)
⇒ **cim-server ล่ม = คลาวด์ใบ้ทันที** และการย้ายตรรกะขึ้นคลาวด์ทีละชิ้น **ไม่แก้ข้อนี้เลยสักชิ้น** (`AS_BUILT.md:22-24`)

### 2.3 · ทำไมถึงต่างจากแผน และอันไหนดีกว่า

รับ 4 ข้อนี้เป็นมาตรฐาน ไม่ต้องดัดกลับให้เหมือนแผนเดิม (`AS_BUILT.md:50-53`):

1. **สมองอยู่ที่ cim-server (ชั้น ③ ของแผนเดิม) ดีกว่า** — ข้อมูลจริงและสิทธิ์เขียนอยู่ที่นั่น ตอบจากที่เดียวกับที่เขียน = ไม่มีความจริงสองชุด
2. **คลาวด์ไม่มี LLM โดยตั้งใจ** — ตอบด้วย SQL + จับคำตายตัว ⇒ ไม่หลอน ไม่กินโควตา ตรงกับกติกา "ไม่มั่นใจ = ไม่เดา"
3. **ความไวที่แผนหวังจากชั้น ① ได้จากเครื่องเราเองแล้ว** — webhook spool ตอบ 200 ใน ~100 ms โดยไม่ต้องมี Worker คั่นหน้า
4. **ฟีดกลุ่มย้ายมาอยู่ฐานเรา (`gf_serves`)** ⇒ homelab หลุดจากเส้นทางวิกฤตของงานประจำวัน

### 2.4 · ที่เบี่ยงแล้วเป็นหนี้ — สถานะเท่าที่ยืนยันได้ (`AS_BUILT.md:55-67`)

| # | หนี้ | สถานะ |
|---|---|---|
| 1 | `person_link` ต้องรัน `bin/aim_link_person.php --all` ด้วยมือ ⇒ คนที่เพิ่งได้สิทธิ์โดนคลาวด์ตอบ 403 "บัญชียังไม่เปิด" (D-P0-14 ข้อ 1) | ✅ **ปิดแล้ว 2026-09-10** — ผูกให้เองเมื่อคลาวด์ตอบ 403 ครั้งแรก (D-2026-09-10-ก "ทำแล้ววันเดียวกัน") |
| 2 | 4 คิวรีใน `worker/src/db/queries.ts` ไม่มีตัวกรองสถานะ (บรรทัด 222 · 333 · 452 · 553/584) — วันนี้รอดเพราะบังเอิญ ไม่ใช่เพราะมีด่าน | ✅ **ปิดแล้ว 2026-09-10** — ตัวกรองสถานะใน `worker/src/db/queries.ts` ครบทุกคิวรี (`f84ff1c` · ตรวจซ้ำ 7 จุดใน D-2026-09-10-จ) · vitest ยิง endpoint จริง (`worker/test/endpoints.test.ts`) |
| 3 | กระจกงานเป็นครึ่งใบ — ทุกใบเป็น `L-` ⇒ `applyTransition()` ปฏิเสธตั้งแต่บรรทัดแรก (`worker/src/taskMachine.ts:85-87`) ⇒ `task_event` ไม่มีวันมีแถว ⇒ intent สถิติตอบจากข้อมูลที่ไม่มีทางมี | ✅ **ปิดแล้ว 2026-09-10** — ทางที่เคาะ (ข้อ 3.5): ตอบตรง ๆ ว่ายังไม่มีข้อมูล ไม่แต่งตัวเลข (`f84ff1c` · `worker/src/queryEngine.ts` case `stats_avg_cycle` / `stats_fastest_accept`) |
| 4 | `outbound_queue` · `sync_state` สร้างไว้แต่ไม่มีโค้ดอ้างถึงเลย | ✅ **จดว่าไม่ใช้แล้ว 2026-09-10** (ข้อ 3.6 · `README.md` ตารางสคีมา) — ลบรอบถัดไปตามกติกา P5 ข้อ 3 (เฝ้าดูก่อนแล้วค่อยลบ) |
| 5 | ตัวนับสตรีค P4 แยกไม่ออกระหว่าง "ไม่ตรง" กับ "ยังไม่ได้วัด" (วันที่ยามยังไม่รัน สตรีคกลายเป็น 0 ทันที) | ✅ **แก้แล้ว 2026-09-10** — แยก "ไม่ตรง / ยังไม่ถึงรอบวัด / ยามไม่ได้รัน" (D-2026-09-10-ก) |
| 6 | เอกสารล้าสมัย — `aim/CLAUDE.md` เคยเขียนว่า "ไม่มี remote · branch master · CI ไม่เคยรัน" · ใบ WP-P0-5 อ้างว่าขาด `bin/smoke_aim_forward.php` ทั้งที่ไฟล์มีจริง | ✅ **ปิดแล้ว 2026-09-10** — `aim/CLAUDE.md` §0 กติกา GitHub-first + §3 สถานะเฟสตรงของจริง · ใบปิด WP-P0-5 และ WP-P0-6 อยู่ใน `docs/WORK_PACKAGES_P0.md` (`bin/smoke_aim_forward.php` 16/16) |

### 2.5 · คำตัดสินที่บังคับงานต่อจากนี้ (อ่านก่อนเสนออะไรใหม่)

| คำตัดสิน | บังคับอะไร |
|---|---|
| **D-P0-01** | คลาวด์เก็บได้แค่ **metadata** (รหัสแทนตัว · แผนก · บทบาท) — ชื่อจริง/เหตุผลลาห้ามออกจากบ้าน · `body_ref` ต้องเป็น NULL เสมอ |
| **D-P0-18** | **`tbl_task` ฝั่ง LIVE คือเจ้าของความจริง** · คลาวด์เป็นสำเนาอ่านอย่างเดียว · `POST /sync/tasks` เป็นทางเดียว **ห้ามสร้าง endpoint เขียนกลับ** |
| **D-2026-08-29-ก** | **ปลายทางยังเป็น Cloudflare** — aim คือ jigsaw ชิ้นแรกที่พิสูจน์ pattern (ข้อนี้ยังมีผล ไม่ถูกยกเลิก) |
| **D-2026-09-10-ก** | **เปลี่ยนทางเดิน ไม่เปลี่ยนปลายทาง**: ปิดหนี้ที่รู้แล้ว → แล้วค่อย mirror ตารางอ่านทีละตัว · **ด่านหน้า LINE บนคลาวด์ไม่ได้ทำ** เพราะวัด downtime ที่พิสูจน์ได้แล้ว = **0 ชั่วโมง/เดือน** ทั้ง 30 และ 60 วัน (`DECISIONS_P0.md:1046-1048`) · และไม่ย้ายสมองทีละชิ้นเพราะไม่แก้จุดตายจุดเดียว (§2.2) |
| **D-2026-09-10-ข** | **GitHub-first** — "ยังไม่อยู่บน `main` = ยังไม่เสร็จ" · เลิกแก้ไฟล์ live บนเครื่องตรง ๆ รวมถึง AI ทุกตัว |

---

## 2-เดิม · สถาปัตยกรรมที่วางไว้ 2026-08-27 (ประวัติ — ไม่ใช่ของจริงแล้ว)

> **ของจริงวันนี้อยู่ที่ §2 ด้านบน** — หัวข้อนี้เก็บไว้เป็น "เจตนาเดิม" ของ 2026-08-27 เท่านั้น **ห้ามลบ** (`docs/AS_BUILT.md:6`)
> ต่างจากของจริงตรงไหน มีตารางเทียบรายชั้นพร้อมหลักฐานที่ `docs/AS_BUILT.md` §1 (`AS_BUILT.md:11-24`)

```
   [LINE]  ผู้ใช้ 11 คน + กลุ่มทดสอบใหม่
      │
      ▼  webhook (public HTTPS ฟรี ไม่ต้องเปิดพอร์ตบ้าน)
 ┌────────────────────────────────────────────────┐
 │  ① ชั้นหน้า — Cloudflare Workers               │
 │  · รับ webhook ตอบภายในไม่กี่ ms               │
 │  · เข้าคิว แล้วจบคำขอทันที (สเปค §32)          │
 │  · Cron Trigger = นาฬิกาของระบบ                │
 │    ★ อยู่นอกทั้ง cim-server และ homelab        │
 │  · เก็บสถานะงาน (D1/KV)                        │
 └───────────────┬────────────────────────────────┘
                 │ คิวงาน (ดึงออกไปทำ)
                 ▼
 ┌────────────────────────────────────────────────┐
 │  ② ชั้นสมอง — homelab (เต้น้อย)                 │
 │  · งานหนัก/ยาว ที่ Worker ทำไม่ได้             │
 │  · เรียก LLM · วิเคราะห์รูป · สรุป             │
 │  · ถือ token OA กลุ่ม (พูดในกลุ่มได้อยู่แล้ว)   │
 │  · Docker restart:unless-stopped (ฟื้นเองได้)  │
 └───────────────┬────────────────────────────────┘
                 │ ขอข้อมูลจริง (read-only)
                 ▼
 ┌────────────────────────────────────────────────┐
 │  ③ ชั้นความจริงเดิม — cim-server (เครื่องนี้)   │
 │  · **อ่านอย่างเดียว** ตามคำสั่งพี่เต้           │
 │  · เปิดท่ออ่านให้ 1 ทาง (ดู §3)                │
 │  · FIM/CIM/vendor-ksk ทำงานเหมือนเดิมทุกอย่าง  │
 └────────────────────────────────────────────────┘

 ┌────────────────────────────────────────────────┐
 │  ④ ชั้นเครื่องมือคน — GitHub + Notion          │
 │  · GitHub: เก็บโค้ด + Actions deploy อัตโนมัติ │
 │  · Notion: จอให้พี่เต้ดู/แก้ "กฎ" โดยไม่ต้อง   │
 │    เขียนหน้าเว็บเอง (ประหยัดแรงมหาศาล)         │
 └────────────────────────────────────────────────┘
```

### ทำไมแบ่งแบบนี้

| ชั้น | เหตุผลที่ต้องอยู่ตรงนั้น |
|---|---|
| ① Cloudflare | LINE ต้องการ **public HTTPS ที่ตอบเร็ว** · Worker ฟรี ไม่ต้องดูแลเครื่อง · **Cron อยู่นอกทั้งสองเครื่อง** ⇒ เครื่องดับก็ยังมีนาฬิกาเดิน (เหตุผลเดียวกับที่เลือกทำ dead-man switch ที่นี่) |
| ② homelab | งาน LLM/รูป กิน CPU เกินขีดของ Worker · **ถือ token กลุ่มอยู่แล้ว** · ไม่มีค่าใช้จ่ายเพิ่ม |
| ③ cim-server | ข้อมูลจริงอยู่ที่นี่และ **ห้ามย้าย** · ทำหน้าที่เดียว: ให้อ่าน |
| ④ GitHub/Notion | โค้ดต้องมีที่อยู่ถาวรนอกเครื่อง · Notion แทนการเขียนหน้าเว็บจัดการกฎ |

---

## 3. ท่ออ่านจากเครื่องนี้ — เลือกแบบ "ยื่นออก" ไม่ใช่ "ให้เข้ามาดึง"

**ข้อเสนอ: cim-server เป็นฝ่ายส่งออกอย่างเดียว (outbound push)** ไม่เปิดทางให้ cloud เข้ามา

| ทางเลือก | ข้อดี | ข้อเสีย | ตัดสิน |
|---|---|---|---|
| A. เปิด API ให้ cloud เรียกเข้ามา | ข้อมูลสดที่สุด | ต้องเปิดผิวสัมผัสสาธารณะเพิ่ม + ต้องดูแลสิทธิ์/rate limit | ❌ |
| B. **cim-server ยิงสรุปออกไปเป็นจังหวะ** | ไม่เปิดพอร์ตใหม่เลย · แพทเทิร์นเดียวกับ heartbeat ที่ใช้ได้ผลมาแล้ว 646 รอบ | ข้อมูลช้าตามจังหวะที่ตั้ง | ✅ **เลือกข้อนี้** |
| C. ผ่าน tailnet ให้ homelab ดึง | ไม่ออกอินเทอร์เน็ต ปลอดภัยสุด | Cloudflare เข้าไม่ถึง ต้องผ่าน homelab เสมอ | ✅ ใช้คู่กับ B สำหรับข้อมูลอ่อนไหว |

**สิ่งที่ยิงออก:** สถานะงาน/ลา/เครื่องมือแบบสรุป — **ไม่ใช่ทั้งฐาน**
**สิ่งที่ห้ามออกจากเครื่องนี้เด็ดขาด:** ข้อมูลศุลกากรทุกชนิด · รหัสผ่าน · เลขใบขน · ราคา/มูลค่า

---

## 4. เรื่องข้อมูลออกนอกบริษัท — ต้องตัดสินก่อนเริ่ม (สำคัญที่สุดในแผนนี้)

พี่เต้พูดเองว่าบริการเหล่านี้ *"กำลังจ้องเทรน AI ตัวเอง เลยปล่อยฟรีเยอะ"* — ถูกต้อง และนั่นแปลว่า **ต้องเลือกว่าอะไรออกไปได้**

**ข้อเสนอ: ใช้รหัสแทนคน (pseudonym) บนบ้านนอก**

| ข้อมูล | บนคลาวด์เก็บอะไร | ชื่อจริงอยู่ที่ไหน |
|---|---|---|
| คน | `W07` · แผนก · บทบาท | cim-server เท่านั้น |
| งาน | หัวข้องาน · สถานะ · เวลา | เหมือนกันทั้งสองที่ |
| ใบลา | ประเภท · ช่วงวัน | **เหตุผลการลาไม่ออกจากบ้าน** |
| รูป | ลิงก์ + ผลวิเคราะห์ | ไฟล์จริงอยู่ homelab (D-009 ตัดสินไว้แล้ว) |

⇒ ต่อให้ผู้ให้บริการเอาข้อมูลไปเทรน ก็ได้แค่ "W07 ปิดงานช้า 2 ใบ" ไม่ใช่ชื่อพนักงานจริง
⇒ ต้นทุน: ต้องมีตารางแมป และทุกข้อความที่ส่งถึงคนต้องแปลงกลับเป็นชื่อจริง **ที่ชั้น ② หรือ ③ เท่านั้น**
(เลขชั้นอ้างตาม **§2-เดิม** · ของจริงวันนี้แปลงกลับที่ cim-server เท่านั้น — ดู §2.1)

---

## 5. กฎกันติดกับดักของฟรี (ต้องมีตั้งแต่บรรทัดแรก)

1. **ฐานข้อมูลต้องย้ายบ้านได้** — ใช้ SQL มาตรฐาน · ห้ามใช้ฟีเจอร์เฉพาะเจ้าใดจนย้ายไม่ได้
2. **ส่งออกได้ทุกวัน** — dump ข้อมูลกลับมาเก็บที่ cim-server อัตโนมัติ (ของฟรีปิดบริการได้ทุกเมื่อ)
3. **ตรรกะธุรกิจห้ามอยู่ใน Notion/บริการสำเร็จรูป** — Notion เป็น "จอ" เท่านั้น ความจริงอยู่ในฐานของเรา
4. **ทุกบริการต้องมีแผนสำรอง 1 บรรทัด** เขียนไว้ล่วงหน้าว่าถ้าเจ้านี้ปิด ย้ายไปไหน
5. **ห้ามผูกบัตรเครดิตกับบริการที่คิดเงินตามการใช้** โดยไม่มีเพดาน (Firebase Functions ต้องระวังข้อนี้)

---

## 6. ลำดับงาน (เฟส) + ประตูที่ห้ามข้าม

### P0 · ท่อและกระดูก (ยังไม่มีฟีเจอร์)
- repo ใหม่บน GitHub · Actions deploy ขึ้น Cloudflare อัตโนมัติ
- Worker รับ webhook + คิว + ฐาน + Cron ว่าง ๆ ที่ยังไม่ทำอะไร
- ท่อ read-only จาก cim-server (แบบ B) ยิงข้อมูลสรุปออกทุก N นาที
- **ประตู:** ข้อความจากกลุ่มทดสอบเดินทางครบวง (LINE → Worker → คิว → homelab → เขียนฐาน → ตอบกลับกลุ่ม) และมี log ทุกจุด

### P1 · ประสาทสัมผัส (สเปค §7–§11)
งาน + สถานะครบ 12 + รับ/ปฏิเสธ/กำลังไป/ถึงแล้ว/รูป/ปิดงาน + idempotency
- **ประตู:** เดินครบเส้นทางในกลุ่มทดสอบ · ยิงซ้ำ 3 ครั้งไม่เกิดงานซ้ำ

### P2 · สายตา + ปาก (ตอบข้อ ① ของพี่เต้)
read model + ตอบคำถามจากข้อมูลจริง + สรุปเช้า-เย็น + **สิทธิ์การมองเห็นรายคน**
- **ประตู:** 20 คำถามมาตรฐาน ไม่มีคำตอบไหนแต่งขึ้น · ลูกน้องถามงานคนอื่นต้องไม่เห็น

### P3 · ความคิดริเริ่ม (สเปค §15–§17)
watcher + escalation 4 ระดับ + ชั้นกฎ AUTO/PROPOSE/ASK + สมุดบันทึกการตัดสิน
- **ประตู:** จำลอง 10 สถานการณ์ได้ผลตามกฎทุกครั้ง · ไม่เตือนซ้ำเกิน 1 ครั้งต่อระดับ · **นับโควตา push ที่ใช้จริงต่อวัน**
- **สถานะ:** ใบปิดกำลังเขียนวันนี้ (2026-09-10) — **ดู `docs/WORK_PACKAGES_P3.md`** (ไฟล์นั้นเป็นเจ้าของคำตอบ
  ห้ามสรุปสถานะ P3 จากที่นี่)

### P4 · ทดสอบเต็มระบบ (คำสั่งข้อ ⑤)
รันคู่ของเดิม ≥ 2 สัปดาห์ในกลุ่มทดสอบ + คนจริงกลุ่มเล็ก · เทียบผลทุกคืน
- **ประตู:** ผลตรงกัน 100% ติดกัน 7 วัน · โควตา push ต่อเดือนไม่เกินที่มี
- ✅ **ผ่านประตูแล้ว** — reconcile `tbl_task` ↔ D1 **ตรงกัน 100% ติดกัน 12 วัน** (30 ส.ค.–10 ก.ย. · 31 ใบ)
  · LINE push 610 / 15,000 ต่อเดือน วัด 27 ส.ค. (`AS_BUILT.md` §2 ตาราง) · วัดใหม่ 2026-09-10 ที่ 12 คน = 79.4/วัน ⇒ ~2,381/เดือน (16% — `CLOSEOUT_GATES.md` §0)
  ⚠ ตัวนับสตรีคที่เคยรายงาน 0 ทั้งที่ข้อมูลตรง 12 วัน **แก้แล้ว 2026-09-10** (ดู §2.4 ข้อ 5)

### P5 · ทยอยย้ายระบบขึ้น Cloudflare ทีละชิ้น (แก้ 2026-08-29 ตาม D-2026-08-29-ก)
> ฉบับเดิมเขียนว่า "ย้ายของจริง แล้วค่อยตัดสาย CIM · แก้ 12 ไฟล์ CIM" — **ตัดสาย CIM จบไปแล้ว**
> (นับใหม่ได้ 0 ไฟล์ ไม่ใช่ 12 — เว็บ CIM ตายตั้งแต่ 23 ก.ค. ก่อนแผนนี้ถูกเขียน)
> เจ้าของระบบเปิดเผยเป้าหมายจริงที่ตัดสินไว้แล้ว: **aim คือ jigsaw ชิ้นแรกที่พิสูจน์ pattern
> "ระบบบน Cloudflare" — ถ้าได้ผล จะทยอยย้าย เครื่องมือ → 5ส → ลา → งาน → LINE bot ตามไปทีละชิ้น**
> เพื่อออกจากสภาพเครื่องเดียวรันทุกอย่าง

แต่ละระบบเดินลำดับขั้นเดียวกับที่ aim เดินผ่านมาแล้ว (รายละเอียดเต็มใน D-2026-08-29-ก):
**1. Mirror ทางเดียว → 2. Dual-run เทียบผล 7 วันติด → 3. สลับความเป็นเจ้าของ (คำตัดสินรายระบบ
+ แผนถอยกลับ) → 4. เฝ้าดู แล้วค่อยตัดของเก่า**
- **ประตูของขั้น 2 ทุกระบบ = ประตูเดียวกับ P4:** ตรงกัน 100% ติดกัน 7 วัน (reconciler + streak)
- **ประตูของขั้น 4:** ยามยืนยันว่าของเก่า "หยุดจริง" ไม่ใช่แค่ของใหม่ทำงาน
- D-P0-18 (ความจริงอยู่ LIVE) ยังบังคับใช้กับทุกระบบจนกว่าระบบนั้นถึงขั้น 3 ของตัวเอง
- ⛔ **ยังไม่เริ่ม** — ยังไม่มีระบบไหนถึงขั้น 3 (สลับความเป็นเจ้าของ) · **สลับเจ้าของ `tbl_task` ไปคลาวด์ = ตัดสินแล้วว่าไม่ทำ**
  (mirror ตรง 12 วันติดคือหลักฐานว่ารูปแบบปัจจุบันใช้ได้ · ปุ่ม LINE ทุกปุ่มเป็น postback ที่มาถึงเครื่องในบ้านเท่านั้น
  · **ยังไม่มีสคริปต์ถอยกลับ**) — `AS_BUILT.md:91-98` · D-2026-09-10-ก
- ทางเดินที่เคาะใหม่ 2026-09-10 (D-2026-09-10-ก): **ปิดหนี้ §2.4 ก่อน → แล้วค่อย mirror ตารางอ่านทีละตัวตาม D-P0-18**
  · ด่านหน้า LINE บนคลาวด์ **ไม่ทำ** เพราะ downtime ที่วัดได้ = 0 ชั่วโมง/เดือน (`DECISIONS_P0.md:1046-1048`)
- **เดินเมื่อมีเหตุเท่านั้น (D-2026-09-10-ฉ · 2026-09-10 ดึก):** ไม่ mirror ตารางใดล่วงหน้า — เริ่มเมื่อ (ก) คำถามที่คลาวด์ตอบไม่ได้ ≥ 5 ครั้ง/7 วัน จากคน ≥ 2 · (ข) ล่มช่วงเวลางาน ≥ 1 ครั้ง/เดือน · (ค) เจ้าของระบบสั่ง — ตัววัดข้อ (ก) = `tbl_aim_ask_miss` ฝั่ง LIVE อ่านใน `bin/selfcheck.php`

---

## 7. แบ่งงานให้ AI (ใช้ CCR ให้คุ้ม)

| ใคร | ทำอะไร | ทำไม |
|---|---|---|
| **Claude (ที่นี่)** | สถาปัตยกรรม · เขียนใบสั่งงาน · **ตรวจงาน** · ตัดสินเมื่อชนกติกาบ้าน | ถือกติกาบ้านครบคนเดียว |
| **Codex** | เขียนโค้ด Worker/บริการ/เทส/migration | สร้างเร็ว · **ห้ามตรวจงานตัวเอง** |
| **โมเดลฟรีผ่าน CCR** | ร่างเทสเคส · seed · แปลงข้อความ · เทียบไฟล์ · สรุป diff | ไม่กินโควตา Claude · ผิดแล้วเห็นทันที |
| **Claude ฝั่ง homelab** | งานบนเครื่องนั้น (ชั้น ② ตาม **§2-เดิม**) ตามสัญญาสะพานเดิม | เจ้าของพื้นที่ |
| **code-reviewer / reconcile-checker** | ด่านสุดท้ายก่อนถือว่าเสร็จ | กติกาบ้านข้อ 5 |

**ใบสั่งงานทุกใบต้องมี 6 ช่อง:** เป้าหมาย · ไฟล์ที่แตะได้/ห้ามแตะ · สัญญา input-output · เกณฑ์ผ่าน · ยามที่ต้องยังเขียว · เจออะไรให้หยุดถาม

---

## 8. ความเสี่ยง + วิธีคุม

| # | ความเสี่ยง | วิธีคุม |
|---|---|---|
| 1 | ~~โควตา LINE push หมด~~ **ตกไป** — วัดจริงแล้ว 15,000/เดือน ใช้ 610 | ยังนับการใช้ต่อวันไว้ในสมุดบันทึก + เตือนเมื่อถึง 70% (กันวันที่ระบบวนลูปส่งซ้ำ) |
| 2 | ข้อมูลพนักงานออกนอกบริษัท | ใช้รหัสแทนชื่อ · เหตุผลลาไม่ออกจากบ้าน (§4) |
| 3 | ของฟรีเปลี่ยนเงื่อนไข/ปิดบริการ | ย้ายบ้านได้ + dump กลับทุกวัน (§5) |
| 4 | บอทพูดซ้อนกัน 2 ตัวในกลุ่ม | ช่วงทดสอบใช้ **กลุ่มใหม่แยก** ตามที่พี่เต้สั่ง · กลุ่มจริงยังเงียบจนกว่าจะเคาะ |
| 5 | ระบบใหม่ยังไม่ดีพอแต่ปิดของเก่าแล้ว | dual-run + เกณฑ์ 7 วันติด |
| 6 | เครื่อง cim-server ดับ = ท่ออ่านขาด | บ้านใหม่ต้องทำงานต่อได้ด้วยข้อมูลที่มี + บอกชัดว่า "ข้อมูลเก่ากี่นาที" ไม่ใช่เงียบ |

---

## 9. ต้องพี่เต้ตัดสิน/จัดให้ ก่อนเริ่ม P0

1. **Cloudflare API token** (สิทธิ์ `Workers Scripts:Edit`) — วางที่ `config/mcp_paste.txt` แบบเดียวกับตอนต่อ MCP
   *หรือ* ให้ homelab เป็นคน deploy แทน (ถ้าไม่อยากให้ token อยู่เครื่องนี้)
2. **ข้อ §4 เรื่องข้อมูลออกนอกบริษัท** — เอาแบบใช้รหัสแทนชื่อไหม (แนะนำ) หรือให้ออกได้ทั้งชื่อจริง
3. **กลุ่มทดสอบใหม่** — พี่เต้สร้างกลุ่มแล้วแอด OA เข้าไป · ฝั่งนี้ต้องได้ `groupId` ซึ่งได้เฉพาะตอนบอทถูกเชิญ ⇒ เชิญเมื่อไรบอกด้วย จะได้เก็บเลขไว้
4. **repo ใหม่** — สร้างใหม่แยก หรือใช้ `KSK-Decepticons` เดิมเป็น monorepo

### 9.1 · ประเมิน Origin (Cursor) แทน GitHub — พี่เต้เสนอ 2026-08-27

Origin = บริการโฮสต์โค้ดของ **Cursor/Anysphere** (ควบรวมเข้าเครือ SpaceXAI 14 ส.ค. 2026) เปิด early beta **17 ส.ค. 2026**

| เกณฑ์ที่แผนนี้ต้องการ | Origin | GitHub |
|---|---|---|
| ฟรี | ❌ ไม่มี free tier — ต้อง Cursor Pro ขึ้นไป | ✅ private repo ฟรี |
| **CI สำหรับ deploy ขึ้น Cloudflare อัตโนมัติ** | ❌ ไม่มีในตัว ต้องต่อ Depot/Buildkite/Vercel | ✅ Actions ฟรี 2,000 นาที/เดือน |
| public repo | ❌ เบต้ายังไม่รองรับ | ✅ |
| quota / rate limit / SLA | ❌ ยังไม่ประกาศ | ✅ |
| sync สองทาง | ✅ commit/branch/tag/PR — **แต่ไม่ sync Actions** | — |

**ข้อสรุป (ปิดแล้ว 2026-08-27):** ❌ **ไม่ใช้ Origin** — ชนหลักการที่ตั้งไว้เอง 2 ข้อ (ต้องฟรี · ต้องมี CI)
และพี่เต้ยืนยันว่า **ไม่มี Cursor Pro แล้ว** (เคยมี) ⇒ **ใช้ GitHub อย่างเดียว** · ห้ามเสนอซ้ำจนกว่า Origin จะมี free tier + CI ในตัว
~~แต่ใช้เสริมได้ถ้าพี่เต้มี Cursor Pro อยู่แล้ว:~~ `GitHub = บ้านจริง (CI+deploy)` · `Origin = โต๊ะทำงานของ agent` sync สองทางอัตโนมัติ · ย้ายทีหลังได้เพราะ sync สองทาง (ตรงกับกฎ §5 ข้อ 1 "ต้องย้ายบ้านได้")

---

## 10. สิ่งที่แผนนี้ไม่ทำ

- ไม่แตะข้อมูล/โค้ดศุลกากร
- ไม่ให้บ้านใหม่ **เขียน** ลงฐานของ cim-server (อ่านอย่างเดียวตามคำสั่ง)
- ไม่ปิดของเก่าก่อนพิสูจน์
- ไม่ให้ AI ตัดสินเรื่องคน/เงิน/สิทธิ์เอง
- ไม่ผูกบัตรเครดิตกับบริการที่คิดเงินตามการใช้โดยไม่มีเพดาน
